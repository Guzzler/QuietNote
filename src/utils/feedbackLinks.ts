/**
 * Static feedback link-outs (human-feedback F1).
 *
 * User-initiated only: plain hrefs opened by the user, never a fetch, and
 * never a query param — nothing may prefill or attach journal content,
 * moods, or any other user data. Keep these as bare constants.
 */
export const FEEDBACK_ISSUES_URL =
  "https://github.com/Guzzler/QuietNote/issues/new/choose";

export const FEEDBACK_MAILTO = "mailto:sharangpaiusa@gmail.com";
