# QuietNote Roadmap

**Created:** 2026-06-10. **Owner:** Sharang. This is the planner's backlog source: when the critic-report BUILD backlog is empty, the `make-quiet-note-better` planner MUST pick the highest unstarted item here (respecting `PHASE.md`). Update item status in place; record outcomes in `docs/decisions.md` as usual.

**Inputs triaged into this roadmap:** external UX spec (2026-06-10, second-agent review), real-user data analysis ([`field-notes/2026-06-09-real-user-data-plan.md`](field-notes/2026-06-09-real-user-data-plan.md)), Day 1–9 eval/critic history, [`design/tool-calling-architecture.md`](design/tool-calling-architecture.md) (2026-03-12).

## Status of the original fundamental problems

| # | Problem | Status |
|---|---|---|
| 1 | Guided modes vanish on first keystroke | **FIXED** (sticky banner, PR #40) |
| 2 | 2048-token context window | **FIXED + closed at population scale** (4096 + prior-turn entity surfacing, PR #53). The residual long-conversation risk is now measured: the Day-19 `eval:scripts:all` population run (4 scripts × 3 strategies) shows the real app path holding all post-trim entity probes and thoughtrecord 5-step coherence → see Track C / `docs/eval-runs/2026-06-21/C2-population-findings.md` |
| 3 | Model response quality untested | **MANAGED** — the daily eval→tune loop owns this (Days 1–9: 54–55/56 collateral, empathy 43/44). Remaining items flow from new eval tracks |
| 4 | Feels like a tech demo | **RESOLVED via Track A (A1–A6 all DONE, 2026-06-23)** — empty-state simplification, visual calm pass, privacy consolidation, inline mood check, sessions previews, and focus mode + keyboard polish all shipped. The daily eval→tune loop continues to own model-quality drift |

---

## Track A — Core experience (UX spec, triaged)

The external UX spec's core diagnosis is right and matches the real-user data: *the app shouts when it should whisper*, and the empty state is a dashboard when it should be an open page. ("Just start writing" drove 36% of real entries in the launched-app data.) But several of its specific calls misread the product — triage below.

### Accepted (priority order)

- **A1. Empty-state simplification** *(high, ~2 BUILD days incl. mockup day)* — One prominent writing invitation in the center; the 4 mode buttons become a quieter bottom strip (modes stay visible — gratitude/check-in are validated by real usage, but they're a style of entry, not a gate); welcome card loses the redundant privacy bullets and feature tour; streak becomes a quiet badge near today's entry, never onboarding copy. Mock first (screenshot-driven), then implement. — **DONE (day 1 shipped 2026-06-10, PR #56: WelcomeEmptyState extraction, quiet mode strip, streak → Sessions-panel badge, textarea auto-focus, no-scroll fit; day-2 residue — icon-tile softening + footer mojibake — folded into A2 day 1, PR #57)**
- **A2. Visual calm pass** *(high, 1–2 days)* — Warm off-white paper surface, reduce chrome, soft 12–16px radii, serif (e.g. Lora/Source Serif) for the writing/reading surface with sans for UI chrome only, one accent color. This is "tech demo → journal" made concrete and is screenshot-verifiable. — **IN PROGRESS (day 1 shipped 2026-06-11, PR #57: Lora serif writing surface, paper-flat conversation [one container, no hover glow], writing-path accent sweep purple/emerald→indigo, ghost-button header; day 2 = off-path accent sweep [MoodHistoryPanel/ThoughtRecordHistory purple, MoodTracker palette] only if screenshots show it jarring against the new surfaces)**
- **A3. Privacy signal consolidation** *(med, ≤1 day)* — One persistent subtle indicator (footer lock + "Stored on this device" — footer line already exists; converge on it). Remove the duplicated reassurances from welcome card and loading screen. Privacy dashboard moves behind Settings; drop the dedicated nav button. (Consistent with standing guidance: privacy is solid, stop re-selling it.) — **DONE (2026-06-14, PR #60: header Privacy (Shield) nav button removed; dashboard relocated into Settings as a "Privacy & your data" row via a new `onOpenPrivacy` callback (close Settings → open Privacy; PrivacyDashboard stays mounted, export/erase verbatim); footer converged to single lock + "stay on this device" indicator. Loading-screen left unchanged — the Lock line is download-timing info (honest/useful) and "private journaling companion" is brand framing, not a duplicated reassurance; welcome-card bullets already removed in A1 and guarded by WelcomeEmptyState.test.ts. UI-only: freeze gate empty, no safety-surface edits, no eval run. New PrivacyConsolidationGuards.test.ts.)**
- **A4. Inline mood check** *(med, 1 day)* — Replace the ambiguous header heart entry point with a gentle "How are you feeling?" dots/emoji row at entry start; selection becomes mood metadata automatically and feeds the existing mood-aware prompts. Keyword extraction stays as fallback. — **DONE (2026-06-15, PR #61: inline "How are you feeling?" chip row at freewrite entry start (`InlineMoodCheck.tsx` inside `WelcomeEmptyState`) → one tap builds a lightweight `MoodEntry` (intensity 5, empty contexts, optional sessionId) via new pure helper `quickMood.makeQuickMoodEntry` and logs it through the existing `onSaveMood`; inline `aria-live` confirmation + "add detail" opens the full `MoodTracker` pre-filled via the existing `onOpenMoodTracker`. Mood feeds the already-mood-aware `buildSessionContext` unchanged (no prompt/eval edit). Header Heart re-scoped to mood history/details (label/tooltip/aria → "Mood history & details"; opens History tab when mood data exists) — **NOT removed**, since it is the sole persistent entry to Mood History/Insights (deliberate correction of "replace": full removal would orphan built features). Keyword extraction kept as fallback. Calm palette only (slate chips, indigo on selection; `InlineMoodCheck` added to `VisualCalmGuards`). UI + mood-metadata only: freeze gate empty, no safety-surface edits, no eval run. New tests: `quickMood.test.ts` + `InlineMoodCheckGuards.test.ts`.)**
- **A5. Sessions sidebar previews** *(low, ≤1 day)* — 2–3-line first-line previews + date + mood dot (title/reflection/date partially exist). — **DONE (2026-06-17, PR #62: each Sessions row now reads like a journal index — preview falls back to the first user message when no AI reflection exists (`s.reflection?.trim() || firstUserMessage(s)`); the raw `toLocaleString()` date is replaced by a journal-friendly relative label (Just now / Nm / Nh / Yesterday / Nd / "Jun 14" / "Dec 25, 2025") with the full timestamp preserved on `title=` hover; a small per-session mood dot renders only when a mood truly relates to the session (sessionId match → time-window fallback → null, never invented). New pure utils `relativeTime.ts`, `emotionMeta.ts` (EMOTION_DOT/EMOTION_LABEL), `sessionPreview.ts` (firstUserMessage, pickSessionMood); `SessionsPanel` gains a `moods` prop wired from `App.allMoods` with a memoized session→mood index. UI + read-only derivation only: freeze gate evalRunner/evalScorer/prompts EMPTY, no crisis/guardrail/disclaimer/sessionContext edits, no eval run. `SessionsPanel` deliberately NOT added to WRITING_PATH_COMPONENTS (the colored dot is correct chrome). New tests: relativeTime.test.ts (7), sessionPreview.test.ts (7), SessionsPanelPreview.test.ts (6); full suite 1120/1120.)**
- **A6. Focus mode + keyboard polish** *(low, filler)* — Esc dims chrome to just the page; Cmd+N new entry; `/` opens prompt picker. Loading-screen copy softened to step-text ("Setting up your private space…") — keep the percentage; it's real download progress, contra the spec. — **DONE (2026-06-23, PR #67: distraction-free focus mode — a single global `keydown` handler (App.tsx, logic extracted to pure `utils/keyboardShortcuts.ts` `resolveShortcut`/`isTypingTarget`) owns all three shortcuts. `Esc` toggles `focusMode`: header collapses (`opacity-0 -translate-y-2 h-0`), sessions sidebar recedes (`lg:hidden`), footer fades, the centered writing surface stays, and a low-contrast "Press Esc to exit focus" hint is the only chrome that remains; modal-open takes precedence (Esc closes the modal, not focus mode) and the ChatPanel writing surface / safety affordances are NEVER `pointer-events-none`. `Cmd/Ctrl+N` → new entry via a shared extracted `handleNewSession` callback (also now backs the header New button; browser-reserved "new window" caveat is best-effort `preventDefault`). `/` → prompt picker via a new `open-prompt-picker` window event (mirrors the `open-crisis-resources` bridge); `isTypingTarget` guard keeps `/` a literal slash inside the journal textarea. **Loading-copy item was already satisfied** by `getLoadingMessage` step-text (A3-era) — left byte-identical, screenshot confirms no regression. UI-only: freeze gate (evalRunner/evalScorer/prompts) EMPTY, no crisis/guardrail/disclaimer/sessionContext edits, no eval run. New tests: `KeyboardShortcuts.test.ts` (14), `FocusMode.test.ts` (10); full suite 1213/1213.)** — **Track A COMPLETE.**

### Rejected from the spec (with reasons — do not re-plan these)

- **"Chat mode: frame as opt-in or cut it. Don't make it the default."** — REJECTED. The AI companion *is* the product; the entire eval/safety/empathy investment (Days 1–9) lives there, and "write → feel heard" is the north star. The legitimate kernel: the *presentation* could feel less like SMS and more like a journal with a thoughtful margin reply — treat as an exploratory A2 follow-up, never as removal/demotion.
- **Autosave + save-button removal** — already true; sessions autosave and no save button exists. Spec was written without reading the app.
- **Weekly reflection digest** — already exists (`WellnessSummary`). If anything, an A1/A2 discoverability tweak.
- **Per-entry privacy badge** — contradicts the spec's own consolidation point (A3). Local-only is global truth; a per-entry badge implies some entries might not be.
- **"Never show a percentage you can't honor"** — the percentage is honored (real model-download progress). Copy softening only.

## Track B — Input robustness (from real-user data)

Owned by the existing eval→tune loop. Spec: [`field-notes/2026-06-09-real-user-data-plan.md`](field-notes/2026-06-09-real-user-data-plan.md).

- **B1.** ✅ **DONE (2026-06-13, PR #58 merged).** Freeze-lift planning entry + 12 derived `input_robustness` eval cases (EVAL_CASES 63→75) wired through the scorer; fresh baseline run. **Result: 41/48 (85%) across 4 modes.** Failure cluster is Family 2 (gibberish/punctuation → model projects emotion instead of plainly clarifying): `ir-2.2` fails all 4 modes, `ir-2.1` fails 3/4 (passes freewrite). Families 1 (terse), 3 (positive-no-pathologize), 4 (toxic-positivity trap), 5 (load-bearing typo) all pass in all 4 modes. *(was: ultra-terse, gibberish→clarify-don't-project, positive→don't-pathologize, toxic-positivity traps, load-bearing typos.)*
- **B2.** ✅ **DONE (2026-06-13).** Fixed the gibberish/nonsense cluster (`ir-2.*`: clarify-don't-project) with **Mechanism A alone** — an UNINTELLIGIBLE INPUT RULE beat in all 5 prompts, scoped as an exception to the FIRST LINE RULE (do not invent a concrete detail or name an emotion on noise; plainly ask for clarification). **Result: `ir-2.1` + `ir-2.2` now PASS 4/4 modes each (was 1/4 + 0/4); input_robustness 41/48 → 46/48 (96%).** Mechanism B (deterministic guard) was not needed. No scorer/EVAL_CASES edits (freeze held). Collateral --per-dim=2 held at 62/64 with zero safety movement (both misses stochastic substring/format artifacts). The 2 residual input_robustness misses are both Family-5 `ir-5.1` stochastic typo-echo/verbosity flickers, independent of B2.
- **B3.** Prompt library seeding from validated high-pull prompts. *(piggybacks any UI day)*

## Track C — Long-conversation evaluation (new harness capability)

**Why:** today's empathy `mt-*` cases are 2–3 turns. Nothing measures a real 10–20-turn session: entity retention across many turns, guided-mode step coherence over a full 5-step thought record, trim behavior at the 4096 boundary, and response-quality drift late in a conversation. Problem 2 was "fixed" at 3-turn scale only.

- **C1. Conversation-script support in the harness** — **DONE (PR #63, 2026-06-18).** New `ConversationScript`/`ScriptTurn` types + `CONVERSATION_SCRIPTS` (3 scripts: freewrite-retention ~12 turns w/ 2 Maya/dinner probes, thoughtrecord-steps 5-step coherence, checkin-retention) in **new** `src/utils/conversationScripts.ts`; `runConversationScript` in **new** `src/utils/conversationDriver.ts` threads the model's REAL replies through turns (history accumulates), scores per-turn via reused `evaluateResponse` (`mustEchoPriorTurn` now echoes the real history, not fixtures), tracks retention-probe pass-rate + guided-mode step coherence; `scriptReportToMarkdown` writer; `run-eval.ts` gained a `--scripts` flag (wired, **not run live** this session — that is C2). Verified by deterministic mock-driven tests only (no model run); freeze gate (evalRunner/evalScorer/prompts) stayed EMPTY (all new code in new files). Freeze-lift recorded in `docs/decisions.md`.
- **C2. Baseline run + trim instrumentation** — **DONE (PR #64, 2026-06-19).** Added a selectable `ContextStrategy` (`raw` | `managed` [= real app path: `buildManagedMessages`, recap+trim] | `managed-norecap` [trim only]) + per-turn trim telemetry (`estHistoryTokens`, `trimmed`, `droppedTurns`, `recapPresent`, `probeEntityInWindow`) and `firstTrimTurnIndex` + before/after-trim probe split to **new** `conversationDriver.ts`; new `script-freewrite-longtrim` (19 turns, test-asserted to cross `AVAILABLE_FOR_HISTORY`); `run-eval.ts` gained `--strategy=…` and `--script=…`. **First live Gemma 4 E2B run** (Node onnxruntime-node CPU loaded in-slot — no browser blocker). Result on the boundary-crossing script: **the prior-turn recap is the load-bearing element** for entity retention — `managed` (recap+trim) **2/2** probes vs `raw` (full history) **0/2** and `managed-norecap` (trim only) **0/2**; the recap recovers the entity even after the trim drops 8 turns (first trim t14). Full history alone does NOT save brief-follow-up grounding (surface-word-latch persists at length). See `docs/eval-runs/2026-06-20/C2-findings.md`. Freeze gate stayed EMPTY (all new code in new files); token/context utils untouched.
- **C2b. Population run (full matrix)** — **DONE (Day 19, 2026-06-21).** Ran the full `eval:scripts:all` matrix: **all 4 scripts × 3 strategies = 12 live cells** (~141 CPU generations), adding the two never-run-live signals — **thoughtrecord 5-step coherence** and the **checkin-retention** probe — under the real app path. Population result: `managed` is **best-or-tied on every probe-bearing script** (freewrite-retention 2/2 vs raw 1/2 vs norecap 0/2; freewrite-longtrim 2/2 vs 0/2 vs 0/2; checkin-retention 1/1 = 1/1 = 1/1, that script never trims); **thoughtrecord `stepCoherent: true` under all strategies** (model walks CBT 1→5 contiguously on the real path); **zero scored-turn regressions** (all warmth/opener/length turns pass everywhere). Decisive post-trim split: only longtrim crosses the boundary, and under `managed` its post-trim probes are **2/2** (first trim t14) vs norecap **0/2** — replicating C2's n=1 on a fresh seed. See `docs/eval-runs/2026-06-21/C2-population-findings.md` (n=1 record at `docs/eval-runs/2026-06-20/C2-findings.md` preserved). Freeze gate stayed EMPTY; no source changed (pure measurement run).
- **C3. Fixes — DEFERRED, CONFIRMED (Day 19 population run shows no decay to fix).** The gate-reopener (*a fuller multi-script `eval:scripts:all` run shows `managed` dropping post-trim probes, or thoughtrecord coherence failing*) **did not trigger**: `managed` holds all post-trim probes across all probe-bearing scripts and thoughtrecord 5-step coherence holds. **No context-limit / `RESERVED_FOR_GENERATION` change is indicated. C3 stays deferred.** The candidate fix (summarize-trimmed-turns into the recap line, extends Day-8 `conversationContext.ts`) remains spec-only and freeze-lift-gated. Re-open only on a future run where `managed` drops a post-trim probe or thoughtrecord coherence fails. **→ Track C's measurement objective is COMPLETE; Problem #2 is declared closed at population scale (caveats: single 2B model, CPU q4f16, substring-match probes, one seed/cell). The next roadmap pick is D1 (tool-calling spike).**

## Track D — Local tool-calling framework (spike-gated)

**History:** the 2026-03-12 design explicitly chose *client-side keyword extraction* over model-side tool calling because Gemma 2 2B couldn't be trusted with structured output. Two things changed: the canonical eval model is now Gemma 4 E2B, and Day 8–9 built the exact machinery a tool loop needs (deterministic response-shape detection + one-shot re-prompt, DRY across app and eval paths). Worth re-testing — but **spike-gated, not committed**.

- **D1. Capability spike — DONE, ❌ NO-GO (2026-06-22).** Built the one-line grammar `<<tool:NAME arg=value>>` + pure parser/validator (`src/utils/toolCalls.ts`) + 20-case Node eval (`src/utils/toolCallEval.ts`, `npm run eval:tools`); live run on Gemma 4 E2B (Node CPU q4f16). **Result: valid-call rate 1/8 = 13%** (gate ≥80%) — **FAIL**; **false-call rate 0/12 = 0%** (gate ~0) — pass; argument accuracy 1/1 on the lone call. The retry was **not load-bearing** (0/8 fired) — every miss was *silence/prose*, not a malformed call, so a re-prompt cannot correct it. Diagnostic shape: the model recognises tool intent but **narrates the action in prose** instead of emitting the grammar (warrant-mood-1 *claimed* "logged it for you" with no call; the keeper `search_past_entries` scored **0/3**). **Per the gate's kill clause, Track D STOPS.** This re-confirms the 2026-03-12 design decision (client-side keyword extraction over model-side tool calling); if `search_past_entries` is ever pursued, the path is deterministic intent-detection + a user-confirmed UI card, **not** model-emitted grammar. Findings: [`eval-runs/2026-06-23/D1-tool-spike-findings.md`](eval-runs/2026-06-23/D1-tool-spike-findings.md).
- **D2. Framework build — NOT BUILT (D1 gate failed).** Spec preserved below for the record. *(2–3 days, only if D1 passes)* — `src/tools/` registry (name, schema, validate, execute-via-UI-card); parser + strip-from-display; one re-prompt retry on malformed calls (reuse `responseShaping` pattern); all executions user-confirmed, reversible, and suppressed entirely on crisis turns; Tier-3 exclusions from the design doc remain binding. No tool may bypass `crisisDetection`/`responseGuardrails`.
- **D3. Tool-call eval dimension — NOT BUILT (D1 gate failed).** Was: eval dimension + change doc + design-doc update.

**Honest assessment:** medium-high risk. 2B–E2B-class models are unreliable at structured output, and the journaling tone cost of a stray `<<tool:…>>` in a tender moment is high. That's why D1 is cheap, measurable, and allowed to kill the track. The genuinely valuable tool is `search_past_entries` ("when did I last write about my sister?") — if only one survives the spike, keep that one.

## Sequencing (what the planner should pick, in order)

1. **A1 → A2** (empty state, then visual calm) — the last fundamental problem, validated by data, and the user-visible debt.
2. **B1** input-robustness cases (can interleave with A-track on EVAL-flavored days).
3. **A3–A5** consolidation/polish items.
4. **C1 → C2 → C3** long-conversation harness, then data-driven context fixes.
5. **D1** tool-calling spike → **D2/D3** only if the gate passes.
6. **A6** + Track B leftovers as filler.

Standing rules unchanged: `PHASE.md` governs allowed verbs; revert to EVAL if north-star drops below 3.5; scorer freeze persists (lifts require a decisions-log entry); never weaken guardrails, crisis detection, or the AI disclaimer; no telemetry/feedback-collection features (local-only app — signal never reaches the developer; quality signal comes from this eval loop).
