# Live Test Log — 2026-03-27

## Pre-Change Testing

### Test 1: App State on Load
- Server reused from previous session
- PrivacyDashboard was stuck open from previous session (bug confirmed)
- PromptSelector was stuck open from previous session (bug confirmed)
- Both panels visible simultaneously, overlapping each other
- Error banner showing: "Something went wrong loading the model. Please try again."
- Textarea had preserved text from previous session

### Test 2: PrivacyDashboard Close (Pre-Fix)
- Clicked footer "Close" button on PrivacyDashboard
- **BUG CONFIRMED**: Dashboard remained in DOM, fully visible and interactive
- All stats displayed correctly (0 sessions, 0 moods, 889.37 MB)

### Test 3: PromptSelector Close (Pre-Fix)
- PromptSelector dropdown was open with "Self-Reflection" prompt
- **BUG CONFIRMED**: Dropdown remained in DOM from previous session
- Close button and X both failed to dismiss

### Test 4: Console Errors
- `[useMLCEngine] Model load failed: UnknownError: Failed to execute 'add' on 'Cache'` — expected in headless Chrome
- No unhandled promise rejections
- No React errors

## Post-Change Testing

### Test 5: Fresh Load After Restart
- Restarted dev server (fresh process)
- App loaded through loading screen → main UI
- Model load failed (expected) → error banner displayed
- **No stuck modals** — clean state, no PrivacyDashboard or PromptSelector visible

### Test 6: PromptSelector Open/Close
- Clicked Prompt button → dropdown appeared with "Challenges" prompt ("How can you be more compassionate with yourself right now?")
- Category selector, New Prompt, Use This, Close buttons all present
- Clicked "Close" button → **dropdown removed from DOM** (verified via JS: `hasDropdown: false, relChildren: 1`)
- Re-opened and verified it shows new prompt correctly

### Test 7: PrivacyDashboard Open/Close (X Button)
- Clicked Privacy button → dashboard opened with full content
- All 4 privacy features displayed, stats loaded (0 sessions, 0 moods, 1.12 GB)
- Clicked X button → **modal removed from DOM** (verified: `hasBackdrop: false, hasModal: false`)

### Test 8: PrivacyDashboard Open/Close (Footer Close Button)
- Re-opened Privacy Dashboard
- Clicked footer "Close" button → **modal removed from DOM** (verified: `!!document.querySelector('.fixed.z-50') === false`)

### Test 9: Input Preservation on Model Failure
- Filled textarea with: "I've been feeling really anxious about an upcoming job interview."
- Clicked Send button (model in error state)
- Waited 3 seconds for model load attempt to fail
- **Input preserved**: textarea value confirmed as original text (verified via `document.querySelector('textarea')?.value`)
- No brief flash of empty textarea observed

### Test 10: Vite HMR
- Initial HMR reload of PromptSelector.tsx and PrivacyDashboard.tsx failed during hot edit
- After full server restart, all changes loaded correctly
- TypeScript compilation clean throughout

## Summary
All 3 bugs confirmed pre-fix and verified fixed post-change:
1. PromptSelector Close: dropdown removed from DOM ✅
2. PrivacyDashboard Close (X and footer): modal removed from DOM ✅
3. Input preservation on model failure: text restored ✅

No regressions observed. Error banner, Mood tracker button, and Sessions panel all functioning normally.
