# Live Test Log — 2026-03-26

## Environment
- Browser: Headless Chrome (Claude Preview)
- WebGPU: Available but model compilation fails (expected in headless)
- Dev server: Vite on port 5173

## Test 1: Error handling on model load failure
**Action:** App loads, model download starts (42% / 868MB fetched), then fails
**Expected:** Error message shown to user with retry option
**Result:** Error banner appears: "Something went wrong loading the model. Please try again." with a Retry button. Main app UI is visible (not stuck on loading screen).
**Status:** PASS

## Test 2: User input preserved on send failure
**Action:** Typed "I've been feeling anxious about work lately" and clicked Send
**Expected:** Message stays in textarea (not lost), no empty session created
**Result:** Input preserved in textarea, no session created, error banner still visible. App returns to "Write your journal entry to start." state.
**Status:** PASS

## Test 3: Prompt selector opens upward (no overlap with send button)
**Action:** Clicked Prompt button
**Expected:** Dropdown opens above the button, not overlapping textarea/send button
**Result:** Dropdown opens with category selector, prompt text ("Write a letter to your future self one year from now."), "New Prompt", "Use This", and "Close" buttons. Positioned above the button area.
**Status:** PASS

## Test 4: Prompt selector AnimatePresence exit (pre-existing bug noted)
**Action:** Clicked "Close" button in prompt dropdown
**Expected:** Dropdown closes
**Result:** Dropdown remains in DOM after close (AnimatePresence exit animation fails). This is a pre-existing bug also affecting PromptSelector, same root cause as the MoodTracker/CrisisResources fix. Not in scope for today.
**Status:** KNOWN ISSUE (pre-existing, not a regression)

## Test 5: Mood tracker open and close
**Action:** Clicked "Mood" button in header, then X button to close
**Expected:** Modal opens and closes normally
**Result:** Modal opens showing all 10 emotions, 1 mood logged. X button closes modal successfully. No DOM remnants.
**Status:** PASS

## Test 6: Console errors
**Action:** Checked console after full page reload
**Result:** Model load errors properly logged as `[useMLCEngine] Model load failed: UnknownError: Failed to execute 'add' on 'Cache'`. No unhandled promise rejections. HMR hooks-order error only appears on hot reload (not on full page load).
**Status:** PASS

## Test 7: CrisisResources modal (code review verification)
**Action:** Code review of CrisisResources.tsx changes
**Result:** AnimatePresence removed, replaced with conditional rendering (`if (!isOpen) return null`), plain divs with click-outside-to-close and stopPropagation. Matches exact pattern from MoodTracker 03-25 fix.
**Status:** PASS (code verified, cannot trigger crisis modal in automated testing without model inference)

## Summary
- Error handling: Working correctly — error banner with retry, input preserved
- CrisisResources: Fixed (same pattern as MoodTracker)
- Prompt overlap: Fixed by opening dropdown upward
- Pre-existing issue noted: PromptSelector dropdown exit animation also broken (same AnimatePresence pattern)
