# Daily Plan — 2026-06-24 (Day 22)

## A. Today's objective

**Re-engage the daily critic→tune loop and ship ONE targeted prompt tune.** Run a fresh full 4-mode dimensional critic (the live loop has been dormant for ~10 days while Tracks C and D ran), **read every response body for qualitative trends** (not just pass/fail counts), then apply the single highest-leverage minimal edit to `src/prompts/systemPrompts.ts` and validate it moved the number without regressing safety. This is the standing eval→tune cadence and directly serves Problem #3 (model response quality, MANAGED by this loop) and the core directive: *make the journaling experience genuinely useful* = make the AI responses better.

## B. Why this matters

**The roadmap is drained — the loop is now the work.** Track A1–A6 DONE (Problem #4 resolved), Track B1–B2 DONE (B3 is filler), Track C COMPLETE (Problem #2 closed at population scale), and **Track D STOPPED** (D1 tool-spike came back NO-GO, 13% valid-call rate ≪ 80% gate). Per [ROADMAP.md](../ROADMAP.md) §Sequencing, what remains is "the daily eval→tune loop continues to own model-quality drift." That loop is the source of *real journaling usefulness* now: every other surface is built; the only lever left that makes someone want to come back tomorrow is whether the **response they get when they write something actually feels heard.**

**The loop has gone quiet.** The last *full 4-mode dimensional* critic run (empathy / specificity / format / boundary / persona / medical_refusal / jailbreak / input_robustness across all 4 prompts) was the **2026-06-13** input-robustness/collateral pass. Everything since — 06-19/06-20/06-21 (`eval:scripts` conversation-driver runs) and 06-23 (`eval:tools` spike) — measured *narrow* things (long-conversation retention, tool grammar), not the everyday single-turn response quality the user actually feels. **~10 days without a qualitative read of the response bodies** is exactly the "measurement without improvement" failure the eval-tune-combined directive was created to prevent. Today re-opens it.

## C. Current state assessment

**Phase:** `BUILD` ([PHASE.md](../PHASE.md)) — all verbs allowed, so `tune:` (prompt edit) and `eval:` (critic data) are both permitted. (Even under EVAL, the standing eval-tune-combined feedback authorizes a same-PR targeted tune.)

**Roadmap pick:** No confirmed critic-report BUILD-backlog item is outstanding, and every Track item is DONE/STOPPED. Falls through to the standing rule: **run the daily eval→tune loop on model-quality drift.** Hard-Rule-#2 "diagnose the critic loop" branch does NOT apply mechanically (the loop *can* run — `npm run eval` works, proven 06-19..06-23), but the *spirit* of #2 — a stale qualitative read — is the reason today is a critic day rather than more filler.

**The eval harness (reuse — do not rebuild):**
- `npm run eval` → `tsx scripts/run-eval.ts`, full per-mode suite on **Gemma 4 E2B** (`onnx-community/gemma-4-E2B-it-ONNX`, Node onnxruntime-node CPU q4f16) — the canonical eval model. Proven to run in-slot on CPU (no browser blocker) since the Day-19 C2 run.
- Flags available: `--dimensions=…`, `--mode=…`, `--per-dim=N`, `--limit=N`, plus `--scripts`/`--strategy`/`--tools` (not used today).
- **Dimensions present in `EVAL_CASES`** (`src/utils/evalRunner.ts`): `empathy` (11), `specificity` (15), `medical_refusal` (16), `format` (6), `jailbreak` (6), `boundary` (4), `persona` (5), `input_robustness` (12). The two **qualitative-richest, most-tunable** dimensions are **`empathy`** and **`specificity`** — they are where "feels heard" lives. `medical_refusal`/`jailbreak`/`boundary` are safety dimensions: read them to confirm no regression, but **do not tune toward them** (they pass and tuning there risks over-triggering — see standing rule).

**The system prompt being tuned** (`src/prompts/systemPrompts.ts`, the single source both App and eval import): 5 mode instructions sharing a common skeleton — MEDICAL RULE, FIRST LINE RULE, UNINTELLIGIBLE INPUT RULE, mode flow, Empathy/Continuity/Format blocks. Known accreted scar tissue worth watching in the qualitative read:
- **"Always end with a question" is hard-coded in every mode** ([systemPrompts.ts:27,40,92,109,119,123,146,180](../../src/prompts/systemPrompts.ts)). Suspected formulaic-ness driver: the model may be sacrificing a natural reflective *statement* to bolt on a question every single turn. Watch for question-fatigue in the bodies.
- **FIRST LINE RULE banned-opener list** ([systemPrompts.ts:18](../../src/prompts/systemPrompts.ts)) — does the model evade the ban by finding a *new* stock opener (e.g. "Reading about…", "Sitting with…")? Banned-opener whack-a-mole is a known shape.
- **"Echo a concrete word/detail"** — does specificity read as genuine grounding, or as a mechanical keyword-paste ("You mentioned Anna. Anna…")?

**Freeze boundary (critical — keep measurement honest):** the **scorer and eval cases are frozen** — `src/utils/evalScorer.ts` and the `EVAL_CASES`/dimensions in `src/utils/evalRunner.ts` must stay **byte-identical** so the before/after comparison is apples-to-apples. The **system prompt (`src/prompts/systemPrompts.ts`) is the ONE thing today is allowed to change**, minimally. Do not "fix" a failing case by editing the scorer or the case — that is moving the goalposts and is forbidden without a decisions-log freeze-lift entry.

## D. Top 3 priorities for the execution task

1. **Run the full 4-mode dimensional critic, NO source edits first.** `npm run eval` across all 4 modes / all dimensions (or `--per-dim` to sample empathy & specificity more deeply — see plan). Capture raw output to `docs/eval-runs/2026-06-24/`. This is the honest baseline.
2. **Read every response body and write the qualitative-trends critic report** to `docs/critic-reports/2026-06-24.md` — the prose trends, not just counts (per eval-tune-combined step 2–3): recurring openers/sentence shapes, companion-voice vs therapist-voice lapses, question-fatigue, where specificity is real vs keyword-paste, length distribution. End with a ranked "How to make this the best tool" list (2–5 moves by leverage).
3. **Apply the single highest-leverage minimal prompt edit** to `src/prompts/systemPrompts.ts`, then **re-run a validation slice** (the affected dimension(s)/mode(s) only) to confirm the number moved up with **zero safety regression**. Split commits: `tune:` for the prompt, `eval:`/`docs:` for the critic data + report.

## E. Concrete implementation plan

> **Not a Track-A UI day — no screenshots required.** This is a model-quality day; the deliverable is critic data + a report + one prompt edit, not a UI diff. (The PR-screenshot standing rule applies to UI PRs; note its non-applicability explicitly in the PR body.)

### Step 1 — Baseline critic run (no source edits)

```bash
cd "C:\Users\shara\OneDrive\Desktop\My Stuff\Work\Dev\QuietNote"
# Full empathy + specificity deep sample (the tunable, qualitative dimensions), all 4 modes:
npm run eval -- --dimensions=empathy,specificity --per-dim=3
# Plus a safety-confirm pass (read-only — must not regress):
npm run eval -- --dimensions=medical_refusal,jailbreak,boundary,format
```
- Capture each mode's output. Save raw transcripts under `docs/eval-runs/2026-06-24/` (mirror the existing per-mode file layout: `freewrite.md`, `gratitude.md`, `checkin.md`, `thoughtrecord.md`, plus `summary.json`).
- If a full per-mode `npm run eval` (no `--dimensions`) is cheap enough on CPU in-slot (the 06-21 run was ~14s/cell), prefer running the **whole suite** once for a complete picture, then the deep empathy/specificity sample for the tuning read. Use judgment on runtime; the empathy+specificity deep sample is the **non-negotiable** part (that is where the tune signal lives).

### Step 2 — Qualitative read + critic report (`docs/critic-reports/2026-06-24.md`)

Follow eval-tune-combined steps 2–3 exactly. The report MUST contain:
- **Scoreboard:** per-dimension passed/failed/total per mode, and a north-star row appended to the running ledger (this re-starts the qualitative cadence after the C/D detour — note that explicitly).
- **Qualitative trends** (the heart of it — read passes too, not just failures):
  - Opener inventory: tally the actual first 2–4 words of every response. Is the FIRST LINE RULE producing genuine concrete grounding, or a new stock opener it rotated to?
  - Companion-voice vs therapist-voice: where does it lapse into clinical/coachy register?
  - Question quality + **question-fatigue**: is every single reply ending in a question making the model feel like an interrogator? Are the questions grounded or generic?
  - Specificity texture: real echo vs mechanical keyword-paste.
  - Length distribution vs the "3–4 sentences" / "max 3" caps.
- **"How to make this the best tool"**: 2–5 concrete next moves ranked by leverage (prompt edits, future scorer additions, UX). The #1 item becomes Step 3.

### Step 3 — ONE targeted, minimal prompt tune (`src/prompts/systemPrompts.ts`)

Pick the single highest-leverage edit the bodies actually justify. **Do not rewrite a prompt.** Likely candidates (the *data decides* — do not pre-commit; this is a menu, choose ≤1–2):
- **If question-fatigue is the dominant trend:** soften the absolute "Always end with a question" to permit an occasional grounded reflective *statement* close when a question would feel forced — **without** weakening the modes where ending-on-a-question is a deliberate safety/flow property. ⚠️ **Guardrail:** the check-in and thought-record modes' `END-OF-RESPONSE RULE` and the **SAFETY CARVEOUT "must end with a question"** requirements are load-bearing (a crisis turn must invite one more reflection) — **do NOT touch the crisis-path question requirement.** Any softening applies to ordinary free-write/gratitude flow turns only.
- **If a new stock opener emerged:** extend the FIRST LINE RULE banned list with the specific new phrase(s) observed (one-line, surgical — same shape as the existing list).
- **If specificity reads as keyword-paste:** sharpen the "Echo a concrete word/detail" guidance with one before/after micro-example of *weaving* the detail vs *naming* it.

**Constraints on the edit:**
- Minimal diff — a sentence or a list item, not a section rewrite.
- ASCII / proper Unicode only (the repo has had mojibake regressions — use `…`, no smart quotes).
- **NEVER** weaken: `crisisDetection`, `responseGuardrails`, the AI-limitations disclaimer, the MEDICAL/HEALTH RULE, the SAFETY CARVEOUT, or any jailbreak/boundary defense. If the chosen tune touches a mode that has a SAFETY CARVEOUT, re-read that carveout and confirm the edit is orthogonal to it.

### Step 4 — Validation slice (prove the move)

```bash
# Re-run ONLY the dimension(s)/mode(s) the tune targeted:
npm run eval -- --dimensions=<the-tuned-dimension> --per-dim=3
# Re-confirm safety did not regress:
npm run eval -- --dimensions=medical_refusal,jailbreak,boundary
```
- Quote 2–4 before/after response bodies on the targeted case(s) in the change doc — show the qualitative move, not just the count.
- **Gate:** the tune ships only if the targeted dimension's pass-rate is **≥ baseline** AND every safety dimension is **unchanged** (no medical/jailbreak/boundary regression, no new banned opener, no lost question on a crisis turn). If the edit regresses anything, **revert it**, record the negative result in the change doc + decisions log, and ship the critic report alone (a measured null result is a valid day's output).

## F. Testing plan

- `npm run test` — full Vitest suite green (current baseline 1213/1213). The prompt edit must not break `src/prompts/__tests__/systemPrompts.test.ts` — **read that test first**; if it asserts the exact wording you're changing, update the assertion to match the *new intended* wording (that is a legitimate prompt-test update, not a scorer-freeze violation — but call it out explicitly in the change doc).
- `npm run build` — `tsc -b && vite build` green (TS strict).
- **Freeze gate (measurement integrity):** `git diff origin/main -- src/utils/evalScorer.ts src/utils/evalRunner.ts` → **MUST be empty** (scorer + eval cases frozen). The **only** allowed source change is `src/prompts/systemPrompts.ts` (+ its test if wording-asserted).
- **Safety diff check:** `git diff` shows **no** change to `crisisDetection.ts`, `responseGuardrails.ts`, the disclaimer, or `sessionContext.ts`.

## G. Deliverables

- [ ] `docs/eval-runs/2026-06-24/` — raw critic transcripts (per-mode) + `summary.json` (baseline, pre-tune).
- [ ] `docs/critic-reports/2026-06-24.md` — scoreboard + **Qualitative trends** + ranked **"How to make this the best tool"** + north-star ledger row (cadence re-started after C/D detour).
- [ ] **ONE** minimal `tune:` edit to `src/prompts/systemPrompts.ts` (or a recorded null result if the edit regressed — either is a valid outcome).
- [ ] Validation-slice re-run + before/after response bodies quoted.
- [ ] `docs/changes/2026-06-24-day22-critic-tune.md` change doc (what the bodies showed, what was tuned, the measured move, safety-confirm).
- [ ] `docs/decisions.md` one-line entry (what was tried + expected, to be back-filled with actual). **Freeze note:** scorer/eval-cases byte-identical; the prompt is the intentional single change — state this so it is not mistaken for a freeze violation.
- [ ] `npm run build` + `npm run test` green; freeze gate (scorer/evalRunner) confirmed empty; safety surfaces unchanged.
- [ ] PR to `main`; commits split `eval:`/`docs:` (critic data, report) vs `tune:` (prompt). PR body notes the screenshot rule is N/A (model-quality day, no UI diff).

## H. Roadmap tracker

| # | Fundamental problem | Status after today |
|---|---|---|
| 1 | Guided modes vanish | FIXED (PR #40) |
| 2 | Tiny context window | CLOSED at population scale (Track C complete) |
| 3 | Model response quality | **MANAGED — today re-engages the daily critic→tune loop after a ~10-day qualitative gap; one targeted tune applied** |
| 4 | Feels like a tech demo | RESOLVED via Track A (A1–A6 DONE) |

**Track progress:** A1–A6 DONE (Track A COMPLETE). B1–B2 DONE (B3 filler). C1–C3 DONE (Track C COMPLETE). **Track D STOPPED (D1 NO-GO).** Backlog drained to filler → the **daily eval→tune loop is now the primary work**; today restarts its qualitative cadence.

### Hard-rule check
- **Phase:** BUILD — `tune:`/`eval:` allowed. ✅
- **Hard Rule #1 (no regression vs 7 days ago):** the tune ships only if the targeted dimension is ≥ baseline and safety is unchanged — gate enforces non-regression. ✅
- **Hard Rule #2 (read latest critic + PHASE):** done; today *re-opens* the dormant qualitative critic. ✅
- **Hard Rule #3 (decisions entry):** required; scorer/eval-cases frozen, prompt is the intentional single change — stated explicitly. ✅
- **Standing rules:** scorer freeze held; never weaken crisisDetection/responseGuardrails/disclaimer/SAFETY CARVEOUT/medical rule; do not tune toward safety dimensions (over-trigger risk); no telemetry; privacy not re-sold (focus is response quality = core usefulness). ✅
