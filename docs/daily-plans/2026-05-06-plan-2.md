# Daily Plan — 2026-05-06 (Evening)

## A. Today's Objective

Rebase and merge PR #35 (mood chart), then implement mood entry editing so users can correct mistakes in their mood log (Objective 2).

## B. Why This Matters

The mood chart (PR #35) is fully built and tested but stuck because the branch diverged from main before PR #34 merged. Getting it merged delivers the single most impactful mood-tracking improvement: visual trend lines that make patterns like "my anxiety spikes on Mondays" immediately visible. Mood entry editing completes the mood data integrity story — if a user accidentally logs the wrong emotion or intensity, they can fix it without deleting and re-entering.

## C. Journaling Experience Assessment (Objective 1)

### Structural Verification (Preview — no WebGPU)
1. Welcome screen renders "Good evening" greeting correctly with mood tracking encouragement
2. All 4 journaling modes visible and selectable (Free Write, Gratitude, Check-in, Thought Record)
3. Prompt button functional
4. Model load fails gracefully with error banner + Retry (expected, no WebGPU)
5. MoodTracker modal opens with Log Mood / History tabs
6. WellnessSummary shows in History tab with "Log 5 more moods" progress bar (0/5)
7. Session panel with search functional
8. Footer renders correctly
9. 832 tests all passing on main

### System Prompt Assessment
All 5 system prompts (Free Write, Gratitude, Morning Check-in, Evening Check-in, Thought Record) are well-structured:
- Clear role definition as journaling companion
- Explicit medical/treatment refusal guardrails
- Appropriate brevity constraints (2-4 sentences)
- Good example responses in Free Write mode
- No changes needed

### Previous Model Quality Assessment
- Gemma 2 2B (WebLLM) response quality: **4/5**
- Multi-turn quality untested on real device
- No changes from previous assessment
- Gap remains: **no real-device WebGPU model comparison** across 3 backends

### Journal Prompts Assessment
64 prompts across 7 categories. Previously identified gaps remain (generic gratitude/creativity prompts, no time/mood-aware selection). Not actionable today.

## D. Mood Tracking and Insights Assessment (Objective 2)

### Current State (main branch after PR #34 merge)
- **MoodTracker modal**: 10 emotions, intensity slider, context tags, notes. Log Mood / History tabs
- **MoodHistoryPanel**: Chronological list grouped by date with session links
- **WellnessSummary**: Wellness report in History tab — trend, top emotions, patterns (needs 5+ entries)
- **MoodInsightsCard**: Trends, correlations, day-of-week patterns (needs 5+ entries)
- **MoodSuggestionCard**: Inline mood logging from emotion detection with cooldowns
- **Personalized welcome**: Time-of-day greeting with mood trend indicator

### Pending (PR #35 — needs rebase + merge)
- **MoodChart**: SVG-based intensity chart with emotion-colored dots, connecting trend line, hover tooltips, time range filter (7D/30D/All), color legend. 10 tests.

### Critical Gaps
1. **No visual mood chart on main** — PR #35 built but not merged due to rebase issue
2. **No mood entry editing** — users can't fix mistakes in logged moods
3. **No mood-journal correlations** — can't see "when I journal about work, I feel anxious"

### What Would Make Mood Tracking More Valuable Next
1. **Merge mood chart** (Priority 1) — visual trends at a glance
2. **Mood entry editing** (Priority 2) — data accuracy and user trust
3. **Mood-journal correlation view** (future) — deeper insight

## E. Model & Runtime Status (Objective 3)

No changes since last assessment. All 3 backends configured. Not actionable today.

## F. Top 3 Priorities for the Execution Task

### 1. Rebase and Merge PR #35 — Mood Chart (Objective 2)
PR #35 was branched before PR #34 merged, causing the diff to show spurious deletions of WellnessSummary, PersonalizedWelcome, and DismissibleTrimNotice files. Rebase on main to incorporate PR #34, resolve any conflicts, force-push, then merge.

### 2. Mood Entry Editing (Objective 2)
Allow users to tap a mood entry in MoodHistoryPanel to edit it — change emotion, intensity, context, or note. The storage layer already supports `putMood()` for updates. This makes mood data trustworthy.

### 3. Mood-Aware Prompt Suggestions (Objective 1 + 2)
After logging a mood, suggest journal prompts that match the emotional state. If a user just logged "anxious", suggest prompts about processing worry or grounding exercises. Connects mood tracking to the journaling experience.

## G. Concrete Implementation Plan

### Priority 1: Rebase and Merge PR #35

**Steps:**
```bash
git checkout feat/2026-05-06-mood-chart
git rebase main
# Resolve conflicts if any — keep both PR #34's files AND MoodChart additions
git push --force-with-lease origin feat/2026-05-06-mood-chart
npm run test   # Verify all tests pass
npm run build  # Verify build succeeds
gh pr merge 35 --squash
git checkout main
git pull origin main
```

**Likely conflicts:**
- `src/components/MoodHistoryPanel.tsx` — the branch adds MoodChart import/render; PR #34 may have modified this file too. Keep both changes.
- `src/components/MoodTracker.tsx` — both branches may modify the History tab layout. Keep WellnessSummary + MoodChart.

**How to verify:**
- All tests pass (832+ with MoodChart's 10 new tests)
- `npm run build` succeeds
- Dev server: MoodTracker History tab shows WellnessSummary → MoodChart → MoodHistoryPanel in that order
- No console errors

### Priority 2: Mood Entry Editing

**Modify `src/components/MoodHistoryPanel.tsx`:**
- Add `onEditMood?: (mood: MoodEntry) => void` prop
- Each mood entry gets a pencil icon button (use `Pencil` from lucide-react)
- Desktop: show on hover. Mobile: always visible (small, subtle)
- Clicking calls `onEditMood(mood)`

**Modify `src/components/MoodTracker.tsx`:**
- Add `editingMood: MoodEntry | null` state
- When `onEditMood` fires:
  - Set `editingMood` to the mood entry
  - Switch to Log tab (`setActiveTab("log")`)
  - Pre-fill all form fields: `setSelectedEmotion(mood.emotion)`, `setIntensity(mood.intensity)`, `setSelectedContexts(mood.contexts)`, `setNote(mood.note || "")`
- When saving while `editingMood !== null`:
  - Use the original `id` and `ts` from `editingMood`
  - Call `onSaveMood` with the updated entry (storage `putMood` handles upserts)
  - Clear `editingMood` and switch back to History tab
- Button text: "Update Mood" when editing, "Save Mood" when creating
- Add "Cancel" link/button when editing — clears `editingMood`, returns to History tab

**Files to modify:**
- `src/components/MoodHistoryPanel.tsx` — add edit button + `onEditMood` prop
- `src/components/MoodTracker.tsx` — handle edit flow with pre-fill + update logic

**How to verify:**
- Click edit on a mood entry → switches to Log tab with values pre-filled
- Change emotion/intensity → Save → entry updated in History (not duplicated)
- Cancel → returns to History tab, no changes
- Button says "Update Mood" when editing, "Save Mood" when creating new
- All existing tests pass + new tests for edit flow

### Priority 3: Mood-Aware Prompt Suggestions

**Modify `src/components/MoodTracker.tsx` or `src/App.tsx`:**
- After saving a mood, if the user hasn't started a journal session yet, show a brief suggestion card:
  - "You're feeling [emotion]. Would you like to journal about it?"
  - Include 2-3 prompts from `journalPrompts.ts` filtered by relevance to the emotion
  - Map emotions to prompt categories:
    - anxious/frustrated → challenges, self-reflection
    - sad/lonely → relationships, self-reflection
    - happy/excited/grateful → gratitude, growth
    - calm/content → creativity, goals
- Clicking a prompt closes MoodTracker and pre-fills the chat input with the prompt

**Files to modify:**
- `src/components/MoodTracker.tsx` — add post-save prompt suggestion UI
- `src/data/journalPrompts.ts` — ensure prompts have good coverage for emotion-based filtering (may need a few additions)
- `src/App.tsx` — wire up the "use this prompt" callback to close modal and pre-fill input

**How to verify:**
- Log a mood with emotion "anxious" → see relevant prompts (challenges/self-reflection category)
- Click a suggested prompt → MoodTracker closes, prompt appears in chat input
- Dismiss suggestion → returns to normal state
- No prompts shown if user already has an active conversation

## H. Testing Plan

### Automated Tests
- Write tests for mood editing flow:
  - Edit button visible on mood entries in MoodHistoryPanel
  - `onEditMood` called with correct mood data when clicked
  - MoodTracker pre-fills form when editing
  - Save uses same id (update, not create)
  - Cancel clears editing state
  - Button text changes between "Save Mood" and "Update Mood"
- Write tests for mood-aware prompt suggestions:
  - Correct prompt categories shown for each emotion
  - Prompt suggestion appears after saving mood
  - Clicking prompt calls the callback
  - No suggestion when user has active conversation

### Build Verification
- All tests pass (0 failures)
- `npm run build` succeeds
- `npm run lint` passes

### Preview Tests
- MoodTracker History tab: MoodChart visible (after PR #35 merge)
- Edit button on mood entries
- Edit flow pre-fills correctly
- Post-save prompt suggestions appear
- Mobile (375px): all new UI renders without overflow
- No console errors

## I. Deliverables

- [ ] PR #35 rebased on main and merged
- [ ] Mood entry editing (edit button on entries, pre-fill form, update-not-create)
- [ ] Mood-aware prompt suggestions after mood logging
- [ ] Tests for editing and prompt suggestions
- [ ] All tests pass, build succeeds
- [ ] PR opened with change documentation and screenshots

## J. Roadmap Tracker

### Objective 1: Make AI Journaling Conversation Genuinely Helpful
| Item | Status |
|------|--------|
| Model output quality (Gemma 2 2B) | Rated 4/5 — adequate |
| System prompt tuning | Stable — no changes needed |
| Crisis detection & guardrails | Working |
| 4 journaling modes | **Done** (PRs #25-#27) |
| Mobile mode selector fix | **Done** (PR #28) |
| Keyboard shortcut hints | **Done** (PR #31) |
| PromptSelector mobile overflow | **Done** (PR #32) |
| Session search/filter | **Done** (PR #33) |
| Smart session titles | **Done** (PR #33) |
| Personalized welcome screen | **Done** (PR #34) |
| Context trimming notice dismissible | **Done** (PR #34) |
| Mood-aware prompt suggestions | **Planned today** |
| Time-of-day aware prompts | Backlog |
| Message editing/deletion | Backlog |
| Real-device model quality comparison | Blocked on WebGPU testing |

### Objective 2: Improve Mood Tracking and Personal Insights
| Item | Status |
|------|--------|
| MoodTracker modal (10 emotions, intensity, context) | **Done** |
| MoodSuggestionCard (inline mood logging) | **Done** |
| MoodInsightsCard (trends, correlations, day-of-week) | **Done** |
| Mood history timeline | **Done** (PR #33) |
| Weekly Wellness Summary UI | **Done** (PR #34) |
| Personalized welcome with mood trends | **Done** (PR #34) |
| Visual mood chart | **PR #35 open** — rebase + merge today |
| Mood entry editing | **Planned today** |
| Mood-aware prompt suggestions | **Planned today** |
| Mood-journal correlation view | Backlog |
| Streak tracking | Backlog |

### Objective 3: Model Quality and Runtime Improvements
| Item | Status |
|------|--------|
| Runtime adapter pattern | **Done** (PR #25) |
| 3 backends (WebLLM, Transformers.js, MediaPipe) | **Done** |
| Phase 4 (Verification & Audit) | **Done** (PRs #31, #32) |
| Gemma 4 E2B (Transformers.js + MediaPipe) | Configured, untested |
| Gemma 4 E2B (WebLLM) | Blocked — mlc-llm#3477 |
| Real-device backend comparison | Blocked on WebGPU testing |

### Completed Features
| Date | Feature | PR |
|------|---------|-----|
| 2026-04-15 | Mood history timeline + session search + smart titles | #33 |
| 2026-04-16 | Wellness summary + personalized welcome + trim notice | #34 |
| 2026-05-06 | Visual mood chart (pending merge) | #35 |

### Low-Hanging Fruit (defined types with no UI)
| Type | What Exists | What's Needed | Status |
|------|-------------|---------------|--------|
| WellnessReport | `moodPatterns.ts` + WellnessSummary UI | Done | **Done** (PR #34) |
| UserSettings | Full type with AI personality fields | Settings panel UI | Backlog |
| ThoughtRecord | Type + guided flow | Structured data saving + history | Backlog |
| CognitiveDistortion | 10 types defined | Integration into ThoughtRecord | Backlog |
| DBTSkillPractice | Full type defined | Not a priority | Backlog |

---

*Generated by automated scheduled task on 2026-05-06 (evening run).*
