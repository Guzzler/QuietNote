# Daily Plan — 2026-05-06

## A. Today's Objective

Merge the stalled PR #34, fix the pre-existing MoodHistoryPanel test failure, and build a visual mood chart so users can see their emotional intensity over time at a glance (Objective 2).

## B. Why This Matters

PR #34 has been open since 2026-04-17 with Wellness Summary, Personalized Welcome, and Dismissible Trim Notice — all tested and mergeable. Merging it unblocks progress. The mood chart addresses the next critical gap: users can see a list of moods (MoodHistoryPanel) and aggregate insights (WellnessSummary), but there's no visual timeline showing how their emotional intensity changes over days and weeks. A simple sparkline or bar chart makes patterns instantly visible — "I can see my anxiety spiking on Mondays" — without requiring the user to mentally assemble a list of entries.

## C. Journaling Experience Assessment (Objective 1)

### Structural Verification (Preview — no WebGPU)
1. Welcome screen renders correctly — greeting, tips, prompt link, mood tracking mention
2. All 4 journaling mode pills visible and functional (Free Write selected by default)
3. Prompt button opens PromptSelector with category filtering
4. Model load fails gracefully — error banner + Retry button (expected, no WebGPU)
5. MoodTracker modal opens with Log Mood / History tabs — both functional
6. Session search present in SessionsPanel
7. Footer renders correctly
8. 798 tests passing on main (2 pre-existing failures in MoodHistoryPanel date grouping)

### Previous Model Quality Assessments
- Gemma 2 2B (WebLLM) response quality: **4/5** — warm, reflective, appropriately brief
- System prompts stable across all 4 modes — no changes needed
- Crisis detection and guardrails verified working
- Key gap remains: **no real-device WebGPU model comparison** across the 3 backends

### System Prompt Assessment
No changes needed. All 4 mode prompts are well-structured and stable.

### Journal Prompts Assessment
64 prompts across 7 categories. Previously identified gaps:
- Some gratitude/creativity prompts are generic
- No time-of-day awareness, mood-aware selection, or seasonal prompts
- Not actionable today — lower priority than mood visualization

## D. Mood Tracking and Insights Assessment (Objective 2)

### Current State (main branch)
- **MoodTracker modal**: 10 emotions, intensity slider, context tags, notes. Log Mood / History tabs.
- **MoodHistoryPanel**: Chronological list grouped by date. Session links work.
- **MoodInsightsCard**: Trends, top emotions, correlations, day-of-week. Requires 5+ entries.
- **MoodSuggestionCard**: Inline mood logging from emotion detection. Smart cooldowns.
- **moodPatterns.ts**: Full analysis engine — `analyzeMoodTrend()`, `findTopEmotions()`, `detectCorrelations()`, `detectDayOfWeekPatterns()`, `generateWeeklyReport()`.

### Pending (PR #34 — not yet merged)
- **WellnessSummary**: Surfaces `generateWeeklyReport()` data in History tab — trend, top emotions, patterns, insights
- **Personalized Welcome**: Time-of-day greeting, mood trend indicator, contextual mode suggestions
- **Dismissible Trim Notice**: X button to dismiss context trimming notice

### Critical Gap: No Visual Mood Chart
Users can see a list of individual entries and aggregate text insights, but there's no visual graph showing mood intensity over time. A chart would:
- Make trends instantly visible (improving/declining/volatile)
- Help users correlate mood with life events by seeing spikes and dips
- Feel rewarding — seeing data accumulate motivates continued tracking

### What Would Make Mood Tracking More Valuable Next
1. **Visual mood chart** (TODAY's Priority 2) — intensity over time, color-coded by emotion
2. **Mood-journal correlation view** (future) — "When you journaled about work, you felt anxious 60% of the time"
3. **Streak tracking** (future) — "You've journaled 5 days in a row"

## E. Model & Runtime Status (Objective 3)

No changes since 2026-04-14. All 3 backends configured. Real-device WebGPU testing remains blocked. Not actionable today.

## F. Top 3 Priorities for the Execution Task

### 1. Merge PR #34 and Fix Pre-existing Test Failure (Housekeeping — unblocks everything)
PR #34 has been open for ~3 weeks with 658 additions, 8 changed files, all tests passing on the branch, and marked MERGEABLE. Merge it to main. Then fix the pre-existing `MoodHistoryPanel` test failure (date grouping test: "groups multiple moods into correct buckets in order").

### 2. Visual Mood Chart Component (Objective 2 — Mood Insights)
Create a `MoodChart` component that renders a simple visual chart showing mood intensity over time. Use pure CSS/SVG — no chart library needed for a sparkline. Show the last 14 days of mood entries as colored dots or bars, with intensity on the Y-axis and date on the X-axis. Color-code by emotion. This goes in the MoodTracker History tab, between the WellnessSummary and the MoodHistoryPanel list.

### 3. Mood Entry Editing (Objective 2 — Data Accuracy)
Users should be able to tap a mood entry in MoodHistoryPanel to edit it (change emotion, intensity, context, or note). This makes mood data more trustworthy — if you accidentally log the wrong emotion, you can fix it. The storage layer already supports `putMood()` for updates.

## G. Concrete Implementation Plan

### Priority 1: Merge PR #34 + Fix MoodHistoryPanel Test

**Step 1: Merge PR #34**
```bash
gh pr merge 34 --squash
git pull origin main
```

**Step 2: Fix MoodHistoryPanel test failure**
- Read `src/components/__tests__/MoodHistoryPanel.test.ts` — find the "groups multiple moods into correct buckets in order" test
- The test likely fails due to date boundary issues (timestamps crossing midnight boundaries depending on timezone/time of day)
- Fix the test to use stable date references or mock `Date.now()`
- Verify all tests pass after fix

**Files to modify:**
- `src/components/__tests__/MoodHistoryPanel.test.ts` — fix date grouping test

**How to verify:**
- `npm run test` — all tests pass (0 failures)
- `npm run build` — succeeds
- `npm run lint` — passes

### Priority 2: Visual Mood Chart

**New file: `src/components/MoodChart.tsx`**

Create a component that:
- Takes `moods: MoodEntry[]` as props
- Shows the last 14 days of mood entries as a simple chart
- Uses pure SVG (no external chart library) for a clean, lightweight visualization
- X-axis: dates (last 14 days, most recent on right)
- Y-axis: intensity (1-10)
- Each mood entry is a colored dot positioned by date and intensity
- Dot color comes from the emotion (reuse `EMOTION_COLORS` from MoodHistoryPanel or define consistent colors)
- Connect dots with a faint line for visual flow
- If multiple entries on the same day, show all dots stacked vertically at that date
- Empty days show no dot (gaps in the line are fine)
- If fewer than 2 entries, show an encouraging message instead: "Log a few more moods to see your chart"
- Responsive: chart fills container width, has fixed height (~120px)
- Hover/tap on dots could show a tooltip with emotion + intensity (stretch goal)

**UI layout within History tab (after PR #34 merge):**
```
[MoodTracker Modal — History Tab]
  ┌─ Wellness Summary Card ──────────────┐
  │ (from PR #34)                         │
  └───────────────────────────────────────┘
  
  ┌─ Mood Chart (last 14 days) ──────────┐
  │ 10│         ●                         │
  │  8│    ●         ●                    │
  │  6│ ●     ●         ●    ●           │
  │  4│                       ●          │
  │  2│                                  │
  │   └──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬── │
  │    Apr 23  25  27  29  May 1   3   5 │
  └───────────────────────────────────────┘
  
  [Mood History Timeline list below...]
```

**Modify `src/components/MoodTracker.tsx`:**
- Import `MoodChart`
- In the History tab content, render `<MoodChart moods={allMoods} />` between `<WellnessSummary>` and `<MoodHistoryPanel>`

**Color mapping for emotions:**
```ts
const EMOTION_CHART_COLORS: Record<MoodEmotion, string> = {
  happy: "#facc15",      // yellow-400
  sad: "#60a5fa",        // blue-400
  anxious: "#c084fc",    // purple-400
  angry: "#f87171",      // red-400
  calm: "#2dd4bf",       // teal-400
  excited: "#fb923c",    // orange-400
  frustrated: "#fbbf24", // amber-400
  content: "#f472b6",    // pink-400
  lonely: "#94a3b8",     // slate-400
  grateful: "#4ade80",   // green-400
};
```

**Files to create:**
- `src/components/MoodChart.tsx`

**Files to modify:**
- `src/components/MoodTracker.tsx` — add MoodChart between WellnessSummary and MoodHistoryPanel in History tab

**How to verify:**
- Open MoodTracker → History tab → see chart between Wellness Summary and mood list
- With 0-1 moods: see "Log a few more moods to see your chart"
- With 2+ moods: see SVG chart with colored dots and connecting line
- Chart is responsive at desktop and mobile (375px)
- Dots are correctly positioned by date (X) and intensity (Y)
- Dot colors match emotion
- All existing tests pass
- No console errors

### Priority 3: Mood Entry Editing

**Modify `src/components/MoodHistoryPanel.tsx`:**
- Add an `onEditMood` callback prop: `onEditMood?: (mood: MoodEntry) => void`
- Each mood entry gets a subtle edit button (pencil icon) that appears on hover (desktop) or is always visible (mobile)
- Clicking edit calls `onEditMood(mood)` with the full mood entry

**Modify `src/components/MoodTracker.tsx`:**
- Add `editingMood: MoodEntry | null` state
- When `onEditMood` is called from MoodHistoryPanel, switch to the "log" tab with the mood's values pre-filled:
  - `setSelectedEmotion(mood.emotion)`
  - `setIntensity(mood.intensity)`
  - `setSelectedContexts(mood.contexts)`
  - `setNote(mood.note || "")`
  - `setActiveTab("log")`
  - `setEditingMood(mood)`
- When saving while `editingMood` is set, update instead of create:
  - Use the same `id` and `ts` from the original entry
  - Call `onSaveMood` with the updated entry (storage layer's `putMood` handles upserts)
- Show "Update Mood" instead of "Save Mood" button text when editing
- Add a "Cancel Edit" option to return to the history tab

**Files to modify:**
- `src/components/MoodHistoryPanel.tsx` — add edit button + `onEditMood` prop
- `src/components/MoodTracker.tsx` — handle edit flow with pre-fill + update logic

**How to verify:**
- Click edit on a mood entry → switches to Log tab with values pre-filled
- Modify and save → entry updated (not duplicated)
- Cancel → returns to History tab without changes
- All existing tests pass

## H. Testing Plan

### Automated Tests
- Fix the pre-existing `MoodHistoryPanel` date grouping test
- Write tests for `MoodChart`:
  - Renders encouragement message with < 2 entries
  - Renders SVG chart with 2+ entries
  - Correct number of dots for mood entries
  - Dots use correct emotion colors
  - Chart handles entries spanning 14+ days (only shows last 14)
  - Chart handles multiple entries on same day
- Write tests for mood editing flow:
  - Edit button visible on mood entries
  - Clicking edit switches to log tab with pre-filled values
  - Saving edited mood uses same id
  - Cancel returns to history tab

### Build Verification
- All tests pass (0 failures — fix pre-existing failure)
- `npm run build` succeeds with zero TypeScript errors
- `npm run lint` passes

### Preview Tests
- MoodTracker History tab: chart renders between WellnessSummary and mood list
- Chart dots appear with correct colors and positions
- Edit button visible on mood entries
- Edit flow pre-fills values correctly
- Mobile (375px): chart and edit UI render without overflow
- No console errors

## I. Deliverables

- [ ] PR #34 merged to main
- [ ] Pre-existing MoodHistoryPanel test failure fixed
- [ ] `src/components/MoodChart.tsx` — New visual mood chart component (SVG)
- [ ] `src/components/MoodTracker.tsx` — Updated to render MoodChart + handle edit flow
- [ ] `src/components/MoodHistoryPanel.tsx` — Updated with edit button
- [ ] Tests for MoodChart and mood editing
- [ ] All tests pass (0 failures), build succeeds
- [ ] PR opened with change documentation and screenshots

## J. Roadmap Tracker

### Objective 1: Make AI Journaling Conversation Genuinely Helpful
| Item | Status |
|------|--------|
| Model output quality (Gemma 2 2B) | Rated 4/5 — adequate |
| System prompt tuning | Stable — no changes needed |
| Crisis detection & guardrails | Working |
| 4 journaling modes | **Done** (PRs #25-#27) |
| Mobile mode selector fix | **Done** (PR #28) |
| Keyboard shortcut hints | **Done** (PR #31) |
| PromptSelector mobile overflow (#20) | **Done** (PR #32) |
| Session search/filter | **Done** (PR #33) |
| Smart session titles (#27) | **Done** (PR #33) |
| Personalized welcome screen | **PR #34 open** — merge today |
| Context trimming notice dismissible (#28) | **PR #34 open** — merge today |
| Time/mood-aware prompt suggestions | Backlog |
| Message editing/deletion | Backlog |
| Real-device model quality comparison | Blocked on WebGPU testing |

### Objective 2: Improve Mood Tracking and Personal Insights
| Item | Status |
|------|--------|
| MoodTracker modal (10 emotions, intensity, context) | **Done** |
| MoodSuggestionCard (inline mood logging) | **Done** |
| MoodInsightsCard (trends, correlations, day-of-week) | **Done** |
| Mood history timeline | **Done** (PR #33) |
| Weekly Wellness Summary UI | **PR #34 open** — merge today |
| Visual mood chart (intensity over time) | **Planned today** |
| Mood entry editing | **Planned today** |
| Mood-journal correlation view | Backlog |
| Streak tracking | Backlog |
| UserSettings / AI personality UI | Backlog |

### Objective 3: Model Quality and Runtime Improvements
| Item | Status |
|------|--------|
| Runtime adapter pattern | **Done** (PR #25) |
| 3 backends (WebLLM, Transformers.js, MediaPipe) | **Done** |
| Phase 4 (Verification & Audit) | **Done** (PRs #31, #32) |
| Gemma 4 E2B (Transformers.js + MediaPipe) | Configured, untested |
| Gemma 4 E2B (WebLLM) | Blocked — mlc-llm#3477 |
| Real-device backend comparison | Blocked on WebGPU testing |
| LiteRT-LM migration | Tracking, not urgent |

### Completed Features
| Date | Feature | PR |
|------|---------|-----|
| 2026-03-28 | Humanize loading screen + welcome animations | #17 |
| 2026-03-29 | Header subtitle + mobile sessions + prompt link | — |
| 2026-03-30 | Humanize footer and placeholder text | #18 |
| 2026-03-31 | Phase 1 safety & correctness fixes | #19 |
| 2026-04-01 | Phase 2 accessibility improvements | #20 |
| 2026-04-02 | Aria-labels, storage display, new session button | #21 |
| 2026-04-03 | Mobile sessions, export feedback, mood grid, animation fix | #22 |
| 2026-04-04 | Auto-scroll, auto-grow textarea, layout overflow fix | #23 |
| 2026-04-04 | Session deletion with confirmation | #24 |
| 2026-04-05 | Runtime adapter + Guided Gratitude mode + TS fixes | #25 |
| 2026-04-06 | Transformers.js backend + Check-in mode + Runtime selector | #26 |
| 2026-04-07 | Unit tests + Gemma 4 E2B upgrade + CBT Thought Record | #27 |
| 2026-04-08 | Mobile fix + dead code removal + types + code splitting | #28 |
| 2026-04-09 | MediaPipe backend + privacy truthfulness (partial) | #29 |
| 2026-04-12 | MediaPipe Gemma 4 E2B + privacy copy fix + WebLLM tests | #30 |
| 2026-04-13 | Network audit utility + keyboard hints + privacy tests | #31 |
| 2026-04-14 | Privacy audit checklist + PromptSelector mobile fix | #32 |
| 2026-04-15 | Mood history timeline + session search + smart titles | #33 |

### Remaining UX Issues
| # | Issue | Priority | Status |
|---|-------|----------|--------|
| 8 | Footer overlap on short viewports | Low | Open |
| 28 | Context trimming notice not dismissible | Low | **PR #34** |

### Low-Hanging Fruit (defined types with no UI)
| Type | What Exists | What's Needed | Status |
|------|-------------|---------------|--------|
| WellnessReport | `moodPatterns.ts` generates them | UI to surface weekly insights | **PR #34** |
| UserSettings | Full type with AI personality fields | Settings panel UI | Backlog |
| ThoughtRecord | Type + guided flow | Structured data saving + history view | Backlog |
| CognitiveDistortion | 10 types defined | Integration into ThoughtRecord analysis | Backlog |
| DBTSkillPractice | Full type defined | Not a priority | Backlog |

---

*Generated by automated scheduled task on 2026-05-06.*
