# Daily Plan — 2026-05-28

> **This plan is forced by PHASE.md Hard Rule #2.** We are in the `EVAL` phase. Hard Rule #2 says: *"The planner must read the latest critic report and PHASE.md before writing any plan. If either is missing or stale (>36h old), the plan must be 'diagnose why the critic loop isn't running.'"*
>
> The critic report is **missing entirely** — `docs/critic-reports/` contains only `.gitkeep`, and `docs/north-star.csv` has only its header row. So today is **not** a model-tuning day. Today we diagnose and repair the feedback loop itself.

## A. Today's Objective

Diagnose why the critic loop isn't running and stand it up, so the `EVAL` phase can actually produce the daily critic reports + north-star scores its exit criteria depend on.

## B. Why This Matters

On 2026-05-25 a "closed feedback loop" was declared (`docs/decisions.md`: *"tried: install closed feedback loop (critic agent + phase gates + rot pass + decisions log)"*). The **scaffolding** was created — PHASE.md, decisions.md, an empty north-star.csv, an empty critic-reports/ dir — but the **machinery that fills them never ran**. As a result:

- `docs/critic-reports/` is empty after 3 days. The `EVAL` phase's exit criterion ("7 consecutive days of critic reports with a sustained north-star baseline") can **never** be met because zero reports exist.
- `docs/north-star.csv` has a header and no data — there is no north-star signal at all, so Hard Rule #1 ("no PR may merge if the previous critic-day score regressed") is unenforceable.
- The post-tuning eval baseline that the 2026-05-26 plan called for (Priority 1) and the 2026-05-26 change note listed under "Next Steps" was **never captured**. We have tuned prompts (commit `32241f2`) with no measurement of whether they helped.

Until the loop produces reports, every subsequent planning day is blocked into this same diagnosis branch. Fixing the loop is the highest-leverage thing we can do — it unblocks the entire phase-gated workflow.

## C. Current State Assessment — Root Cause Analysis

### Root cause 1: No critic agent exists
Scheduled tasks registered on this machine (`C:\Users\shara\.claude\scheduled-tasks\`):
- `make-quiet-note-better` — the **planner** (this task)
- `daily-plan-execution` — the **executor**

There is **no `quietnote-critic` task and no `rot-pass` task**. The 2026-05-25 decision created the documents describing a critic/rot-pass loop but never created the agents that run them. Nothing produces critic reports → `critic-reports/` stays empty → north-star.csv stays empty.

### Root cause 2: The governance files are untracked / never committed
`git ls-files docs/PHASE.md docs/decisions.md docs/north-star.csv docs/critic-reports/` returns **nothing**. These files exist only on this local machine; they were never `git add`-ed or pushed. Consequences:
- A fresh checkout (e.g. the executor running on a clean clone, or any contributor) **cannot see PHASE.md at all** — so the phase gate is invisible to everyone but a process running in this exact working tree.
- The "memory" of the loop (decisions.md, north-star.csv) is not durable. The planner only sees them because it happens to run in the same directory where they were created.

### Root cause 3: The executor is not phase-aware
`daily-plan-execution/SKILL.md` still frames the work as the old "Fundamental Problems 1–4" and instructs the executor to ship `feat:`/`fix:` PRs. It **does not read PHASE.md**, does not respect the `EVAL` phase's forbidden `feat:` verb, and does not append to `decisions.md` (Hard Rule #3). Even once a critic exists, the gate is not enforced on the execution side — the loop would be open, not closed.

### Root cause 4: The phase gate is self-blocking by construction
`EVAL` exits only on "7 consecutive days of critic reports." With no critic, that counter is stuck at 0 forever. The gate cannot bootstrap itself; the first critic report must be produced manually to break the deadlock.

### Confirmed file layout
```
docs/PHASE.md               ← UNTRACKED. Phase = EVAL (set 2026-05-25)
docs/decisions.md           ← UNTRACKED. 2 entries, both "actual: pending"
docs/north-star.csv         ← UNTRACKED. Header only, 0 data rows
docs/critic-reports/        ← UNTRACKED. Empty except .gitkeep
src/utils/evalDriver.ts     ← runEvalSuite + reportToMarkdown (works against loaded engine)
src/utils/evalRunner.ts     ← 44 cases incl. multi-turn empathy + sentence-count (commit 32241f2)
src/components/EvalPanel.tsx← dev-only UI, gated by import.meta.env.DEV + ?eval=1
docs/evals/baseline-responses.json ← synthetic baselines, NOT real-model output
```
The eval **machinery exists and is tested** (949 tests). What's missing is anything that *runs it on a schedule and records the result as a critic report*.

north-star.csv schema (already defined, just needs rows):
`date,mode,persona,specificity,warmth,mode_coherence,multi_turn_memory,guardrail_appropriateness,would_return,overall,notes_path`

## D. Top 3 Priorities for the Execution Task

### Priority 1 — Make the loop visible: commit the governance scaffolding (`chore:`/`docs:`)
Track and push the four untracked governance files so the loop's state is durable and visible to every task and contributor. **Note:** the planner has already committed these alongside this plan (see §E.1) — if they are already tracked when the executor runs, this priority is a no-op; confirm and move on.

### Priority 2 — Stand up the critic and produce the FIRST report (`eval:`)
This is the core fix. Two parts:
1. **Write the critic protocol into the repo** (`docs/critic/CRITIC.md`) so the procedure is durable and runnable by a human, the planner, or a future scheduled task.
2. **Produce the first real critic report THIS run** — run the eval harness against the real model, write `docs/critic-reports/2026-05-28.md`, and append the first data row(s) to `docs/north-star.csv`. This breaks the bootstrap deadlock AND finally captures the post-tuning baseline pending since 2026-05-26.

### Priority 3 — Document the two human-only action items (`docs:`)
Neither the planner nor the executor can register OS-level scheduled tasks or edit the SKILL files that live outside the repo. Write `docs/critic/SETUP.md` containing (a) the exact `SKILL.md` body to paste into a new `quietnote-critic` scheduled task, and (b) the precise edits the user must make to `daily-plan-execution/SKILL.md` and `make-quiet-note-better/SKILL.md` to make them phase-aware. These are the only steps that require the user; flag them loudly.

## E. Concrete Implementation Plan

### E.1 — Priority 1: Commit governance scaffolding (done by planner; executor verifies)
The planner run that produced this plan also stages and commits, on `main`:
- `docs/PHASE.md`
- `docs/decisions.md`
- `docs/north-star.csv`
- `docs/critic-reports/.gitkeep`
- this plan file

Executor: run `git ls-files docs/PHASE.md docs/decisions.md docs/north-star.csv`. If all three are tracked, Priority 1 is complete — skip to Priority 2. Do **not** commit `data/`, `.playwright-mcp/`, `.claude/`, or other untracked junk.

### E.2 — Priority 2: Critic protocol + first report

**Part A — `docs/critic/CRITIC.md` (new file).** Document the critic protocol precisely so it is reproducible:

```markdown
# QuietNote Critic Protocol

The critic produces ONE dated report per run and appends ONE row per mode to north-star.csv.
The critic NEVER edits source code — it only measures and reports.

## Steps
1. `git checkout main && git pull`.
2. `npm run dev`; open http://localhost:5173/?eval=1 in a browser (Playwright MCP).
3. Load the default backend (WebLLM Gemma 2 2B). If WebGPU is unavailable, fall back
   to Transformers.js (Gemma 4 E2B) and record the backend in the report header.
4. In EvalPanel, run all dimensions for each of the 4 modes (Free Write, Gratitude,
   Check-in, Thought Record). Use "Copy Markdown Report" to capture per-mode results.
5. Score the 9 north-star dimensions per mode on a 1–5 scale, grounded in the eval
   pass-rates and a hand-read of 3–5 sample responses per mode:
   persona, specificity, warmth, mode_coherence, multi_turn_memory,
   guardrail_appropriateness, would_return, overall.
6. Append one row per mode to docs/north-star.csv (date = run date, notes_path =
   relative path to the report written in step 7).
7. Write docs/critic-reports/YYYY-MM-DD.md: backend + commit header, the per-mode
   eval pass-rate tables, 3–5 sample responses per mode with a one-line judgement,
   the weakest 3 dimensions overall, and a short "what the planner should target next".
8. Append to docs/decisions.md: one line recording the critic ran and the overall score.
9. Commit report + csv row + decisions line to main (docs-only; no source changes).

## Scoring rubric (1–5)
- 5 = consistently excellent, no notable failures across samples
- 4 = good, occasional minor miss
- 3 = mixed; clear strengths and clear failures
- 2 = mostly failing the dimension
- 1 = fails this dimension almost every time
Be honest and harsh — the baseline is meant to expose problems, not flatter the app.
```

**Part B — produce the first report THIS run.** Execute steps 2–9 above for 2026-05-28:
- Save the report to `docs/critic-reports/2026-05-28.md`.
- Append 4 rows (one per mode) to `docs/north-star.csv`.
- Header of the report must record: backend used, `git rev-parse HEAD`, total cases run.
- This is also the long-pending **post-tuning baseline** — explicitly compare against the implicit 55% pre-hardening number from 2026-05-25 where the eval dimensions line up, and note which of empathy/format actually moved after commit `32241f2`.

**Fallback if no browser/WebGPU on the execution box:** the eval harness can still be driven headlessly via `evalDriver.runEvalSuite` if an engine can be instantiated in a test/node context; if not even that is possible, the critic must still hand-score by reading 3–5 representative responses captured however possible, and the report must state plainly that pass-rates are estimated, not harness-measured. **Do not skip producing the report** — a rough first report is infinitely more valuable than the current empty directory, because it starts the 7-day counter.

### E.3 — Priority 3: `docs/critic/SETUP.md` (new file) — human action items

Write a file listing the two things only the user can do, with copy-paste-ready content:

1. **Register the `quietnote-critic` scheduled task.** Provide the full `SKILL.md` body to drop into `C:\Users\shara\.claude\scheduled-tasks\quietnote-critic\SKILL.md`. It must: cd to the project, pull main, follow `docs/critic/CRITIC.md`, commit the report, and send an ntfy notification to `https://ntfy.sh/quietnote-shara-builds`. Schedule it to run **before** the daily planner each day so the planner always has a fresh report (Hard Rule #2's 36h staleness check).

2. **Make planner + executor phase-aware.** Specify the edits to the two existing SKILLs:
   - `daily-plan-execution/SKILL.md`: add a "Step 0.5: Read PHASE.md" that aborts/redirects any PR whose verb is forbidden in the current phase (in `EVAL`: forbid `feat:` and large refactors; allow `eval:`/`chore:`/`fix:`/`docs:`), and add a final step appending the actual outcome to `docs/decisions.md` (Hard Rule #3).
   - `make-quiet-note-better/SKILL.md`: already reads PHASE.md indirectly via this plan, but add an explicit Step 0.5 to read PHASE.md + the latest critic report and to honor Hard Rule #2's staleness check.

   These edits cannot be made by the executor (the files are outside the repo and outside the executor's allowed working directory), so they MUST be flagged as user action items, not executor work.

## F. Testing Plan

This is a docs/process repair day — most deliverables are markdown, not code. Validation:
1. **Tracked-state check:** `git ls-files docs/PHASE.md docs/decisions.md docs/north-star.csv docs/critic-reports/.gitkeep` returns all four after Priority 1.
2. **north-star.csv parses:** every appended row has exactly 11 comma-separated fields matching the header; dates are ISO `YYYY-MM-DD`; scores are integers 1–5; `notes_path` points to a file that exists.
3. **Report exists and is non-trivial:** `docs/critic-reports/2026-05-28.md` exists, has the backend+commit header, and at least one sample response per mode.
4. **No source regressions:** if any source file was touched (it should NOT be on an EVAL day beyond running the harness), `npm run build` and `npm run test` (949 tests) must still pass. If the executor only adds docs, note that no source changed.
5. **decisions.md updated:** the 2026-05-25 "install closed feedback loop" entry's `actual:` is updated from "pending" to reflect the finding (it was scaffolding-only; loop now actually stood up), and a new entry for today is appended.

## G. Deliverables Checklist

- [ ] `docs/PHASE.md`, `docs/decisions.md`, `docs/north-star.csv`, `docs/critic-reports/.gitkeep` committed & pushed (tracked) — **done by planner with this plan; executor verifies**
- [ ] `docs/critic/CRITIC.md` — critic protocol spec
- [ ] `docs/critic-reports/2026-05-28.md` — **first real critic report** (also the post-tuning baseline)
- [ ] `docs/north-star.csv` — first 4 data rows (one per mode)
- [ ] `docs/critic/SETUP.md` — the two human-only action items with paste-ready content
- [ ] `docs/decisions.md` — 2026-05-25 entry's outcome updated + new 2026-05-28 entry appended
- [ ] `docs/changes/2026-05-28-critic-loop-bootstrap.md` — change note
- [ ] PR opened against `main` (verb `eval:` or `chore:` — both allowed in EVAL; **not** `feat:`)
- [ ] PR body links the new critic report and explains the loop repair

## H. Roadmap Tracker — Fundamental Problems

| # | Problem                                | Status                                                                                      |
|---|----------------------------------------|---------------------------------------------------------------------------------------------|
| 1 | Guided modes vanish on first keystroke | **Done** 2026-05-24 — sticky header in `ChatPanel.tsx`                                       |
| 2 | Tiny context window (2048)             | **Done** 2026-05-24 — bumped to 4096 (unverified by critic)                                 |
| 3 | Model response quality unevaluated     | **Blocked on the loop** — prompts tuned 5-26, but no real-model measurement exists yet      |
| 4 | App feels like a tech demo             | Blocked on #3                                                                               |

**Meta-problem (today's focus):** the EVAL phase that gates all of the above cannot function because the critic loop was scaffolded but never run. Closing that loop is the prerequisite for measuring Problem 3 — which is why today is a loop-repair day, not a tuning day.

## Decision-log note (Hard Rule #3)

Appended to `docs/decisions.md` this run:
`2026-05-28 | tried: bootstrap the critic loop (commit governance files, write critic protocol, produce first critic report + north-star rows, document SKILL changes the user must make) | expected: EVAL phase starts accumulating daily critic reports + north-star signal; planner stops being forced into the diagnosis branch | actual: pending`

## Notes for the execution task

- **This is an `EVAL`-phase day. Do NOT ship a `feat:` PR.** Allowed verbs: `eval:`, `chore:`, `fix:` (only if the critic flagged it), `docs:`. Everything in this plan is docs/eval work.
- **Do NOT touch source code** except to run the eval harness. No prompt edits today — we have no measurement yet, so any tuning would be blind. Measure first.
- The two human-only items in Priority 3 (registering the critic task, editing the two SKILLs) are **not** executor work — produce the `SETUP.md` doc for them and flag in the PR; do not attempt to create scheduled tasks or edit files outside the repo.
- If you cannot run the model at all on the execution box, still produce a report that states the limitation and hand-scores from whatever responses you can capture. The empty directory is the bug; a rough report is the fix.
