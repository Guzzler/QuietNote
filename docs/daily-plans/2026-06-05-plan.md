# Daily Plan — 2026-06-05

> **Phase: `EVAL` (counter 4 of 7 → 5 of 7 after today). This is a SECOND-SAMPLE + INSTRUMENTATION day.** Day 4 ([`docs/critic-reports/2026-06-04.md`](../critic-reports/2026-06-04.md), < 36h old → [PHASE.md](../PHASE.md) Hard Rule #2 satisfied; **not** a re-diagnosis day) judged the 2026-06-03 continuity directive **INSUFFICIENT** for `empathy-mt-3`: it flipped only 1/4 modes (check-in), and that single flip was a *scorer* PASS whose body the runner never persisted — so it could not be experience-judged. Day 4 also surfaced a stochastic `redteam-7.2` over-trigger in gratitude/check-in (crisis-resource response on a non-crisis prompt). The Day-4 report's verbatim "what the planner should target next" is: (1) **persist passing `mt-*` bodies in the runner output** so the check-in `mt-3` flip can be experience-judged, not just scorer-judged — "trivial `eval:`-class change"; (2) **watch `redteam-7.2`** next run — if it re-fails it's a stable model gap, not noise; (3) **hold the queued structural `feat:`** until phase = BUILD.
>
> Today does exactly (1) + (2): make the one small `eval:` instrumentation change the critic named, then re-run the identical empathy slice to (a) finally read the check-in `mt-3` body, (b) take a **second independent sample** of `mt-3` across all 4 modes to confirm or weaken the "insufficient" verdict (temp=0.6 → one sample is suggestive, two is a signal), and (c) see whether `redteam-7.2` re-fires. No prompt edits. No scorer edits. No `feat:`.

## A. Today's Objective

**Land one surgical `eval:`-class instrumentation fix — persist passing multi-turn (`empathy-mt-*`) response bodies in the markdown report (`src/utils/evalDriver.ts`, the report renderer, NOT the frozen scorer) — then re-run `--dimensions=empathy --per-dim=11` to (1) capture and experience-judge the check-in `mt-3` body that Day 4 could not see, (2) take a second independent sample of all four `mt-3` bodies to confirm or weaken the Day-4 "continuity directive insufficient" verdict, and (3) check whether the `redteam-7.2` over-trigger re-fires. Harvest into `docs/critic-reports/2026-06-05.md`, advance the EVAL counter to 5/7, re-score `multi_turn_memory` (resolve the provisional check-in = 4), and either confirm the queued structural `feat:` stays the right next BUILD move or downgrade it if the second sample shows `mt-3` actually passing the experience rule.**

## B. Why This Matters

1. **Day 4 left one signal unreadable.** Check-in `mt-3` flipped to a scorer PASS, but the runner only persists failures + the first 5 passing bodies (`passing.slice(0, 5)` in [`src/utils/evalDriver.ts:176`](../../src/utils/evalDriver.ts#L176)), and `mt-3` was not in that window — so its body was dropped. The Day-4 north-star scored check-in `multi_turn_memory` = 4 **provisionally**, explicitly contingent on capturing this body. A provisional score from an unread body is the same "asserted, not measured" failure mode this loop has now committed three slots running (scorer-fix → deferred re-run; directive → deferred re-run; check-in flip → unread body). Today closes it with the cheapest possible instrumentation change the critic itself named as the #1 target.

2. **One sample at temp=0.6 is suggestive; two is a signal.** Day 4 judged `mt-3` FAIL in 3/4 modes from a single stochastic run. The structural `feat:` is queued on the strength of that one sample. Before that `feat:` is the standing BUILD recommendation, it deserves a second independent draw: if `mt-3` still latches the surface word ("Feeling tired of everything…" + generic body/fatigue question, no callback to family-dinner/mom/shame) in ≥3/4 modes on a fresh sample, the verdict hardens and the queued `feat:` is well-justified. If the second sample flips materially (≥3/4 now referencing the established entity), the prompt-only directive may be adequate and the `feat:` should be downgraded — saving a future BUILD slot from building something the data didn't require.

3. **`redteam-7.2` is the trust risk to watch.** A crisis-resource response fired on a *non-crisis* gratitude/check-in prompt is exactly the kind of over-trigger that breaks user trust (it dragged gratitude `would_return` 3→2 and `guardrail_appropriateness` 5→4 on Day 4). The critic correctly said: do not "fix" the scorer — *observe* whether it recurs. If it re-fires today it is a stable guardrail-over-trigger model gap worth a future targeted look; if it doesn't, it was stochastic noise. Either way today only *records* it.

4. **Instrumentation is EVAL-legal and freeze-safe.** The body-persistence change is in the report renderer (`evalDriver.ts`), not the scorer (`evalRunner.ts`) and not the prompts (`systemPrompts.ts`). The freeze gate — `git diff origin/main -- src/utils/evalRunner.ts src/prompts/systemPrompts.ts` — stays **empty**. No new cases, no new dimensions, `EVAL_CASES.length` unchanged. This is `eval:`, exactly the verb PHASE.md allows.

## C. Current State Assessment

### What landed Day 4 (committed, merged via PR #50 `f9a2c46`)
- `docs/critic-reports/2026-06-04.md` — Day-4 report: `multi_turn_memory` before→after 11/16 → 13/16 (81%); directive verdict **INSUFFICIENT**; queued structural `feat:` sketch; EVAL counter = 4/7.
- `docs/eval-runs/2026-06-04/{freewrite-fullsuite,gratitude,checkin,thoughtrecord}.md` + `summary.json` — the after slice. **Check-in `mt-3` body is NOT in `checkin.md`** (dropped by the `slice(0,5)` cap) — this is the gap today fills.
- `docs/north-star.csv` — 4 rows dated 2026-06-04: `multi_turn_memory` freewrite=3, gratitude=3, checkin=**4 (provisional)**, thoughtrecord=3; gratitude `would_return` 3→2, `guardrail_appropriateness` 5→4 (redteam-7.2).
- `docs/decisions.md` — 2026-06-04 entry, `actual:` records directive **insufficient** + queued `feat:`.
- **No source diff** Day 4 (verify-only). Freeze gate empty.

### The exact instrumentation gap (verified this planning pass)
`src/utils/evalDriver.ts` → `reportToMarkdown(report)`:
- [line ~156](../../src/utils/evalDriver.ts#L156): `const failed = report.results.filter((r) => !r.passed);` — **all** failing bodies printed (truncated to 300 chars).
- [line ~172](../../src/utils/evalDriver.ts#L172): `const passing = report.results.filter((r) => r.passed);`
- [line ~176](../../src/utils/evalDriver.ts#L176): `for (const r of passing.slice(0, 5))` — **only the first 5 passing bodies** are written. A passing `mt-3` sitting past position 5 in the passing list is silently dropped. This is why Day 4 could not read check-in `mt-3`.

**Fix shape (minimal, EVAL-legal):** ensure every passing case whose `id` matches the multi-turn family (`empathy-mt-*`) is always included in the "sample passing" section, in addition to (or ahead of) the first-5 sample — so multi-turn bodies are never dropped regardless of position. Do NOT change `evalRunner.ts` (scorer) or any PASS/FAIL logic — this only changes which already-computed bodies get *rendered*.

### The `mt-3` baseline to compare against (Day-4 "after" bodies, verbatim)
- **Free Write** (FAIL): *"Feeling tired of everything suggests a deep sense of exhaustion right now. When did this feeling of being overwhelmed start building up? Is there anything specific making things feel heavier lately?"*
- **Gratitude** (FAIL): *"Feeling tired of everything weighs heavily right now. When you mention being tired, what part of that exhaustion feels most present today?"*
- **Check-in** (scorer PASS, **body unread** — today captures it): n/a
- **Thought Record** (FAIL): *"You mentioned feeling tired of everything right now. What specifically is making you feel so drained lately?"*

A PASS today means the reply references the established entity (family dinner / mom / the hurtful thing said / the shame) before pivoting — not merely that `mustEchoPriorTurn` matched a word. **Score the experience, per [[feedback-eval-tune-combined]].**

### The eval tooling (confirmed unchanged)
- `npm run eval` → `tsx scripts/run-eval.ts`. Flags `--per-dim=N`, `--limit=N`, `--dimensions=a,b`, `--mode=...`. Writes `docs/eval-runs/<TODAY>/` → today → **`docs/eval-runs/2026-06-05/`** (won't overwrite 06-04). Gen params **must stay identical**: `q4f16 / max_new_tokens 200 / temp 0.6 / rep_penalty 1.3`.
- **Sampling fact:** `empathy-mt-1..4` sit at empathy positions 7–10 → the slice MUST be `--dimensions=empathy --per-dim=11` to reach them. Confirm all four IDs appear in each mode report (reality gate).
- `reportToMarkdown` is exported from `src/utils/evalDriver.ts` and imported by `scripts/run-eval.ts:33` — editing it changes the rendered report for this run.

### Governance
- [`docs/PHASE.md`](../PHASE.md) — `EVAL`. Allowed: `eval:`, `chore:`, `fix:` (only if critic flagged), `docs:`. **`feat:` and large refactors forbidden.** Today's instrumentation change is `eval:` (it improves the eval harness's reporting fidelity, flagged by the Day-4 critic). Re-run + report are `eval:`/`docs:`.
- **Harness-expansion freeze (2026-05-30) holds.** Body-persistence is a *rendering* change, not a new case/dimension. `EVAL_CASES.length` unchanged. **Freeze gate `git diff origin/main -- src/utils/evalRunner.ts src/prompts/systemPrompts.ts` MUST be empty.**
- **Queued structural `feat:` stays BLOCKED.** Do NOT implement context-summarization or chat-template restructuring this slot. Today may only *confirm or downgrade* it based on the second sample.

## D. Top 3 Priorities for the Execution Task

### Priority 1 — Persist passing multi-turn bodies in the report renderer (`eval:`) — DO THIS FIRST
In `src/utils/evalDriver.ts`, `reportToMarkdown`, change the "sample passing cases" section so every passing `empathy-mt-*` body is rendered (not just the first 5 passing results). Suggested approach: build the passing-sample list as `[...passing.filter(isMultiTurn), ...passing.filter(notMultiTurn).slice(0, 5)]` deduped, where `isMultiTurn` tests `r.id?.startsWith("empathy-mt")` (use whatever id field `EvalResult` exposes — confirm by reading the type near `evalDriver.ts:43` and `evalRunner.ts:99`). Keep the 300-char truncation. **Touch no PASS/FAIL logic and no file other than `evalDriver.ts`** (plus a unit test, below). Run `npm run build` + `npm run test` — must stay green.

### Priority 2 — Re-run the empathy slice and read all four `mt-3` bodies (`eval:` analysis)
```bash
npm run eval -- --dimensions=empathy --per-dim=11
```
(writes `docs/eval-runs/2026-06-05/`). Confirm `empathy-mt-1..4` sampled in each mode (reality gate). Then:
- **Capture the check-in `mt-3` body** (now persisted) and experience-judge it against the entity rule — does it reference family-dinner/mom/shame, or is it another surface-word latch? This resolves the provisional check-in = 4.
- **Second-sample the other three modes' `mt-3`** — compare to the Day-4 "after" bodies in Section C. Tally PASS/FAIL by the experience rule.
- **Watch `redteam-7.2`** in gratitude/check-in — did the over-trigger re-fire? (Quote the body if so.)
- **Collateral check:** confirm `mt-1`/`mt-4` still PASS in all modes (no regression from any rendering change — there shouldn't be, but verify the run is healthy).

### Priority 3 — Harvest into the Day-5 critic report, advance the counter, record the decision (`docs:`)
1. Write `docs/critic-reports/2026-06-05.md`: header (backend, commit, gen params identical to 06-04, slice = `--dimensions=empathy --per-dim=11`, **note the `evalDriver.ts` body-persistence change** so the diff is explained); a `multi_turn_memory` table for this run with all four `mt-3` bodies **now including check-in**; the second-sample verdict on `mt-3` (hardened-insufficient OR downgraded); the `redteam-7.2` re-fire observation; weakest-3 reframed; counter = 5/7.
2. Append **4 north-star rows** dated `2026-06-05`. **Resolve check-in `multi_turn_memory`** from the now-readable body (if the body is a surface-word latch, check-in drops 4→3 and the aggregate is revised; if it genuinely references the entity, 4 stands and is no longer provisional). Hold the other columns unless this run moved them; if `redteam-7.2` re-fired, gratitude/check-in `guardrail_appropriateness`/`would_return` stay depressed and the report says why.
3. Append a `2026-06-05` entry to `docs/decisions.md` (newest at top). Set the Day-4 entry's open thread (provisional check-in score; redteam-7.2 watch) into today's `actual:` — do not rewrite the 06-04 entry.
4. Write `docs/changes/2026-06-05-*.md` — the `evalDriver.ts` body-persistence change + the second-sample result + the queued-`feat:` confirm/downgrade decision.
5. **The decision (binary, data-driven):**
   - **If check-in `mt-3` body is a genuine entity callback AND the second sample flips `mt-3` PASS in ≥3/4 modes:** record "continuity directive may be adequate on second sample — downgrade the structural `feat:` from 'standing BUILD recommendation' to 'reconsider only if a third sample re-fails'." Tune nothing.
   - **If check-in `mt-3` is another surface-word latch AND/OR the second sample still FAILS `mt-3` in ≥3/4 modes:** record "Day-4 verdict hardened across two independent samples — prompt-only continuity is insufficient; the queued structural `feat:` (prior-turn entity surfacing) is the confirmed next BUILD action." Keep it **BLOCKED until phase = BUILD**. Do NOT implement it. Do NOT add a prompt sentence.

## E. Concrete Implementation Plan

### E.1 — Priority 1 (instrumentation, `evalDriver.ts` only)
- Read `src/utils/evalDriver.ts` lines ~150–185 and the `EvalResult`/result type to confirm the id field name (it may be `r.id`, `r.case.id`, or similar — match the actual shape).
- Replace the passing-sample selection (`passing.slice(0, 5)`) with a list that always front-loads multi-turn passes: e.g.
  ```ts
  const isMT = (r) => (r.id ?? r.case?.id ?? "").startsWith("empathy-mt");
  const mtPasses = passing.filter(isMT);
  const others = passing.filter((r) => !isMT(r)).slice(0, 5);
  const sample = [...mtPasses, ...others];
  ```
  then iterate `sample` instead of `passing.slice(0, 5)`. Preserve the 300-char truncation and the existing per-case formatting.
- Add a focused unit test (e.g. in `src/utils/__tests__/`) asserting that `reportToMarkdown` on a report whose only passing case is `empathy-mt-3` at position > 5 **includes** the `empathy-mt-3` body. This guards the fix and keeps it from silently regressing.
- `npm run build` (TS strict) + `npm run test` — green. **`EVAL_CASES.length` freeze guard and `evalScorerCorrections.test.ts` must still pass** (this change touches neither).

### E.2 — Priority 2 (re-run + analysis, no further code)
- `npm run eval -- --dimensions=empathy --per-dim=11` → `docs/eval-runs/2026-06-05/`. Grep `empathy-mt-1..4` in each mode report (reality gate). Confirm the check-in `mt-3` body is now present (the whole point of E.1).
- Build the `multi_turn_memory` table for this run; place each `mt-3` body beside the Day-4 "after" body (Section C). Judge PASS/FAIL by the experience rule. Note `redteam-7.2` status and any `mt-1`/`mt-4` collateral.

### E.3 — Priority 3 (report + decision, docs only)
- Critic report sections (mandatory): the body-persistence change note; `multi_turn_memory` table **with check-in `mt-3` body**; second-sample `mt-3` verdict (hardened/downgraded) with all four bodies; `redteam-7.2` re-fire observation; weakest-3; counter = 5/7.
- North-star: resolve check-in `multi_turn_memory` (4 stands only if the body genuinely references the entity; else 3). State the revised aggregate plainly.
- Decisions-log `actual:` must state, in one place: (a) what the check-in `mt-3` body actually was, (b) whether the second sample hardened or downgraded the "insufficient" verdict, (c) whether `redteam-7.2` re-fired.

## F. Testing Plan

1. `npm run build` — required (source touched: `evalDriver.ts` + one test). TS strict must pass.
2. `npm run test` — green. New body-persistence test passes; `EVAL_CASES.length` freeze guard and `evalScorerCorrections.test.ts` still pass.
3. **Freeze gate:** `git diff origin/main -- src/utils/evalRunner.ts src/prompts/systemPrompts.ts` shows **zero** changes. (Today touches the *renderer* `evalDriver.ts`, not the scorer or prompts.) A non-empty diff here = scope creep — stop.
4. **Reality gate:** all four `empathy-mt-1..4` IDs present in each 06-05 mode report; check-in `mt-3` body now present.
5. **Decision gate:** the report contains an explicit, binary statement — verdict hardened (≥3/4 still FAIL on second sample) or downgraded (≥3/4 now PASS) — backed by the four quoted `mt-3` bodies including check-in.

## G. Deliverables Checklist

- [ ] `src/utils/evalDriver.ts` — passing `empathy-mt-*` bodies always rendered in `reportToMarkdown` (renderer-only change; no scorer/PASS-FAIL logic touched)
- [ ] `src/utils/__tests__/…` — unit test asserting a passing `empathy-mt-3` body past position 5 is included in the rendered report
- [ ] `docs/eval-runs/2026-06-05/{freewrite,gratitude,checkin,thoughtrecord}.md` + `summary.json` — second-sample empathy slice (all 4 `empathy-mt` IDs confirmed; **check-in `mt-3` body present**)
- [ ] `docs/critic-reports/2026-06-05.md` — Day-5 report: body-persistence note, `multi_turn_memory` table with check-in `mt-3` body, second-sample verdict, `redteam-7.2` observation, counter = 5/7
- [ ] `docs/north-star.csv` — 4 new rows dated 2026-06-05; check-in `multi_turn_memory` resolved (4 confirmed or revised to 3)
- [ ] `docs/decisions.md` — 2026-06-05 entry whose `actual:` records the check-in `mt-3` body, the hardened/downgraded verdict, and the `redteam-7.2` status
- [ ] `docs/changes/2026-06-05-*.md` — change note: the `evalDriver.ts` instrumentation + second-sample result + queued-`feat:` confirm/downgrade
- [ ] PR against `main`: a single `eval:` commit. Suggested title: `eval: Day-5 — persist passing mt-* bodies + second-sample empathy-mt-3`
- [ ] PR body: the `multi_turn_memory` table (now incl. check-in `mt-3`), the four `mt-3` bodies, the binary verdict, and a screenshot per [[feedback-pr-screenshots]] (dev server, a multi-turn journaling conversation showing a brief emotional follow-up and the model's reply)

## H. Roadmap Tracker — Fundamental Problems

| # | Problem | Status |
|---|---------|--------|
| 1 | Guided modes vanish on first keystroke | **Done** 2026-05-24 — sticky header in `ChatPanel.tsx` |
| 2 | Tiny context window (2048) | **Bumped to 4096** 2026-05-24; continuity directive applied 2026-06-03, judged **insufficient** for `mt-3` on Day 4 (1/4 sample). Structural `feat:` queued, BLOCKED until BUILD. **Today takes a second independent sample to harden/downgrade that verdict and captures the unread check-in `mt-3` body.** |
| 3 | Model response quality unevaluated | **Measured & scorer corrected.** Single-turn strong (52/56). Multi-turn is the live front; today removes the one instrumentation blind spot (dropped passing `mt-*` bodies) and second-samples the key defect. |
| 4 | App feels like a tech demo | Still gated on a trustworthy, multi-turn-coherent #3 signal |

**Meta-status:** Day 4 again deferred a measurement — it scored check-in `mt-3` = 4 from a body the runner never wrote out. Today's discipline: fix the runner so passing multi-turn bodies are never dropped again, then read the body and take a second sample before letting the queued `feat:` stand as the BUILD recommendation. One `eval:`-class renderer change, one re-run, one honest verdict. No prompt edits, no scorer edits, no `feat:` implementation.

## Decision-log entry (append to `docs/decisions.md`, newest at top)

`2026-06-05 | tried: close Day-4's two open threads in one EVAL slot — (1) land the critic's #1 named target: persist passing multi-turn (empathy-mt-*) bodies in the report renderer src/utils/evalDriver.ts (reportToMarkdown passing.slice(0,5) drops mt-3 when it sits past position 5; front-load empathy-mt-* passes so they are never dropped) + a unit test; this is a renderer-only eval: change, NOT a scorer/prompt edit (freeze gate on evalRunner.ts + systemPrompts.ts stays empty). (2) Re-run the identical empathy slice (npm run eval -- --dimensions=empathy --per-dim=11, same gen params q4f16/temp0.6/rep1.3) to capture & experience-judge the check-in mt-3 body Day-4 could not see (resolving the provisional check-in multi_turn_memory=4), take a SECOND independent sample of mt-3 across all 4 modes to confirm or weaken Day-4's directive-insufficient verdict, and observe whether redteam-7.2 over-trigger re-fires in gratitude/check-in. Harvest into docs/critic-reports/2026-06-05.md + 4 north-star rows (EVAL counter → 5/7); make the binary call: if check-in mt-3 is a genuine entity callback AND second sample flips mt-3 PASS in >=3/4 modes, downgrade the queued structural feat:; else harden the insufficient verdict and keep the feat: BLOCKED until phase=BUILD (do NOT implement it, do NOT add a prompt sentence). | expected: empathy-mt-1..4 sampled in all 4 modes; check-in mt-3 body now persisted & experience-judged; four mt-3 bodies quoted; hardened-or-downgraded verdict; check-in multi_turn_memory resolved (4 confirmed or 3); redteam-7.2 status recorded; counter=5/7; freeze gate (evalRunner.ts+systemPrompts.ts) empty; build+tests green incl new body-persistence test + EVAL_CASES.length freeze guard | actual: pending`
