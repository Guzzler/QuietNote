# Daily Plan — 2026-04-08

## A. Today's Objective

Fix mobile mode selector overflow, remove dead code, type-safe ChatPanel props, and add code splitting for the oversized production bundle — all focused on polish and technical debt before adding new features.

## B. Why This Matters

QuietNote has shipped 4 journaling modes, 2 inference backends, and 345 tests over the last week. But mobile UX is degraded (mode pills wrap text at 375px), the codebase has dead code (`useMLCEngine.ts`), ChatPanel uses `any` types in 5 places, and the production bundle is 28MB+ without code splitting. Fixing these now prevents compounding debt and ensures a solid foundation before the next feature push.

## C. Model Quality Assessment

**Environment limitation:** Preview environment lacks WebGPU; model inference cannot be tested in the automated preview.

**Structural verification (this session):**
1. Welcome screen renders correctly in Free Write mode with all privacy messaging
2. Mode selector shows 4 modes (Free Write, Gratitude, Check-in, Thought Record) — all switching works
3. Gratitude mode: "Step 1 of 3" with "What are you grateful for today?" — correct
4. Check-in mode: Correctly detects evening (after 12pm), shows "Evening Check-in" with Moon icon, "How was your day?" — correct
5. Thought Record mode: "Step 1 of 5" with "What happened? Describe the situation briefly." — correct
6. Privacy Dashboard shows "Inference Engine" section with WebLLM (Gemma 2 2B) and Transformers.js (Gemma 4 E2B) — correct labels
7. Error recovery UI works (model load failure → error banner + Retry button)
8. All 345 tests pass; `npm run build` succeeds with zero errors
9. Console errors limited to expected Cache API/WebGPU failures in preview

**Mobile UX issue found:** At 375px width, all 4 mode pills wrap their text across 2 lines:
- "Free Write" → "Free" / "Write"
- "Check-in" → "Check-" / "in"
- "Thought Record" → "Thought" / "Record"
This makes the mode selector harder to scan and looks cramped. **This is Priority 1 for today.**

**Previous assessments (through 2026-04-07):**
- Gemma 2 2B (WebLLM) response quality: 4/5 — warm, reflective, appropriately brief
- System prompt successfully blocks medical advice, maintains conciseness
- Crisis detection and guardrails verified and working
- Gemma 4 E2B (Transformers.js): model ID set, untested with real WebGPU — quality comparison pending

## D. Journaling Modes Exploration

### Current State (4 modes, all functional)
- **Free Write** — default mode, fully functional
- **Gratitude** — 3-step guided flow (PR #25)
- **Check-in** — Morning/Evening time-aware 3-step flow (PR #26)
- **Thought Record** — 5-step CBT guided flow (PR #27)

### Assessment: Modes are sufficient for now
Four modes cover the core journaling use cases well:
- Free-form reflection (Free Write)
- Positive psychology (Gratitude)
- Daily structure (Check-in)
- Cognitive restructuring (Thought Record)

Adding a 5th mode now would increase mobile overflow issues further and dilute the UX. **Today should focus on polishing the existing 4 modes** before considering:
- Stream-of-consciousness with AI summary (future — needs timer-based UI pattern)
- Emotional processing / affect labeling (future — overlaps with existing emotion extraction)
- Goal reflection / accountability (future — weekly cadence, different interaction model)

### Mobile mode selector needs attention
The immediate UX issue is making 4 pills fit cleanly on mobile. Options:
1. **Horizontal scroll with overflow-x-auto** — keeps all pills visible, allows swipe
2. **Abbreviated labels on mobile** — "Write", "Thanks", "Check", "CBT"
3. **Icon-only on mobile with tooltip** — most compact, but loses discoverability
4. **2x2 grid on mobile** — uses vertical space, keeps labels readable

**Recommendation:** Option 1 (horizontal scroll) — it's the most familiar mobile pattern, preserves full labels, and requires minimal code change. Add `overflow-x-auto` and `flex-nowrap` to the container, with `whitespace-nowrap` on pill labels.

## E. Model & Runtime Research

### Gemma 4 E2B — Status Update
- **Model ID set:** `onnx-community/gemma-4-E2B-it-ONNX` in `transformersjs-engine.ts`
- **Status:** Untested — requires real browser with WebGPU to verify download and inference
- **Blocker:** Preview environment lacks WebGPU/Cache API
- **Next step:** Manual testing on a WebGPU-capable browser (Chrome 121+)

### Runtime Status Summary (as of 2026-04-08)

| Runtime | Gemma 4 E2B | Gemma 2 2B | Status |
|---------|-------------|------------|--------|
| **Transformers.js v4** | YES (ONNX Q4) | YES | **Implemented** — model ID set to Gemma 4 E2B |
| **@mlc-ai/web-llm** | NOT YET | YES (current default) | **Implemented** — stable fallback |
| **MediaPipe LLM Inference** | YES (text-only) | N/A | Not implemented — deferred |
| **Chrome Built-in AI** | NO | NO | Not applicable |

### Build Size Concern
Production build output:
- `ort-wasm-simd-threaded.asyncify.wasm` — **22.8 MB** (ONNX Runtime WASM)
- `index.js` — **5.9 MB** (includes web-llm + app code)
- `transformers.web.js` — **540 KB**
- Total: **~29 MB** before gzip

This is acceptable for a model-downloading PWA, but the main JS bundle (5.9MB) could benefit from code splitting. Today's Priority 3 addresses this by lazy-loading the inference engines.

### Key Decision
No model or runtime changes today. Focus on code quality and UX polish. The runtime adapter architecture is proven with 2 backends and model quality comparison waits on WebGPU testing.

## F. Top 3 Priorities for the Execution Task

### 1. Fix Mobile Mode Selector Overflow (Objective 1B — UX)
The 4-pill mode selector wraps text on 375px mobile. Add horizontal scroll with `overflow-x-auto`, `flex-nowrap`, and `whitespace-nowrap`. This is the most user-visible issue.

### 2. Code Cleanup: Remove Dead Code + Type ChatPanel Props (All Objectives — Quality)
Remove the unused `useMLCEngine.ts` hook (confirmed zero imports). Replace the 5 `any` type annotations in `ChatPanel.tsx` (line 74 props, lines 160/382/383/386 message types) with proper types from `types.ts`. This reduces confusion and improves maintainability.

### 3. Code Splitting for Inference Engines (Objective 3 — Performance)
Lazy-load the inference engine modules (`webllm-engine.ts`, `transformersjs-engine.ts`) using dynamic `import()` in the engine factory. This keeps the initial JS bundle small and only loads the selected engine's code when needed. The WASM binary is already separate; this targets the 5.9MB JS bundle.

## G. Concrete Implementation Plan

### Priority 1: Fix Mobile Mode Selector Overflow

**Files to modify:**

1. **`src/components/JournalingModeSelector.tsx`** (line 29):
   - Change the container div classes from:
     ```
     className="inline-flex rounded-lg border border-slate-200 bg-slate-50 p-0.5 gap-0.5"
     ```
   - To:
     ```
     className="inline-flex rounded-lg border border-slate-200 bg-slate-50 p-0.5 gap-0.5 overflow-x-auto max-w-full"
     ```
   - Add `whitespace-nowrap` to each pill button (line 36):
     ```
     className={`flex items-center gap-1.5 px-3 py-1.5 text-xs font-medium rounded-md transition-all whitespace-nowrap ${...}`}
     ```
   - Alternatively, use `flex-shrink-0` on each button to prevent shrinking

**What to test:**
- Desktop (1280px): Mode selector still looks normal, no unnecessary scroll
- Mobile (375px): All 4 pills are readable without text wrapping, horizontal scroll if needed
- Tablet (768px): Pills fit without scroll
- Verify all 4 modes still switch correctly

### Priority 2: Code Cleanup

**A. Remove dead code:**

1. **Delete `src/hooks/useMLCEngine.ts`** — 80 lines, zero imports, fully superseded by `useInferenceEngine.ts`
   - Verify: `grep -r "useMLCEngine" src/` returns nothing (confirmed in this session)

**B. Type ChatPanel props:**

2. **`src/components/ChatPanel.tsx`** (line 74):
   - The component props are typed as `any`. Replace with a proper interface:
     ```typescript
     interface ChatPanelProps {
       session: Session | null;
       activeThread: Thread | null;
       onSend: (message: string) => void;
       loading: boolean;
       modelLoading: boolean;
       loadProgress: number;
       error: string | null;
       onRetry: () => void;
       journalingMode: JournalingMode;
       onModeChange: (mode: JournalingMode) => void;
       gratitudeStep: number;
       checkinStep: number;
       thoughtRecordStep: number;
       promptSuggestions: string[];
       modelRef: ModelRef | null;
     }
     ```
   - Import `Session`, `Thread`, `ModelRef` from `../types` and `JournalingMode` from `./JournalingModeSelector`

3. **`src/components/ChatPanel.tsx`** (lines 160, 382, 383, 386):
   - Replace `(m: any)` with `(m: ChatMessage)` — import `ChatMessage` from `../types`
   - This covers all 4 remaining `any` usages in the file

**What to test:**
- `npm run build` — zero TypeScript errors (proves types are correct)
- `npm run test` — all 345 tests still pass
- No behavioral changes — this is a pure type/cleanup refactor

### Priority 3: Code Splitting for Inference Engines

**Files to modify:**

1. **`src/inference/index.ts`** (lines 1-28):
   - Currently statically imports both engines at the top
   - Change to dynamic imports:
     ```typescript
     export async function createEngine(runtime: RuntimeId): Promise<InferenceEngine> {
       switch (runtime) {
         case "webllm": {
           const { WebLLMEngine } = await import("./webllm-engine");
           return new WebLLMEngine();
         }
         case "transformersjs": {
           const { TransformersJSEngine } = await import("./transformersjs-engine");
           return new TransformersJSEngine();
         }
         case "mediapipe":
           throw new Error("MediaPipe runtime not yet implemented");
       }
     }
     ```
   - Export model refs separately (they're small metadata objects, can stay static or be lazily loaded too)

2. **`src/hooks/useInferenceEngine.ts`**:
   - Update `createEngine` call if it's not already async (it likely needs to `await` the factory)
   - The hook already handles async model loading, so this should integrate cleanly

**What to test:**
- `npm run build` — check if the bundle splits (should see separate chunks for webllm and transformers.js)
- `npm run test` — all tests still pass
- Dev server: verify engine selection in Privacy Dashboard still works
- Check bundle sizes in build output — main chunk should be significantly smaller

## H. Testing Plan

### Automated Tests
- Run `npm run test` — all 345 existing tests must pass after changes
- Run `npm run build` — zero TypeScript errors
- No new test files needed today (changes are refactoring/styling, not new features)

### Manual / Preview Tests
- **Mobile (375px):** Mode selector pills readable, no text wrapping, horizontal scroll works
- **Desktop (1280px):** Mode selector unchanged, all 4 modes switch correctly
- **Privacy Dashboard:** Engine selector still works after code splitting
- **Error recovery:** Model load failure still shows error banner + Retry

### Regression
- All 4 journaling modes: Free Write, Gratitude, Check-in, Thought Record
- Mode switching resets steps correctly
- Sessions, mood tracker, privacy dashboard unchanged
- Crisis detection and guardrails unaffected

## I. Deliverables

- [ ] `src/components/JournalingModeSelector.tsx` — Mobile-friendly overflow handling
- [ ] Delete `src/hooks/useMLCEngine.ts` — Remove dead code
- [ ] `src/components/ChatPanel.tsx` — Replace 5 `any` types with proper interfaces
- [ ] `src/inference/index.ts` — Dynamic imports for code splitting
- [ ] `src/hooks/useInferenceEngine.ts` — Updated for async engine factory (if needed)
- [ ] All 345 tests pass, build succeeds with zero TS errors
- [ ] Build output shows separate chunks for inference engines
- [ ] PR opened with mobile screenshot showing fixed mode selector

## J. Roadmap Tracker

### Objective 1: Model Quality & Journaling Modes
| Item | Status |
|------|--------|
| Model output quality (Gemma 2 2B) | Rated 4/5 — adequate |
| System prompt tuning | Stable |
| Crisis detection & guardrails | Working |
| Guided Gratitude mode | **Done** (PR #25) |
| Morning/Evening Check-in mode | **Done** (PR #26) |
| CBT Thought Record mode | **Done** (PR #27) |
| **Mobile mode selector fix** | **Planned for today** |
| Stream-of-consciousness mode | Designed, not started |

### Objective 2: Port to Gemma 4 E2B / Explore Models
| Item | Status |
|------|--------|
| Gemma 4 E2B availability assessment | Done |
| Transformers.js v4 backend | **Done** (PR #26) |
| Upgrade to Gemma 4 E2B ONNX | **Done** (PR #27) — model ID set, untested with WebGPU |
| Model quality comparison (Gemma 2 vs 4) | Blocked on WebGPU testing |
| Migration path | Phase 1 (adapter) done; Phase 2 (backends) done; Phase 2.5 (model upgrade) done |

### Objective 3: Explore Local Inference Paradigms
| Item | Status |
|------|--------|
| Design doc | Done — `docs/design/2026-04-02-local-gemma-runtime-plan.md` |
| Runtime adapter interface | **Done** (PR #25) |
| WebLLM backend (via adapter) | **Done** (PR #25) |
| Transformers.js backend | **Done** (PR #26) |
| Runtime selection UI | **Done** (PR #26) |
| Unit tests for engines | **Done** (PR #27) — 345 total tests |
| **Code splitting for engines** | **Planned for today** |
| **Dead code removal (useMLCEngine)** | **Planned for today** |
| MediaPipe/LiteRT-LM backend | Not started — deferred |
| Privacy truthfulness pass (Phase 3) | Not started |

### Completed Features
| Date | Feature | PR |
|------|---------|-----|
| 2026-03-28 | Humanize loading screen + welcome animations | #17 |
| 2026-03-29 | Header subtitle + mobile sessions + prompt link | — |
| 2026-03-30 | Humanize footer and placeholder text | #18 |
| 2026-03-31 | Phase 1 safety & correctness fixes | #19 |
| 2026-04-01 | Phase 2 accessibility improvements | #20 |
| 2026-04-02 | Aria-labels, storage display, new session button | #21 |
| 2026-04-03 | Mobile sessions, export feedback, mood grid, animation fix | #22 |
| 2026-04-04 | Auto-scroll, auto-grow textarea, layout overflow fix | #23 |
| 2026-04-04 | Session deletion with confirmation | #24 |
| 2026-04-05 | Runtime adapter + Guided Gratitude mode + TS fixes | #25 |
| 2026-04-06 | Transformers.js backend + Check-in mode + Runtime selector | #26 |
| 2026-04-07 | Unit tests + Gemma 4 E2B upgrade + CBT Thought Record | #27 |

### Remaining UX Issues
| # | Issue | Priority | Status |
|---|-------|----------|--------|
| 8 | Footer overlap on short viewports | Low | Open |
| 11 | ChatPanel props typed as `any` | Medium | **Planned today** |
| 20 | PromptSelector mobile overflow | Medium | Open |
| 22 | No keyboard shortcut hints | Medium | Open |
| 27 | Session titles are raw text truncation | Low | Open |
| 28 | Context trimming notice not dismissible | Low | Open |
| NEW | Mode selector pills wrap text on mobile (375px) | High | **Planned today** |
| NEW | Dead code: `useMLCEngine.ts` has zero imports | Medium | **Planned today** |
| NEW | Production bundle 29MB+ without code splitting | Medium | **Planned today** |

---

*Generated by automated scheduled task on 2026-04-08.*
