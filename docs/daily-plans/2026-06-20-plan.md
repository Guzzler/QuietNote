# 2026-06-20 (Day 19) — Track C population run: full conversation-script matrix + C3 gate resolution

> Planning-only document. The `daily-plan-execution` task implements this. Written autonomously by the `make-quiet-note-better` scheduled task.

## A. Today's objective

Run the **full conversation-script eval matrix** (`npm run eval -- --scripts --strategy=all`, 4 scripts × 3 strategies) against the real Gemma 4 E2B model to turn yesterday's **n=1** C2 result into a small population — adding the still-unmeasured **thoughtrecord 5-step coherence** signal and the shorter retention/checkin scripts under the real app path — then make the explicit, falsifiable **C3 go/no-go decision** from that data.

This closes the *population* half of Fundamental Problem #2 ("context management FIXED at 3-turn scale only — long-conversation behavior unmeasured"). It is the next step both [`docs/eval-runs/2026-06-20/C2-findings.md`](../eval-runs/2026-06-20/C2-findings.md) and [`docs/changes/2026-06-19-day18-conversation-script-baseline.md`](../changes/2026-06-19-day18-conversation-script-baseline.md) name verbatim.

## B. Why this matters

- **For users:** Problem #2 (the model forgetting what you wrote earlier in a long session) was only ever proven fixed at *3-turn* scale, then at *one* 19-turn freewrite. A user in a real 10–20-turn thought-record or evening check-in needs the model to still hold their established entity (their sister, their presentation) late in the conversation. We have measured this for exactly one freewrite script under one seed; everything else is an assumption.
- **For the eval loop:** Yesterday's clean result — *the prior-turn recap is the load-bearing element for entity retention; `managed` (recap+trim) 2/2 vs `raw` 0/2 vs `managed-norecap` 0/2* — is a single data point. It is a **falsifiable claim**: if a fuller multi-script run shows `managed` dropping post-trim probes anywhere, C3 (summarize-trimmed-turns-into-the-recap) re-opens; if `managed` holds across all probe-bearing scripts, C3 stays deferred and we can finally declare Problem #2 closed at long-conversation scale.
- **Unmeasured gap:** The **thoughtrecord step-coherence** question — does the model walk CBT steps 1→5 without skipping/looping over a full guided session under the *real* (`managed`) path? — has **never** been run live. It was bounded out of yesterday's CPU slot. This is the single biggest hole in Track C.

## C. Current state assessment

Track C is two-thirds done; the harness is built and proven, only the breadth run remains.

- **C1 (DONE, PR #63):** `ConversationScript`/`ScriptTurn` model + `runConversationScript` driver that threads the model's *real* replies through turns. All in new files; freeze gate empty.
- **C2 (DONE, PR #64):** Added `ContextStrategy` (`raw` | `managed` | `managed-norecap`), per-turn trim telemetry, `script-freewrite-longtrim` (19 turns), and `run-eval.ts` flags `--strategy=` / `--script=`. First **live** Gemma 4 E2B run (Node onnxruntime-node CPU) on the one boundary-crossing script × 3 strategies. Result: recap is load-bearing (2/2 vs 0/2 vs 0/2).
- **C3 (GATED/DEFERRED):** No decay observed yet to justify summarize-on-trim. The gate-reopener is explicitly "a fuller multi-script / multi-seed `--strategy=all` run shows `managed` dropping post-trim probes."

Key code already in place (no source changes needed to run this):

- Runner & flags: [`scripts/run-eval.ts:78-101`](../../scripts/run-eval.ts) (`--scripts`, `--strategy=…`, `--script=…`), [`scripts/run-eval.ts:216-248`](../../scripts/run-eval.ts) (the script-run loop, writes `conversation-scripts.md` + `summary.json` `scripts` block).
- npm aliases: `eval:scripts` (managed only) and **`eval:scripts:all`** (all 3 strategies) in `package.json`.
- The 4 scripts in [`src/utils/conversationScripts.ts`](../../src/utils/conversationScripts.ts):
  - `script-freewrite-retention` — 12 turns, 2 probes (Maya/dinner) at turns 8 & 11.
  - `script-freewrite-longtrim` — 19 turns, 2 probes at 17 & 19 (the boundary-crossing one already run at n=1).
  - `script-thoughtrecord-steps` — intro + 5 CBT steps, `stepIndex` on each, `expectedSteps: 5`. **No probes** — measures step coherence. **Never run live.**
  - `script-checkin-retention` — 10 turns, 1 probe (the 2pm client presentation) at turn 9. **Never run live.**
- The driver computes, per script run: `passedTurns/scoredTurns`, `probesPassed/probes`, `stepCoherent` (null for non-guided), `firstTrimTurnIndex`, `probesPassedAfterTrim/probesAfterTrim`. `scriptReportToMarkdown` already renders the summary table + per-script telemetry table + failing-turn bodies.

**Output-path note (important):** the runner stamps the output dir in **UTC** (`docs/eval-runs/<UTC-date>/`). It overwrites `conversation-scripts.md` and `summary.json` in that dir but does **not** touch `C2-findings.md`. The new full-matrix `conversation-scripts.md` is a strict superset of yesterday's (it includes `script-freewrite-longtrim` under all 3 strategies again), so a same-dir overwrite is acceptable — but **preserve `docs/eval-runs/2026-06-20/C2-findings.md`** as the historical n=1 record, and write today's analysis as a **new** file (see Priority 3) rather than editing C2-findings in place.

## D. Top 3 priorities for the execution task

1. **Run the full matrix** `npm run eval -- --scripts --strategy=all` (all 4 scripts × 3 strategies, ~141 CPU generations). If the CPU slot can't finish all 12 cells, fall back to the **bounded priority order** in Priority 1 below — but the goal is the full matrix.
2. **Analyze** the per-script, per-strategy results: probe pass-rates (population view), thoughtrecord step-coherence under `managed`, and the before/after-trim probe split for every probe-bearing script.
3. **Resolve the C3 gate** in writing and update the roadmap/decisions: either C3 stays *deferred* (managed holds everywhere → Problem #2 declarable closed at long scale) or C3 *re-opens* with the specific failing script/probe as justification.

## E. Concrete implementation plan

### Priority 1 — Run the matrix (the core deliverable)

**Primary command:**
```bash
cd "C:\Users\shara\OneDrive\Desktop\My Stuff\Work\Dev\QuietNote"
npm run eval:scripts:all
# equivalent to: tsx scripts/run-eval.ts --scripts --strategy=all
```
This runs all 4 scripts under `raw`, `managed`, and `managed-norecap`, writing `docs/eval-runs/<UTC-date>/conversation-scripts.md` and the `scripts` block of `summary.json`.

> The plain per-mode `EVAL_CASES` suite also runs first (the `for (const mode of RUN_MODES)` loop) unless you scope it. To spend the CPU budget on the *scripts* (the point of today), add `--mode=freewrite --limit=1` so the single-mode pre-pass is trivial and the scripts get the slot:
> ```bash
> npm run eval -- --mode=freewrite --limit=1 --scripts --strategy=all
> ```
> (This mirrors yesterday's invocation but **without** `--script=`, so all 4 scripts run.)

**Cost & timeout:** ~141 generations at ~10–15 s each on CPU ≈ 25–40 min of inference plus model load. Run it with a generous timeout / in the background and poll. Watch the per-turn console progress lines the runner prints.

**Bounded fallback (only if the full matrix can't finish in the slot)** — run in this priority order, because the highest-value *new* signal is thoughtrecord coherence and the population view of the probe scripts under the real path:

1. `--script=script-thoughtrecord-steps --strategy=managed` — the never-measured step-coherence signal under the real app path (6 turns, cheap, ~highest value).
2. `--strategy=managed` for **all 4 scripts** (the real-app-path population: `npm run eval -- --mode=freewrite --limit=1 --scripts --strategy=managed`). ~47 generations.
3. `--strategy=all` on the three **probe-bearing** scripts only (`--script=script-freewrite-retention,script-checkin-retention,script-freewrite-longtrim`) to get the raw/norecap isolation across the population. Skipping thoughtrecord here is fine — it has no probes, so raw/norecap add nothing for it.

Record in the change doc exactly which cells ran if you fall back, so the remaining cells are an explicit carry-forward.

**Optional stretch (only if a cell finishes fast and time remains):** a **second seed** on `script-freewrite-longtrim --strategy=managed` (generation already uses `do_sample:true`, `temperature:0.6`, no fixed seed, so a re-run is a fresh seed). A managed pass that holds 2/2 again strengthens the "no decay" claim; a drop would be the C3 re-opener. Breadth (all 4 scripts) beats depth (multi-seed of one) — only do this if breadth is already covered.

**Do NOT** change generation params, prompts, scorer, or `EVAL_CASES`. This is a measurement run on existing code. The freeze gate (`src/utils/evalRunner.ts`, `src/utils/evalScorer.ts`, `src/prompts/`) and the context/token utils (`tokenEstimator.ts`, `conversationContext.ts`) must stay byte-identical to `origin/main`.

### Priority 2 — Analyze the results

Read the generated `conversation-scripts.md` and `summary.json`. For each script × strategy, extract and tabulate:

- **Probe pass-rate** (`probesPassed/probes`) — the population view. Does `managed` hold ≥ `raw` and ≥ `managed-norecap` on *every* probe-bearing script (`freewrite-retention`, `freewrite-longtrim`, `checkin-retention`)?
- **Thoughtrecord step-coherence** (`stepCoherent`) under `managed` — true means the replies progressed 1→5 contiguously without skipping/looping. If false, capture *which* step broke from the failing-turn bodies. This is the new signal; give it its own short subsection.
- **Trim telemetry** — `firstTrimTurnIndex` and the **before/after-trim probe split** (`probesPassedAfterTrim/probesAfterTrim`). The decisive question for C3: on any script, does a probe that sits *after* the first trim turn **fail under `managed`**? (That is the literal decay condition C3 exists to fix.)
- **Scored (non-probe) turns** — confirm warmth/banned-opener/length criteria still hold at length across strategies (yesterday all 10/10). A regression here is independent of the recap question but worth noting.

Be explicit about which strategy is the **real app path** (`managed`) — that is the one whose numbers describe what users actually get. `raw` and `managed-norecap` exist only as the isolation comparison.

### Priority 3 — Resolve the C3 gate & document

Write a **new** analysis doc (do not overwrite the n=1 record):
`docs/eval-runs/<UTC-date>/C2-population-findings.md` (or `C-track-population-findings.md`). Include:

- The matrix table (script × strategy → probes passed, step-coherent, first-trim, after-trim probes).
- The thoughtrecord step-coherence finding.
- The **C3 decision**, stated as one of two falsifiable outcomes:
  - **C3 stays DEFERRED** — iff `managed` holds all post-trim probes across all probe-bearing scripts AND thoughtrecord coherence holds. Then: state that Problem #2's residual long-conversation risk is now measured-and-closed at population scale (caveats: model size, CPU/q4f16, substring-match probes, single seed per cell). No `RESERVED_FOR_GENERATION`/`MODEL_CONTEXT_LIMIT` change indicated.
  - **C3 RE-OPENS** — iff any script shows `managed` dropping a post-trim probe (or thoughtrecord step-coherence fails). Name the exact script/probe/step. The candidate fix per the roadmap is *summarize-trimmed-turns into the recap line* (extends Day-8 `conversationContext.ts`); spec it briefly but **do not implement it today** — it becomes the next BUILD plan, scored separately, freeze-lift-gated.

Then update the trackers:
- **`docs/ROADMAP.md`** — update the C2 line to note the population run completed; set C3 to either DEFERRED-confirmed or RE-OPENED (with the failing case). If C3 stays deferred, add a one-line note that Track C's measurement objective is complete and the next roadmap pick is **D1 (tool-calling spike)** per the sequencing list.
- **`docs/changes/<date>-day19-conversation-script-population.md`** — standard change doc: what was run, the matrix result, the C3 decision, freeze-gate-empty confirmation, tests/build status, and any carried-forward cells if the run was bounded.
- **`docs/decisions.md`** — append the one-line `tried/expected/actual` entry (Hard Rule 3).

## F. Testing plan

This is primarily a **run**, not a build, so most "testing" is verifying the run executed cleanly and the harness behaved:

- **No new source code is expected.** If the matrix run surfaces a genuine harness bug (e.g. a crash on the thoughtrecord/checkin path that the deterministic mock tests didn't catch), fix it in the **new** driver files only, add a deterministic regression test, and keep the freeze gate empty. Note any such fix prominently — it changes the deliverable from "run" to "run + fix."
- **Full suite green:** `npm run test` must stay green (1150/1150 baseline). If a harness fix is made, add to the count.
- **Build clean:** `npm run build` (TS strict).
- **Freeze gate empty:** `git diff origin/main -- src/utils/evalRunner.ts src/utils/evalScorer.ts src/prompts/` → must print nothing. Also confirm `tokenEstimator.ts` and `conversationContext.ts` are untouched.
- **Sanity-check the run output:** the `summary.json` `scripts` block has one entry per script×strategy cell actually run; `conversation-scripts.md` rendered without error; probe counts are integers in `[0, probes]`.

## G. Deliverables (checklist)

- [ ] `docs/eval-runs/<UTC-date>/conversation-scripts.md` — full (or bounded) matrix output.
- [ ] `docs/eval-runs/<UTC-date>/summary.json` — with the `scripts` block.
- [ ] `docs/eval-runs/2026-06-20/C2-findings.md` — **preserved unchanged** (historical n=1 record).
- [ ] `docs/eval-runs/<UTC-date>/C2-population-findings.md` — new analysis + the C3 decision.
- [ ] `docs/changes/<date>-day19-conversation-script-population.md` — change doc.
- [ ] `docs/ROADMAP.md` — C2 line updated; C3 set to DEFERRED-confirmed or RE-OPENED; next-pick note.
- [ ] `docs/decisions.md` — one-line tried/expected/actual entry appended.
- [ ] `npm run test` green; `npm run build` clean; freeze gate confirmed empty.
- [ ] PR to `main` (BUILD phase allows it). Per standing feedback, include screenshots **only if** any UI changed — this run is non-UI, so a screenshot is not required here; the eval-run markdown tables are the evidence. (Note: this is the rare plan where the "PR screenshots" rule doesn't apply because nothing user-visible changes. State that explicitly in the PR description.)
- [ ] One ntfy notification (handled by the execution task's own flow).

## H. Roadmap tracker

| Track / Problem | Status after this plan |
|---|---|
| #1 Guided modes vanish | FIXED (PR #40) |
| #2 Context window (long-conversation) | **Measurement completing** — C1+C2 done; this run gives the population that lets us declare it closed-at-scale (or re-open C3) |
| #3 Model quality | MANAGED by the eval loop |
| #4 Tech-demo feel (Track A) | A1–A5 DONE; A6 filler remains |
| Track B (input robustness) | B1, B2 DONE; B3 piggybacks future UI day |
| Track C (long-conversation eval) | C1 DONE, C2 DONE, **C3 decided by this run** (deferred-confirmed or re-opened) |
| Track D (tool-calling spike) | Not started — **becomes the next pick** if C3 stays deferred (Track C measurement complete) |

### Notes / autonomous choices made by the planner

- **Critic-loop staleness:** `docs/critic-reports/` stops at 2026-06-07. Per PHASE.md rule #2 a >36h-stale critic report would normally force a "diagnose the critic loop" plan. However, the BUILD phase has run the eval→tune loop via `docs/eval-runs/` (not critic reports) continuously and intentionally since 06-07 — 12 days of roadmap-driven plans (A1–A5, B1–B2, C1–C2) confirm this is the established operating mode, not a broken loop. I am therefore proceeding with the roadmap pick rather than a diagnosis plan, but flagging the critic-report gap here so it is on the record. If the execution task or owner wants the formal critic loop revived, that is a separate, reasonable follow-up.
- **Why this item and not D1:** the roadmap sequencing is "C1 → C2 → C3 … then D1." C3 is *gated on data this run produces*. Running the population matrix is the precondition for both closing Track C and cleanly moving to D1; the C2 findings name it as the explicit next step. Doing D1 first would leave Problem #2's population claim and the thoughtrecord-coherence hole open.
