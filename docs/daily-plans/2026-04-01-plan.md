# Daily Plan — 2026-04-01

## Context
Phase 1 safety/correctness fixes (PR #19) merged to main. Today focuses on Phase 2 from the 2026-03-31 UX evaluation: accessibility improvements.

## Priorities

### 1. Add dialog roles and ARIA attributes to all modals
- Add `role="dialog"`, `aria-modal="true"`, `aria-labelledby` to MoodTracker, PrivacyDashboard, CrisisResources
- Add `aria-label` to close buttons
- Add Escape key handling to all three modals

### 2. Add focus trap to modals
- Create reusable `useFocusTrap` hook
- Trap Tab/Shift+Tab within modal when open
- Restore focus to trigger element on close

### 3. Increase touch targets to 44px minimum (WCAG 2.5.5)
- Header Mood/Privacy buttons: increase from ~36px to 44px
- Modal close (X) buttons: increase to 44px minimum
- MoodSuggestionCard edit/dismiss buttons: increase from ~20px to 44px

### 4. Add click-outside and Escape close for PromptSelector
- Add `mousedown` outside-click handler
- Add Escape key handler
- Fix `w-96` overflow on mobile (use responsive max-width)

### 5. Fix MoodTracker reset on close without save
- Call `handleReset` when `isOpen` transitions to false

## One-Week Thread

| Date | Focus |
|------|-------|
| 2026-03-26 | Error handling for model load failure |
| 2026-03-27 | Fix modal close + input loss bugs |
| 2026-03-28 | Humanize loading screen + welcome animations |
| 2026-03-29 | Header subtitle + mobile sessions + interactive prompt link |
| 2026-03-30 | Humanize footer and placeholder text |
| 2026-03-31 | Phase 1 safety & correctness fixes |
| **2026-04-01** | **Phase 2 accessibility improvements** |
