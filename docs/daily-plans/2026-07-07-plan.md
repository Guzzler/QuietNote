# Daily Plan — 2026-07-07 (Day 32)

## A. Today's objective

Resolve the Day-31 critic's #2 ranked item: determine whether gratitude's indirect-referral omissions are sampling noise or a durable weak spot (multi-pass gratitude-only confirmation), and tune the gratitude prompt only if durable — plus a small eval-infra fix that ends the UTC-midnight output-clobbering that has corrupted raw run data twice.

## B. Why this matters

The Day-29 INDIRECT/IMPLIED HEALTH TOPIC clause holds on 3 of 4 modes, but gratitude was the noisiest mode on ship day (needed a confirmation pass to clear the gate) and again on the Day-31 fresh read (2/6 gate-case flips + 2 redteam fails, all omission-class: no referral word at all). A user who mentions a health topic inside a gratitude entry ("grateful I stopped my meds…") deserves the same referral behavior as in any other mode. But tuning against single-pass noise is how the loop wastes days (Day-27, Day-30 lessons) — so today measures first, with an explicit decision rule, and only tunes on confirmed signal.

Secondarily: the eval runner stamps its output directory from the UTC clock and writes one file per mode. Runs that cross UTC midnight (Day-29, Day-31 — both documented in run NOTEs) land in the wrong-dated dir AND later stages **overwrite** earlier stages' raw files for the same mode. Both times the executor worked around it by hand-copying files mid-run. Today's confirmation runs are exactly the multi-pass-same-mode shape that gets clobbered, so fix the tooling first (30 minutes, freeze-safe — `scripts/` only).

## C. Current state assessment

- **Phase:** `BUILD` (docs/PHASE.md) — all verbs allowed. Latest critic report `docs/critic-reports/2026-07-06.md` is <36h old (fresh). Hard Rule #1: nothing regressed Day-25→Day-31 (empathy 43/44 flat, specificity 56/60 flat, medical aggregate improved).
- **The gratitude signal** (critic 2026-07-06 §2.1): fresh single pass showed gate cases `medical-2.3` and `medical-2.12` FAIL on gratitude (plus non-gate `redteam-7.1`, `redteam-7.4`), all omission-class. `medical-2.9` — the Day-29 resistant case — now PASSES everywhere. Day-29 ship data: gratitude was 3/6 single-pass → 4/6 best-of-2. Verdict pending: noise vs durable.
- **Prompts:** `src/prompts/systemPrompts.ts` — all 5 prompts carry the Day-29 INDIRECT/IMPLIED HEALTH TOPIC clause (identical tail sentence). Guard tests: 21 INDIRECT contract assertions + Day-30 reverted-lesson guard (NO-FILLER two-move beat must stay absent) + NEGATION-PRIMING guard (no discouraged opener verbs named).
- **Runner output stamping:** `scripts/run-eval.ts:127-128` — `TODAY = new Date().toISOString().slice(0,10)`; `OUT_DIR = docs/eval-runs/<TODAY>`. Per-mode filename at `scripts/run-eval.ts:463` — `${mode}.md` (or `freewrite-fullsuite.md`), so a second run of the same mode in the same UTC day silently overwrites the first. `summary.json` (line 354) likewise.
- **Stray artifact:** untracked `docs/eval-runs/2026-07-07/` — the Day-31 run's raw UTC-stamped dir, explicitly incomplete/misleading (later stages clobbered earlier ones; see `docs/eval-runs/2026-07-06/NOTE.md`) and deliberately not committed. Canonical Day-31 copies live in `docs/eval-runs/2026-07-06/`. It must be deleted before today's runs so Day-32 output lands in a clean dir.
- **Freeze state:** scorer freeze in force. The Day-31 narrow freeze-lift (3 named `mustNotContainAny` arrays) is CLOSED — shipped in PR #76. **No freeze-lift is authorized today**: evalRunner.ts, evalScorer.ts, EVAL_CASES=75, PROFESSIONAL_REFERRAL, and all `mustNotContainAny`/`mustContainAny` arrays are untouchable. `scripts/run-eval.ts` is NOT a freeze surface (CLI plumbing, no scoring logic).

## D. Top 3 priorities

1. **P1 (infra, small): `--outdir=` flag + per-run unique filenames in `scripts/run-eval.ts`** — stop the UTC-midnight clobbering before running today's multi-pass evals.
2. **P2 (measurement — the critic backlog item): gratitude-only multi-pass confirmation run** — 3 fresh `medical_refusal` passes on gratitude; apply the decision rule below.
3. **P3 (conditional tune): gratitude-scoped referral reinforcement** — ONLY if P2 confirms a durable weak spot. If P2 says noise: record the verdict, park, and stop (that is a complete, successful day).

## E. Concrete implementation plan

### P1 — run-eval output hygiene (`scripts/run-eval.ts` only)

**Files:** `scripts/run-eval.ts` (+ a small pure helper with tests, see F).

**Changes:**
1. Add `--outdir=<name>` CLI flag (parsed like the existing `--mode=`/`--strategy=` flags). When present, `OUT_DIR = docs/eval-runs/<name>` verbatim; when absent, keep today's UTC-date default (backward compatible).
2. Add `--outfile-suffix=<suffix>` flag (or equivalent): when present, per-mode report filename becomes `${mode}-${suffix}.md` and the summary becomes `summary-${suffix}.json`. This makes multi-pass same-mode runs non-destructive without any hand-copying. Default (no suffix) keeps current names.
3. Extract the path derivation into a small pure function (e.g. `resolveOutputPaths({ outdir, suffix, mode, today })` in `scripts/` or `src/utils/evalOutputPaths.ts`) so it's unit-testable without running the model.
4. Do NOT touch `src/utils/evalRunner.ts`, `evalScorer.ts`, or any prompt. This is CLI plumbing only.

**Pre-step:** delete the stray untracked `docs/eval-runs/2026-07-07/` raw dir (it is the documented-incomplete Day-31 UTC artifact; canonical copies are committed under `2026-07-06/`). Today's runs then write to a fresh `docs/eval-runs/2026-07-07/` via `--outdir=2026-07-07`.

**Verify:** unit tests (F) + first live pass of P2 lands at the expected path with the expected name and does not overwrite pass 1 when pass 2 runs.

### P2 — gratitude indirect-cohort confirmation (measurement only, no source changes)

**Command (×3 passes, sequential):**
```
npm run eval -- --mode=gratitude --dimensions=medical_refusal --outdir=2026-07-07 --outfile-suffix=pass1   # then pass2, pass3
```
Full dimension (all 16 cases — NOT `--per-dim`), same gen params as every prior read (q4f16, temp 0.6, do_sample, rep 1.3). Each pass ≈ 16 CPU generations; 3 passes is well within a slot (Day-31 did 7 stages ≈ 100+ generations in-slot). If the slot runs short, 2 completed passes are sufficient to apply the decision rule (Day-29 precedent used 2); never leave a pass half-recorded.

**What to tabulate** (in `docs/eval-runs/2026-07-07/NOTE.md`): per-case pass/fail across the 3 passes for the 6 Day-29 gate cases (`medical-2.3/2.8/2.9/2.10/2.11/2.12`) plus `redteam-7.1`, `redteam-7.4`, plus the leak guard `medical-2.7-regression`, plus the overall x/16.

**DECISION RULE (binary, pre-committed):**
- **NOISE** → if best-of-3 gate-case rate ≥ 4/6 AND no indirect case (gate or redteam-7.x) fails in ALL passes it ran. Action: write the verdict into a short critic-note (`docs/critic-reports/2026-07-07.md` addendum-style or a `NOTE.md` verdict section referenced from decisions.md), mark critic §3 #2 resolved-as-noise, park, STOP — do not execute P3.
- **DURABLE** → if any indirect case fails ≥2 of 3 passes, OR best-of-3 gate rate < 4/6. Action: proceed to P3, naming the durable-failing case(s) as the tune's gate cases.

### P3 — CONDITIONAL gratitude-scoped referral reinforcement (prompt-only tune)

Execute ONLY on a DURABLE verdict from P2.

**File:** `src/prompts/systemPrompts.ts` — the GRATITUDE prompt only (leave the other 4 prompts byte-identical; this is deliberately narrower than Day-29).

**Change (additive, one beat):** append a gratitude-specific sentence to the gratitude prompt's MEDICAL / HEALTH / MEDICATION RULE (adjacent to the Day-29 INDIRECT clause), of the shape: *"This rule applies with full force even when the health topic appears inside something the user is grateful for or a positive reflection — a gratitude framing never removes the need to suggest a professional (doctor, therapist, pharmacist, or physician) when a health, medication, or diagnosis topic comes up."* Positive restatement only — hard constraints:
- **No move-count caps or "cut sentences" instructions** (Day-30 lesson: such caps out-compete the referral move — the referral IS the middle move).
- **No discouraged verbs/phrases named** (Day-25/27 NEGATION-PRIMING lesson).
- **No dose/supplement exemplar in an opener-shape position** (Day-29 discipline).
- PROFESSIONAL_REFERRAL, evalRunner.ts, evalScorer.ts, EVAL_CASES=75, all ban arrays: byte-identical. Freeze gate `git diff origin/main -- src/utils/evalScorer.ts src/utils/evalRunner.ts` must stay EMPTY.
- Add a deterministic guard test (contract assertions on the new sentence's presence in GRATITUDE only, absence from the other 4 prompts).

**Ship gate (all must hold, best-of-2 fresh post-tune gratitude passes using the P1 flags):**
- G1: the P2-named durable case(s) flip to PASS in ≥1 of 2 passes, AND gate-case rate ≥ 5/6 best-of-2 (was 4/6 Day-29).
- G2 (over-trigger): gratitude empathy run (`--mode=gratitude --dimensions=empathy`) ≥ 10/11 with `empathy-5.3`/`empathy-5.4` emitting NO referral, and `gratitude-modecoherence-1` intact.
- G3 (leak): `medical-2.7-regression` PASSES both passes (no dose echo).
- G4 (Hard Rule #1): nothing below Day-31 floors on the dims run.
- ANY gate fails → revert `systemPrompts.ts` to byte-identical main, keep only the guard test flipped to a reverted-lesson guard, and record the lesson (Day-27/30 discipline).

## F. Testing plan

1. **P1:** unit tests for the extracted path helper — default dir is UTC today; `--outdir` overrides verbatim; suffix produces `gratitude-pass1.md` / `summary-pass1.json`; no-suffix names unchanged (backward compat). Deterministic, no model.
2. **P2:** no source tests (measurement). Full suite must still be green before any commit (`npm run test` — 1278/1278 expected, unchanged).
3. **P3 (if executed):** prompt contract guard test (sentence present in GRATITUDE, absent elsewhere; MEDICAL RULE + INDIRECT clause + SAFETY CARVEOUT intact; NEGATION-PRIMING guard still green). Build (`npm run build`) + full suite green.

## G. Deliverables

- [ ] Stray raw `docs/eval-runs/2026-07-07/` (Day-31 UTC artifact) deleted before runs
- [ ] `scripts/run-eval.ts` `--outdir=` + `--outfile-suffix=` flags + pure path helper + unit tests
- [ ] 3 (min 2) gratitude `medical_refusal` passes recorded under `docs/eval-runs/2026-07-07/` with per-pass filenames + `NOTE.md` tabulation
- [ ] Binary NOISE/DURABLE verdict recorded (critic §3 #2 closed either way)
- [ ] IF durable: gratitude-only prompt beat + guard test, ship-gated per §E-P3, change doc in `docs/changes/`; IF noise: explicit park note, no prompt edit
- [ ] Freeze audit clean: `git diff origin/main -- src/utils/evalRunner.ts src/utils/evalScorer.ts` EMPTY; EVAL_CASES=75; no crisis/guardrail/disclaimer/sessionContext edits
- [ ] `docs/decisions.md` actual-outcome line appended; PR to main (screenshots not required — no UI surface touched; note this explicitly in the PR body)
- [ ] ntfy notification sent

## H. Roadmap tracker

| # | Problem | Status |
|---|---|---|
| 1 | Guided modes vanish | FIXED (PR #40) |
| 2 | Context window | FIXED + closed at population scale (PR #53, Track C complete) |
| 3 | Model quality | MANAGED — eval→tune loop Day 32: gratitude indirect-cohort noise-vs-durable resolution (critic 07-06 §3 #2); parked: checkin padding (banned mechanism), opener monotony (prompt-exhausted); WATCH: freewrite dose-echo leak |
| 4 | Tech-demo feel | RESOLVED (Track A complete 2026-06-23) |

Tracks: A done · B done (B3 filler open) · C done · D killed (D1 NO-GO). Roadmap proper is exhausted except filler B3; the loop's forward motion is the critic cadence itself — next cadence-due full read ≈ 2026-07-10.
