# Daily Plan — 2026-06-07

> **Phase: `EVAL` (counter 6 of 7 → 7 of 7 after today). THIS IS THE EVAL PHASE-EXIT CHECK.** Day 6 ([`docs/critic-reports/2026-06-06.md`](../critic-reports/2026-06-06.md), < 36h old → [PHASE.md](../PHASE.md) Hard Rule #2 satisfied; **not** a re-diagnosis day) closed the loop's last open EVAL thread: it took the THIRD independent sample of the gratitude crisis-deflection (3/3 curt-bail re-fire on `redteam-7.2` in gratitude — **stable, not stochastic**), corrected the Day-4/5 "over-trigger" mischaracterization with source evidence (the deflection text is **model-generated** — `grep` finds it nowhere in `src/`; `scripts/run-eval.ts` never imports `crisisDetection`/`responseGuardrails`), and re-framed the defect as a **gratitude-mode-specific UNDER-engagement** (curt bail skips the pain-acknowledgement + engaging question). It also confirmed `empathy-mt-3` holds settled (experience-FAIL 4/4 modes, 4th independent sample) and wrote the **Phase-readiness BUILD backlog** (2 confirmed `feat:`s). The Day-6 critic's explicit "target next (Day 7)": *"confirm the baseline holds and flip the phase to BUILD … Day-7 should be a clean full-suite run that simply confirms the baseline … Do not re-sample `empathy-mt-3` and do not re-sample the gratitude curt-bail — both are settled across 3+ independent samples."*
>
> **Today is a decision day, not a discovery day.** The two live failures are settled and queued for BUILD. Today's job is the formal **EVAL phase-exit gate**: run one clean cross-dimensional baseline confirmation, verify the north-star baseline is *sustained* (Hard Rule #1 — no regression vs the start-of-cycle baseline), and — IF and ONLY IF the baseline holds — **flip `docs/PHASE.md` from `EVAL` to `BUILD`** so the first BUILD slot can start the queued `feat:` work. No prompt edits. No scorer edits. No `feat:` implementation. One baseline run, one honest exit decision, the counter to 7/7, the phase flip, and a clean BUILD hand-off.

## A. Today's Objective

**Run a clean cross-dimensional baseline-confirmation eval (`npm run eval -- --per-dim=2`, full suite across all dimensions, same gen params q4f16 / max_new_tokens 200 / temp 0.6 / rep_penalty 1.3) to confirm the north-star baseline that was established on 2026-06-02/03 is still sustained across all four modes and all scored dimensions (specificity, warmth, mode_coherence, guardrail_appropriateness, would_return) — NOT another empathy-only slice, because the last five days narrowed onto the empathy/multi-turn front and the exit check must re-verify the WHOLE baseline, not just one dimension. Harvest into `docs/critic-reports/2026-06-07.md`, advance the EVAL counter to 7/7, append 4 north-star rows, append the decisions-log `actual:`. Then make the binary EVAL-exit decision per [`docs/PHASE.md`](../PHASE.md) Hard Rule #1 + the EVAL exit criterion ("7 consecutive days of critic reports with a sustained north-star baseline"): IF the baseline holds (overall not lower than the cycle-start baseline; no green→red dimension collapse), **flip `docs/PHASE.md` Current phase from `EVAL` to `BUILD` and append a phase-transition line**, and carry the already-characterized BUILD backlog (priority 1: prior-turn entity surfacing for `empathy-mt-3`; priority 2: gratitude-mode distress carve-out) into the first BUILD plan. IF the baseline regressed, DO NOT flip — stay `EVAL`, record the regression as the new weakest finding, and the next slot diagnoses it. Do NOT re-sample `empathy-mt-3`. Do NOT re-sample the gratitude curt-bail. Do NOT add a prompt sentence. Do NOT touch the scorer. Do NOT implement either queued `feat:`.**

## B. Why This Matters

1. **This is the day the loop converges.** Per [[feedback-eval-tune-combined]] the 7-day EVAL cycle exists to *converge on a trustworthy baseline before building*. Six days of real Node-runner critic data (06-02 → 06-06) have characterized exactly two load-bearing defects and proven everything else is strong. Day 7 is the gate that lets the loop stop measuring and start fixing — but only if the baseline genuinely holds. Flipping to BUILD on a regressed baseline would violate the loop's whole purpose.

2. **The exit check must re-verify the WHOLE baseline, not just empathy.** Days 4–6 ran `--dimensions=empathy --per-dim=11` to chase the multi-turn and gratitude-deflection threads. That deliberately did NOT refresh specificity / warmth / mode_coherence / guardrail_appropriateness across the non-empathy cases. The last full cross-dimensional read was 06-02/06-03 (`--per-dim=2`, all dimensions: overall 4 across modes, with gratitude would_return dipping to 3). Before declaring the baseline "sustained" and flipping to BUILD, today must confirm those other dimensions did not silently rot while attention was on empathy. A `--per-dim=2` full-suite run is the apples-to-apples comparator for the 06-02/06-03 baseline.

3. **Hard Rule #1 is a merge gate — it must be evaluated with real data, not asserted.** "No PR may merge if the previous critic-day north-star score was lower than the score from 7 days ago." The loop's recurring failure mode (per every prior decisions entry) is *asserting* a result it never *measured*. The phase flip is the highest-stakes assertion in the cycle. It gets one clean measurement behind it before it stands.

4. **A clean hand-off prevents the first BUILD slot from re-deriving the backlog.** The Day-6 report already wrote the BUILD backlog with evidence pointers. Today's job is to carry it forward verbatim into the exit decision, so the first BUILD plan can open with "implement priority 1" instead of re-running six days of diagnosis.

5. **EVAL-legal and freeze-safe.** Today is one full-suite re-run + docs + a one-line governance edit to `PHASE.md` (a docs/governance file, explicitly editable by the phase owner; flipping it is the *point* of the exit check, not a source change). The source freeze gate — `git diff origin/main -- src/utils/evalRunner.ts src/prompts/systemPrompts.ts` — stays **empty**. No new cases, no new dimensions, `EVAL_CASES.length` unchanged. No `feat:` code.

## C. Current State Assessment

### What landed Day 6 (executed; see [`docs/critic-reports/2026-06-06.md`](../critic-reports/2026-06-06.md))
- **Day-6 critic report** — gratitude crisis-deflection **re-framed** (model-generated, mode-specific, under-engagement — NOT an over-trigger); 3/3 gratitude curt-bail tally on `redteam-7.2`; `empathy-mt-3` settled (4th sample, experience-FAIL 4/4); **Phase-readiness BUILD backlog** written; conditional `feat:` sketch TRIGGERED and written (gratitude-mode distress carve-out, BLOCKED until BUILD); EVAL counter = 6/7.
- `docs/eval-runs/2026-06-06/{freewrite-fullsuite,gratitude,checkin,thoughtrecord}.md` + `summary.json` — third-sample empathy slice. Aggregate 38/44 (86%), matching Day-5.
- 4 north-star rows dated 2026-06-06 (expected — appended by the Day-6 execution; if not yet present because the Day-6 PR is still open at run time, that is fine, today appends 2026-06-07 rows regardless).
- Day-6 decisions entry; change note `docs/changes/2026-06-06-day6-third-sample-and-reframe.md`.

### The north-star baseline to confirm against (from `docs/north-star.csv`)
The cross-dimensional baseline was established on the last full-suite runs:
- **2026-06-02** (`--per-dim=2`, all dims): freewrite/gratitude overall **4**, checkin/thoughtrecord overall **4**; specificity 3–4; guardrail_appropriateness **5** across all modes.
- **2026-06-03** (empathy `--per-dim=11`): freewrite overall 4; gratitude overall 3 (would_return dipped 4→3); checkin 4; thoughtrecord 3.
- **2026-06-04/05/06** (empathy slices): gratitude would_return held at 2–3 and guardrail_appropriateness 4 (the gratitude under-engagement); other modes held overall 3–4; multi_turn_memory settled at **3/3/3/3**.

**The sustained baseline = overall 3–4 across modes, guardrail_appropriateness 4–5, multi_turn_memory 3, specificity 3–4, warmth/mode_coherence 4.** The exit check passes if today's `--per-dim=2` run reproduces this envelope with no dimension collapsing (e.g. guardrail dropping below 4, or any mode's overall falling to ≤2).

### The two settled failures — do NOT re-open (confirmed across 3–4 independent samples)
1. **`empathy-mt-3`** — prior-turn entity-drop, experience-FAIL 4/4 modes, Days 3/4/5/6. Structural `feat:` (prior-turn entity surfacing) is the **confirmed primary BUILD action**, BLOCKED until phase=BUILD. **No third+ sample today** — `--per-dim=2` does not even reach the `empathy-mt-*` cases (they sit at empathy positions 7–10), which is correct: the exit check is a baseline confirmation, not a multi-turn re-litigation.
2. **Gratitude-mode distress under-engagement** (`redteam-7.2` / `gratitude-modecoherence-1`) — 3/3 gratitude curt-bail, model-generated, under-engagement. Carve-out `feat:` sketched (Day-6), **secondary BUILD action**, BLOCKED until phase=BUILD. **No re-sample today.** Note: `redteam-7.2` IS a `--per-dim=2` case (it is `dimension: "empathy"` but also a redteam case sampled in the per-dim=2 slice depending on pool ordering); if it incidentally appears in the gratitude run, record its verdict in one line for completeness but do NOT expand it into a re-litigation — it is settled.

### Governance — the gate this plan operates
- [`docs/PHASE.md`](../PHASE.md) — Current phase `EVAL`, set 2026-05-25. EVAL allows `eval:`/`chore:`/`fix:`(critic-flagged)/`docs:`; forbids `feat:`/large refactors. **EVAL exit criterion: "7 consecutive days of critic reports with a sustained north-star score baseline (no required score floor — we're learning what the baseline is)."** After today's report the counter is 7/7 → criterion met IF baseline sustained. Hard Rule #1 (regression blocks). Phase-transition lines are appended at the bottom of PHASE.md, newest at top.
- **Harness-expansion freeze (2026-05-30) holds.** No new cases/dimensions. `EVAL_CASES.length` unchanged.
- **Both queued `feat:`s stay BLOCKED until the flip lands.** The flip is what *unblocks* them — but the flip is a governance edit, not their implementation. Today writes zero `feat:` code.

## D. Top 3 Priorities for the Execution Task

### Priority 1 — Run the cross-dimensional baseline-confirmation eval (`eval:`) — DO THIS FIRST
```bash
npm run eval -- --per-dim=2
```
Full suite, **all dimensions** (not `--dimensions=empathy`). Writes `docs/eval-runs/2026-06-07/` (**watch the UTC date-rollover quirk** — the runner may label the directory `2026-06-08`; if so, rename it to `2026-06-07` to match this plan, exactly as Days 5 and 6 did). Gen params **must stay identical**: `q4f16 / max_new_tokens 200 / temp 0.6 / rep_penalty 1.3`.

**Reality gate:** confirm the run produced real Gemma text (not error strings) across all 4 modes and that the per-dimension cases that fed the 06-02/06-03 baseline (medical-2.x, jailbreak-3.x, specificity-8.x, redteam-7.x, empathy-5.x) are present in the run output. Then:
- **Score each north-star dimension per mode** from the real bodies, the same way 06-02/06-03 did. Produce the per-mode overall.
- **Baseline comparison table:** today vs 06-02 vs 06-03 for each mode's overall + guardrail_appropriateness + specificity. Call out any dimension that dropped.
- **`mt-3` / gratitude-bail:** one line each — NOT re-sampled (out of `--per-dim=2` scope or settled); status carried forward from Day 6. Do not expand.

### Priority 2 — Make the binary EVAL-exit decision + (conditionally) flip the phase (`docs:`)
Evaluate the exit gate against the run:
- **Baseline SUSTAINED** = (a) no mode's overall fell to ≤2; (b) guardrail_appropriateness ≥4 in all modes; (c) no dimension that was ≥4 on 06-02 collapsed to ≤2; (d) the only weak signals are the two already-settled-and-queued failures.
- **IF SUSTAINED → FLIP THE PHASE.** Edit [`docs/PHASE.md`](../PHASE.md):
  1. Change `## Current phase: ` + the line `## Current phase: \`EVAL\`` to `## Current phase: \`BUILD\``.
  2. Append a phase-transition line at the bottom (newest at top): `- 2026-06-07 → \`BUILD\` (EVAL exit: 7 critic reports 05-28…06-07, baseline sustained at overall 3–4 / guardrail 4–5 / multi_turn_memory 3; two confirmed BUILD feat:s queued — prior-turn entity surfacing (primary), gratitude distress carve-out (secondary))`.
  3. Do NOT alter the phase-table or hard-rules sections.
- **IF REGRESSED → DO NOT FLIP.** Leave PHASE.md `EVAL`. Record the regression as the Day-7 weakest finding, quote the regressing bodies, and note the next slot is a diagnosis of the regression (not a phase flip). The counter still reads 7/7 but the exit criterion ("sustained baseline") is NOT met, so the cycle extends.

### Priority 3 — Harvest the Day-7 critic report + north-star + decisions + change note + BUILD hand-off (`docs:`)
Write `docs/critic-reports/2026-06-07.md`:
1. **Header:** Day 7 / 7 — EVAL EXIT CHECK; backend (Transformers.js Gemma 4 E2B, Node onnxruntime-node, q4f16); commit (pre-run main HEAD); gen params (identical); slice = `--per-dim=2` full suite; source diff = **none**; freeze gate empty.
2. **Reality gate** + **Aggregate table** (today vs 06-02/06-03/06-06 totals).
3. **Baseline-sustained verdict** (the centerpiece): the per-dimension comparison table and the explicit binary call (SUSTAINED → flip / REGRESSED → hold), with evidence.
4. **The two settled failures:** one paragraph each — carried forward, queued for BUILD, NOT re-sampled today. Reference the Day-6 BUILD backlog rather than re-deriving it.
5. **EVAL counter = 7/7.** State whether the exit criterion is met and whether PHASE.md was flipped.
6. **BUILD backlog (carried from Day 6, verbatim with pointers):** priority 1 prior-turn entity surfacing (`empathy-mt-3`; mechanism candidates: context-summary injection OR chat-template restructuring); priority 2 gratitude-mode distress carve-out (acknowledge-then-redirect; candidates: gratitude-prompt distress branch OR response-shaping post-step; non-goals: do NOT weaken `crisisDetection.ts`, do NOT drop the AI-limitations disclaimer). This is the input to the first BUILD plan.

Then:
- **`docs/north-star.csv`** — append 4 rows dated `2026-06-07` from the real run. Hold multi_turn_memory at 3/3/3/3 (settled, not sampled — carry forward, note in report). Score the other dimensions from today's actual bodies.
- **`docs/decisions.md`** — append a `2026-06-07` entry (newest at top) per the template at the bottom; set `actual:` after the run. Set the Day-6 entry's open thread (the phase-exit decision it queued) as resolved here.
- **`docs/changes/2026-06-07-*.md`** — the baseline-confirmation result + the exit decision + the phase flip (or hold).

## E. Concrete Implementation Plan

### E.1 — Priority 1 (baseline run, ZERO source edits)
- `npm run eval -- --per-dim=2` → `docs/eval-runs/2026-06-07/` (rename from a UTC-rollover mislabel if needed). Confirm real text across all 4 modes.
- Build the per-mode, per-dimension score table from the actual bodies. Compute each mode's overall. Build the today-vs-06-02-vs-06-03 comparison.

### E.2 — Priority 2 (exit decision + conditional PHASE.md flip)
- Apply the SUSTAINED test (D-Priority-2 (a)–(d)). Write the binary verdict into the report.
- IF sustained: edit `docs/PHASE.md` (Current phase → `BUILD`; append transition line). IF regressed: leave it `EVAL` and document the regression.

### E.3 — Priority 3 (docs only)
- Critic report sections (mandatory): header + freeze-gate-empty note; reality gate; aggregate + baseline-comparison tables; **baseline-sustained verdict + flip/hold call**; the two settled failures carried forward; counter = 7/7 + exit-criterion statement; **BUILD backlog carried verbatim**.
- North-star: 4 rows dated 2026-06-07 (multi_turn_memory held 3/3/3/3; other dims from real bodies).
- Decisions `actual:` must state, in one place: (a) the baseline-comparison result (sustained or regressed, with the per-mode overalls), (b) whether PHASE.md was flipped to BUILD, (c) the counter = 7/7, (d) the carried BUILD backlog (2 feat:s, priority order).

## F. Testing Plan

1. **No build/test required for source** — today makes **zero source edits**. (`npm run build` / `npm run test` optional sanity only; nothing in `src/` changed.)
2. **Freeze gate:** `git diff origin/main -- src/utils/evalRunner.ts src/prompts/systemPrompts.ts` → **empty** (trivially — no source touched). A non-empty diff = scope creep — stop.
3. **Reality gate:** the `--per-dim=2` run produced real Gemma bodies (not error strings) in all 4 modes; the baseline-feeding cases (medical/jailbreak/specificity/redteam/empathy-5) are present.
4. **Exit-decision gate:** the report contains an explicit, binary statement — baseline SUSTAINED (→ PHASE.md flipped to BUILD, transition line appended) OR REGRESSED (→ PHASE.md held EVAL, regression documented) — backed by the today-vs-06-02/03 comparison table.
5. **Phase-flip gate (only if sustained):** `docs/PHASE.md` Current phase reads `BUILD` and a `2026-06-07 → BUILD` transition line is present. Verify the phase table and hard rules sections were NOT altered.
6. **No-re-sample gate:** the report does NOT contain a new `empathy-mt-3` sample or a new gratitude curt-bail sample — both are carried forward from Day 6 by reference, not re-measured.

## G. Deliverables Checklist

- [ ] `docs/eval-runs/2026-06-07/` (full-suite `--per-dim=2`, all dimensions; directory renamed from any UTC-rollover mislabel; real bodies confirmed in all 4 modes)
- [ ] `docs/critic-reports/2026-06-07.md` — Day-7 EVAL EXIT report: baseline-comparison table; **binary sustained/regressed verdict**; the two settled failures carried forward; counter = 7/7 + exit-criterion statement; **BUILD backlog carried verbatim**
- [ ] `docs/PHASE.md` — **flipped to `BUILD`** with a `2026-06-07 → BUILD` transition line **IF** baseline sustained (else held `EVAL` with the regression documented)
- [ ] `docs/north-star.csv` — 4 new rows dated 2026-06-07 (multi_turn_memory held 3/3/3/3; other dims from real bodies)
- [ ] `docs/decisions.md` — 2026-06-07 entry whose `actual:` records the baseline comparison, the flip/hold call, counter = 7/7, and the carried BUILD backlog
- [ ] `docs/changes/2026-06-07-*.md` — change note: baseline confirmation + exit decision + phase flip (or hold)
- [ ] PR against `main`: a single `eval:`/`docs:` commit. Suggested title (if sustained): `eval: Day-7 — EVAL exit check, baseline sustained, flip phase EVAL→BUILD`; (if regressed): `eval: Day-7 — baseline regression on <dim>, EVAL extended (no flip)`
- [ ] PR body: the baseline-comparison table, the binary exit verdict, the phase-flip statement, the carried BUILD backlog, and a screenshot per [[feedback-pr-screenshots]] (dev server, all 4 modes reachable, a normal journaling exchange that demonstrates the sustained baseline)

## H. Roadmap Tracker — Fundamental Problems

| # | Problem | Status |
|---|---------|--------|
| 1 | Guided modes vanish on first keystroke | **Done** 2026-05-24 — sticky header in `ChatPanel.tsx` |
| 2 | Tiny context window (2048) | **Bumped to 4096** 2026-05-24; continuity directive 2026-06-03 judged **insufficient** for the brief-emotional-follow-up shape across 4 samples (Days 4–6). Structural `feat:` (prior-turn entity surfacing) is the confirmed **primary BUILD action**, unblocked by today's phase flip. |
| 3 | Model response quality unevaluated | **Measured & baselined.** Single-turn strong (52/56). Two live multi-turn/mode fronts fully characterized and queued for BUILD: (a) `empathy-mt-3` entity-drop (settled, primary feat:); (b) gratitude-mode distress under-engagement (settled, secondary feat:). Day-7 confirms the cross-dimensional baseline holds. |
| 4 | App feels like a tech demo | Unblocked once the two queued BUILD feat:s land — both are direct usefulness/trust fixes ([[feedback-focus-usefulness]]). The phase flip is the gate that lets that work begin. |

**Meta-status:** Day 7 is the convergence gate of the 7-day EVAL cycle. The loop's recurring failure mode is *asserting* a result it never *measured* — and the phase flip is the cycle's highest-stakes assertion. So today measures the WHOLE baseline once (full-suite `--per-dim=2`, not another empathy slice), evaluates Hard Rule #1 against real data, and flips to BUILD only if the baseline genuinely holds. No source edits, no prompt edits, no scorer edits, no `feat:` implementation — just the clean exit decision and the BUILD hand-off the prior six days earned.

## Decision-log entry (append to `docs/decisions.md`, newest at top)

`2026-06-07 | tried: EVAL EXIT CHECK (counter 6→7/7). Run a clean cross-dimensional baseline-confirmation eval (npm run eval -- --per-dim=2, FULL suite all dimensions, NOT another empathy slice, same gen params q4f16/max200/temp0.6/rep1.3) to re-verify the north-star baseline established 06-02/03 is still sustained across all 4 modes and all scored dimensions (specificity/warmth/mode_coherence/guardrail_appropriateness/would_return) — because Days 4–6 narrowed onto empathy/multi-turn and the exit gate must confirm the WHOLE baseline didn't silently rot. Make the binary EVAL-exit call per PHASE.md Hard Rule #1 + exit criterion (7 critic reports + sustained baseline): IF sustained (no mode overall ≤2, guardrail ≥4 all modes, no ≥4 dimension collapsed to ≤2, only weak signals = the 2 already-queued failures) FLIP docs/PHASE.md Current phase EVAL→BUILD + append transition line, and carry the Day-6 BUILD backlog (priority 1: prior-turn entity surfacing for empathy-mt-3; priority 2: gratitude-mode distress carve-out) into the first BUILD plan; IF regressed DO NOT flip, document the regression, extend EVAL. NO re-sample of empathy-mt-3 (settled, 4 samples; also out of --per-dim=2 scope). NO re-sample of the gratitude curt-bail (settled, 3 samples). NO prompt edit, NO scorer edit, NO feat: code. Harvest into docs/critic-reports/2026-06-07.md + 4 north-star rows (multi_turn_memory held 3/3/3/3) + change note. ZERO source edits. | expected: real bodies in all 4 modes; baseline-comparison table (today vs 06-02/03); binary sustained/regressed verdict; PHASE.md flipped to BUILD (or held EVAL with regression documented); counter=7/7 + exit-criterion statement; BUILD backlog carried verbatim (2 feat:s, priority order); freeze gate (evalRunner.ts+systemPrompts.ts) empty (no source touched); EVAL_CASES.length unchanged | actual: pending`
