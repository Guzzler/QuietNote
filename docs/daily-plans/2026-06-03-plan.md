# Daily Plan — 2026-06-03

> **Phase: `EVAL`. This is the RE-MEASURE day — execute the re-run that 2026-06-02 deferred, then measure the one dimension we have never measured.**
> The latest committed critic report is [`docs/critic-reports/2026-06-02.md`](../critic-reports/2026-06-02.md) (Day 2, < 36h old → PHASE.md Hard Rule #2 is satisfied; this is **not** a re-diagnosis day). Yesterday harvested the first real model data since Day 1 and corrected three scorer false-positives **in code** ([`src/utils/evalRunner.ts`](../../src/utils/evalRunner.ts), commit `be5d482`). But **Priority 3 — re-running the corrected scorer — was deferred.** There is no `docs/eval-runs/2026-06-03/`, no `-corrected` slice, and the critic report's "Corrected pass-rates" section still reads *"Pending Priority 3 re-run."* The scorer fix is therefore **unvalidated against real output**: we changed the measuring instrument and never took the next measurement.
>
> **EVAL counter: 2 of 7 today; → 3 of 7 once today's real run is harvested into a critic report.** (Real-data days so far: 2026-05-28 Day 1, 2026-06-02 Day 2. 05-29/05-30/06-01 were tooling-only.)

## A. Today's Objective

**Take the next real measurement: re-run the corrected scorer on the same `--per-dim=2` slice to prove the three flagged cases flip green (the deferred 2026-06-02 Priority 3), AND run a dedicated empathy/multi-turn slice to finally measure `multi_turn_memory` — the genuine, never-measured weakness that the `--per-dim=2` sampling has silently skipped on every run. Harvest both into `docs/critic-reports/2026-06-03.md`, advance the EVAL counter to 3/7, then apply the ONE surgical improvement the *real* data demands — and only if the data demands it.**

## B. Why This Matters

1. **An unvalidated scorer fix is worse than no fix.** Yesterday we loosened three medical/jailbreak criteria and *asserted* the three overnight failures would flip to PASS, backed only by paired unit tests on synthetic strings. The unit tests prove the *logic* changed; they do **not** prove the *real model responses* now pass end-to-end (e.g. whether the live Thought-Record jailbreak response actually contains one of the broadened `mustContainAny` tokens). Until we re-run the model, the corrected pass-rates are a hypothesis. The cheapest, highest-value action today is the one yesterday skipped: re-run `--per-dim=2`, confirm `medical-2.2` (checkin + thoughtrecord) and `jailbreak-3.1`/`3.2` (thoughtrecord) go green, and confirm **no previously-green case went red** (the over-loosening guard).

2. **`multi_turn_memory` has been the named "real weakness" since Day 1 — and we have still never measured it.** Every run has used `--per-dim=2`, which takes the *first 2 cases per dimension*. The four multi-turn cases (`empathy-mt-1..4`, [evalRunner.ts:847–909](../../src/utils/evalRunner.ts#L847)) sit at **positions 7–10 of the `empathy` dimension** (six single-turn empathy cases precede them at lines 493/503/518/534/549/620). `--per-dim=2` reaches positions 1–2 only, so **the multi-turn cases have been zero-sampled on every single run.** The 4096-token context bump (Roadmap #2, shipped 2026-05-24) has **never** been validated against an actual multi-turn echo test. Two north-star cycles have carried `multi_turn_memory = 2` as a conservative *guess*, not a measurement. Today we measure it for real — the Node runner already injects `priorTurns` into the chat template ([evalDriver.ts:45–47](../../src/utils/evalDriver.ts#L45) → [run-eval.ts:92–93](../../scripts/run-eval.ts#L92)), so the wiring is sound; we simply have to *sample the cases*.

3. **The data must drive the tune, not the reverse.** Yesterday's hard-won lesson (per [[feedback-eval-tune-combined]]) was: do **not** tune the prompt against a broken scorer. The scorer is now corrected. So today, for the first time, a prompt-level improvement *is* legitimate — **but only against what the corrected, multi-turn-inclusive data actually shows.** If multi-turn reads weak (model ignores prior turns / asks generic questions), the surgical fix is a **continuity directive** in the system prompt. If it reads fine, the honest move is to record the baseline and tune nothing. The plan below makes the tune strictly conditional on the run output — the execution task reads the bodies first, then decides.

## C. Current State Assessment

### What landed yesterday (committed, `9fc9bde` / `be5d482`)
- `src/utils/evalRunner.ts` — scorer corrections 2a/2b/2c: `PROFESSIONAL_REFERRAL` constant (adds `clinician`/`provider`/etc.), assertive-only diagnosis bans replacing the bare noun, guided-mode-aware + quote-tolerant jailbreak `mustContainAny`. Paired tests in `src/utils/__tests__/evalScorerCorrections.test.ts` (green).
- `docs/critic-reports/2026-06-02.md` — Day 2 report; **"Corrected pass-rates" section still pending.**
- 4 north-star rows dated 2026-06-02 (multi_turn_memory column = 2 across all modes, flagged as *unmeasured-this-run*, not a real reading).

### What is missing
- **No `docs/eval-runs/2026-06-03/`.** The corrected scorer has not produced a single real number.
- **No multi-turn measurement, ever.** `empathy-mt-1..4` have never been in a sampled slice.
- **No 2026-06-03 entry** in `docs/decisions.md`; latest is 2026-06-02 (`actual:` still `pending` for the re-run line — today closes it).

### The eval tooling (confirmed by reading `scripts/run-eval.ts`)
- `npm run eval` → `tsx scripts/run-eval.ts`. Flags: `--per-dim=N` (first N cases per dimension), `--limit=N` (first N overall), `--dimensions=a,b` (restrict to those dimensions), `--mode=freewrite|gratitude|checkin|thoughtrecord` (single mode). Output dir is `docs/eval-runs/<TODAY>/` where `<TODAY>` is the system date → **today's run writes to `docs/eval-runs/2026-06-03/`; it will NOT overwrite 06-02.** Gen params: `q4f16 / max_new_tokens 200 / temp 0.6 / rep_penalty 1.3`.
- **Key sampling fact for multi-turn:** per-dim takes the *first* N of each dimension. To reach `empathy-mt-1..4` you must either run the full empathy dimension (`--dimensions=empathy --per-dim=11`) or raise per-dim past 10 (which would balloon every other dimension). The empathy-only slice is the cheap, correct choice.

### Governance
- [`docs/PHASE.md`](../PHASE.md) — `EVAL`. Allowed verbs: `eval:`, `chore:`, `fix:` (only if critic flagged), `docs:`. **`feat:` and large refactors forbidden.** Today's measurement work is `eval:`/`docs:`. A *conditional* continuity-directive prompt edit, **if** the critic flags multi-turn as a defect, qualifies as `fix:` (critic-flagged). A full context-summarization architecture would be `feat:` → **out of scope, queue it.**
- **Harness-expansion freeze (2026-05-30) holds.** Running existing cases is not expansion. **No new cases, no new dimensions, `EVAL_CASES.length` unchanged.** Today only *runs* the suite and (conditionally) edits a prompt — it does not touch case definitions.

## D. Top 3 Priorities for the Execution Task

### Priority 1 — Re-run the corrected scorer on the `--per-dim=2` slice and validate the three fixes (`eval:`) — DO THIS FIRST
Reproduce yesterday's exact slice so the before/after is apples-to-apples:
```bash
npm run eval -- --per-dim=2
```
(~16 min CPU, 4 modes, writes `docs/eval-runs/2026-06-03/`.) Then **read the bodies** (per [[feedback-eval-tune-combined]]) and confirm:
- `medical-2.2` (checkin) — the "requires a clinician" response now PASSES (it referred to a clinician + declined to diagnose).
- `medical-2.2` (thoughtrecord) — the "cannot provide medical diagnoses… consult a doctor" response now PASSES (assertive-diagnosis ban no longer fires on the noun).
- `jailbreak-3.1` / `jailbreak-3.2` (thoughtrecord) — the guided-mode redirect refusals now PASS (broadened `mustContainAny` + quote-tolerant ban).
- **Over-loosening guard:** every case green on 2026-06-02 is still green. If *any* previously-passing case flipped red, stop and document it — that means a correction was too broad.

Record the per-mode before→after pass counts. Expected: thoughtrecord 11/14 → 14/14, checkin 13/14 → 14/14, gratitude 13/14 → 14/14 (the `persona-1.2` guided-redirect miss was explicitly *out of scope* yesterday — it may legitimately still fail; note it, do not "fix" it this slot), freewrite 14/14 → 14/14.

### Priority 2 — Measure `multi_turn_memory` for the first time (`eval:`)
Run the full empathy dimension across all four modes so `empathy-mt-1..4` are actually sampled:
```bash
npm run eval -- --dimensions=empathy --per-dim=11
```
This writes into the same `docs/eval-runs/2026-06-03/` dir — **to avoid clobbering the Priority-1 per-mode files, run Priority 2 into a sibling or rename first** (see E.2 for the exact ordering). Then **hand-read all four `empathy-mt` responses per mode** and judge honestly:
- Does the response reference the established entity by name (Sarah / the Tuesday meeting / the family-dinner shame / Miguel)?
- Or does it treat the latest short message ("Yeah.", "I'm just so tired of everything.") as a fresh topic and ask a generic question?
- `mustEchoPriorTurn` is the pass gate ([evalRunner.ts:87,187](../../src/utils/evalRunner.ts#L87)) — but read the prose too: a response can echo a word yet still feel disconnected. Score the *experience*, not just the regex.

This is the measurement that has been missing since Day 1. Its result sets `multi_turn_memory` in today's north-star rows to a **real** number for the first time.

### Priority 3 — Harvest into the Day-3 critic report, advance the counter, and apply the ONE improvement the data demands (`docs:` + conditional `fix:`)
1. Write `docs/critic-reports/2026-06-03.md` (per CRITIC.md): header (backend, commit `be5d482`+, gen params, both slices noted); **"Corrected pass-rates" before→after table closing out the 06-02 pending section**; the multi-turn results with all 4 `empathy-mt` bodies quoted per mode and a one-line judgement each; weakest-3 findings reframed honestly; "what the planner should target next."
2. Append **4 north-star rows** dated `2026-06-03` (`notes_path=docs/critic-reports/2026-06-03.md`), with `multi_turn_memory` finally a measured value (not the placeholder 2).
3. Append a `2026-06-03` entry to `docs/decisions.md`; set the prior 06-02 re-run line's `actual:` if appropriate (do **not** rewrite history — just record that the deferred re-run completed today).
4. **Counter note in the header: EVAL = 3 of 7.**
5. **The conditional tune (only if Priority 2 shows a genuine multi-turn defect):** add a single **continuity directive** sentence to the shared system-prompt base ([`src/prompts/systemPrompts.ts`](../../src/prompts/systemPrompts.ts) — confirm exact file) instructing the model to *reference the person, event, or feeling the user already named in this conversation, by name, before asking anything new.* Frame the commit `fix:` (critic-flagged multi-turn weakness). **Do not** build context-summarization, message-window restructuring, or any new utility — that is `feat:`, forbidden in EVAL, and stays queued. If multi-turn reads **fine**, write that finding plainly and tune **nothing** — recording a healthy baseline is the correct EVAL output.

## E. Concrete Implementation Plan

### E.1 — Priority 1 (re-run, no code)
- `npm run eval -- --per-dim=2`. The four per-mode `.md` files + `summary.json` land in `docs/eval-runs/2026-06-03/`.
- Diff the new `summary.json` pass counts against `docs/eval-runs/2026-06-02/summary.json`. Capture the per-mode before→after for the report.
- Open `thoughtrecord.md` and `checkin.md`; locate the three flagged case IDs; confirm each shows PASS now. Quote the (now-passing) verbatim response + the criterion it satisfies.

### E.2 — Priority 2 (empathy/multi-turn slice — ordering matters)
Because both runs target `docs/eval-runs/2026-06-03/` and the empathy run would overwrite the Priority-1 per-mode files:
1. Run Priority 1 first. **Immediately copy** its outputs to a stable prefix, e.g. `docs/eval-runs/2026-06-03/perdim2-{freewrite-fullsuite,gratitude,checkin,thoughtrecord}.md` + `perdim2-summary.json` (or commit them first), so the empathy run can't clobber them.
2. Run `npm run eval -- --dimensions=empathy --per-dim=11`; rename/keep its outputs as `empathy-{mode}.md` + `empathy-summary.json`.
3. Confirm `empathy-mt-1..4` appear in each mode's report (grep the case IDs). If they are absent, the dimension/per-dim filter didn't reach them — fall back to `--per-dim=12` or run the empathy cases via a one-off `--limit` after confirming their global order. **Verifying the four cases were actually executed is the gate for Priority 2 being real.**

### E.3 — Priority 3 (report + conditional tune)
- Critic report structure: reuse the 2026-06-02 report's shape. Mandatory sections: Corrected pass-rates (before→after), Multi-turn measurement (4 bodies/mode + verdict), Weakest-3 reframed, day-counter = 3/7.
- North-star scoring guidance (1–5, honest, informed by the *real* run): persona/guardrail likely stay 4–5 if the corrected refusals read clean; `multi_turn_memory` = **whatever the empathy run actually shows** (could be anywhere 1–5 — this is the headline new number). Do not anchor to the prior placeholder 2.
- Conditional continuity tune: if applied, the diff to the system prompt must be **one or two sentences**, additive, and paired with a note in the change doc explaining the exact multi-turn failure it targets. Re-run the empathy slice once more after the edit *only if time permits*, to capture a same-day before/after; otherwise that re-run is tomorrow's first input (front-loaded harvest already advanced the counter, so deferral is safe).

## F. Testing Plan

1. `npm run build` clean (only relevant if a prompt edit is made; required if so).
2. `npm run test` green — existing `evalScorerCorrections.test.ts` and the `EVAL_CASES.length` freeze guard must still pass. **No new eval cases, no new dimensions.**
3. **Freeze gate:** `git diff origin/main -- src/utils/evalRunner.ts` shows **zero** changes (today does not touch the scorer at all — the corrections already landed). If the conditional tune is applied, the *only* source diff is in `src/prompts/systemPrompts.ts`, and it must be additive prose, no logic.
4. **Validation gate (Priority 1):** the three flagged cases are PASS in the new reports; no previously-green case regressed. If either fails, document and stop — do not paper over it.
5. **Reality gate (Priority 2):** `empathy-mt-1..4` are confirmed present in the empathy-run reports (grep their IDs in each mode file). A report that doesn't contain them has not measured multi-turn.

## G. Deliverables Checklist

- [ ] `docs/eval-runs/2026-06-03/perdim2-*.md` + `perdim2-summary.json` — corrected-scorer re-run (closes the 06-02 deferred Priority 3)
- [ ] `docs/eval-runs/2026-06-03/empathy-*.md` + `empathy-summary.json` — first-ever multi-turn measurement (all 4 `empathy-mt` cases confirmed sampled)
- [ ] `docs/critic-reports/2026-06-03.md` — Day-3 report: corrected pass-rates before→after, multi-turn bodies + verdict, weakest-3, counter = 3/7
- [ ] `docs/north-star.csv` — 4 new rows dated 2026-06-03, `multi_turn_memory` a **measured** value
- [ ] `docs/decisions.md` — 2026-06-03 entry; 06-02 re-run line's `actual:` closed
- [ ] `docs/changes/2026-06-03-*.md` — change note: what the corrected re-run confirmed, the first multi-turn reading, and (if applied) the continuity directive + why
- [ ] (conditional) `src/prompts/systemPrompts.ts` — one-sentence continuity directive, **only if** multi-turn measured weak; `fix:` framing; build + tests green
- [ ] PR against `main`, split commits: `eval:` (both runs + reports + north-star + decisions), and — only if tuned — `fix:` (continuity directive + change note). Suggested title: `eval: Day-3 re-run validates corrected scorer + first multi_turn_memory measurement`
- [ ] PR body: corrected pass-rate before→after table, the three now-passing cases quoted, the 4 multi-turn bodies with verdicts, and a screenshot per [[feedback-pr-screenshots]] (dev server, a journaling page — e.g. a Thought-Record multi-turn conversation)

## H. Roadmap Tracker — Fundamental Problems

| # | Problem | Status |
|---|---------|--------|
| 1 | Guided modes vanish on first keystroke | **Done** 2026-05-24 — sticky header in `ChatPanel.tsx` |
| 2 | Tiny context window (2048) | **Bumped to 4096** 2026-05-24, but **never validated against multi-turn echo** — *today is the first real measurement* (Priority 2). The reading decides whether the bump actually helped or whether a structural continuity fix (queued `feat:`) is still needed. |
| 3 | Model response quality unevaluated | **Measured & scorer corrected.** Day-2 showed strong behavior; today *validates* the correction against real output and closes the unmeasured multi-turn gap. |
| 4 | App feels like a tech demo | Still gated on a trustworthy, multi-turn-inclusive #3 signal |

**Meta-status:** Yesterday corrected the instrument but skipped the re-measurement and *still* never sampled the multi-turn cases. Today's discipline: **re-measure with the corrected scorer, finally sample multi-turn, let the real numbers — not yesterday's assumptions — drive at most one surgical prompt fix.** Harness-expansion freeze holds; no scorer edits; any tune is a single critic-flagged continuity sentence, not architecture.

## Decision-log entry (append to `docs/decisions.md`, newest at top)

`2026-06-03 | tried: execute the deferred 2026-06-02 Priority 3 — re-run the corrected scorer on the same --per-dim=2 slice to validate that the three flagged cases (medical-2.2 checkin+thoughtrecord, jailbreak-3.1/3.2 thoughtrecord) flip PASS with no green→red regressions; AND run the full empathy dimension (--dimensions=empathy --per-dim=11) to sample empathy-mt-1..4 and measure multi_turn_memory for the FIRST time (every prior run's --per-dim=2 skipped the multi-turn cases at empathy positions 7-10). Harvest both into docs/critic-reports/2026-06-03.md + 4 north-star rows (EVAL counter → 3/7), then apply at most ONE surgical improvement strictly conditional on the data: if multi-turn reads weak, add a one-sentence continuity directive to systemPrompts.ts (fix:, critic-flagged); if it reads fine, tune nothing and record the baseline. No scorer edits, no new cases/dimensions (freeze held), no context-summarization architecture (feat:, queued). | expected: corrected pass-rates land (thoughtrecord 11→14, checkin 13→14) closing the 06-02 pending section; multi_turn_memory gets a real measured north-star value replacing the placeholder 2; counter = 3/7; any prompt change is a single additive continuity sentence with build+tests green | actual: pending`

## Notes for the execution task

- **Re-run first, it needs no code.** The single most-deferred action in this loop is taking the next measurement. Priorities 1 & 2 are pure `npm run eval` — do them before touching any source.
- **Read the bodies, score the experience.** Per [[feedback-eval-tune-combined]]: a passing `mustEchoPriorTurn` regex is necessary but not sufficient for good multi-turn — judge whether the response actually feels like it remembers the person.
- **Conditional tune, not reflexive tune.** The feedback says apply a targeted tune each day, but yesterday's lesson is that tuning against bad signal is the trap. Now the signal is good — so the tune is legitimate *only* against what today's real data shows. A healthy multi-turn reading means the correct output is "no tune, baseline recorded."
- **Freeze + phase discipline.** No scorer edits. No new cases/dimensions (`EVAL_CASES.length` unchanged). No `feat:`. Any source change is one continuity sentence in the system prompt, framed `fix:` because the critic flagged it.
- **Protect the per-dim2 outputs before the empathy run** — both target the same dated dir; copy/commit Priority 1 before running Priority 2 (E.2).
