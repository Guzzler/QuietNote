# Daily Plan — 2026-04-05

## A. Today's Objective

Begin the runtime adapter architecture (Phase 1 of the Gemma migration plan) and design the first guided journaling mode — these are the two highest-impact improvements that move QuietNote forward on Objectives 1B and 2/3.

## B. Why This Matters

The runtime adapter is the single most important architectural change remaining. It unblocks every future model and runtime improvement — MediaPipe, Chrome Built-in AI, Gemma-3n E2B, and eventually Gemma 4. Without it, QuietNote remains locked to a single inference stack. Meanwhile, guided journaling modes expand the app's core value proposition beyond free-form chat, giving users structured tools for self-reflection.

## C. Model Quality Assessment

**Environment limitation:** The preview environment lacks WebGPU, so the model cannot load. Error recovery UI was verified instead.

**Previous assessments (through 2026-04-04):**
- Response quality rated 4/5 for both positive and anxious journal entries
- Responses are warm, reflective, appropriately brief (3-4 sentences), and contextual
- Crisis detection and guardrails function correctly
- System prompt successfully blocks medical advice and keeps responses concise
- No new system prompt changes needed at this time

**Specific tests run this session (structural only — no model inference):**
1. Welcome screen renders correctly on desktop and mobile
2. Error recovery UI (model load failure -> "Something went wrong" + Retry button) works
3. Mood tracker modal opens with all 10 emotions, dialog role, save/reset buttons
4. Sessions panel toggles correctly on mobile
5. Sessions panel shows "No saved sessions yet." when empty
6. Desktop layout shows Sessions panel on right, all header button labels visible
7. No console errors beyond expected model load failure (`Cache` API error in preview env)

**Recommendation:** Model quality testing should be prioritized when a WebGPU-capable environment is available. Current Gemma 2 2B responses are adequate but the move to Gemma-3n E2B (Phase 2) will require fresh quality evaluation.

## D. Journaling Modes Exploration

### Current State
QuietNote currently supports only **free-form conversational journaling**. The types file (`src/types.ts`) already defines types for CBT (`ThoughtRecord`, `CognitiveDistortion`), DBT (`DBTSkillPractice`), and structured prompts — but none are wired to the UI.

### Proposed Journaling Modes (ordered by value and feasibility)

#### 1. Guided Gratitude Journaling (BUILD NEXT)
- **How it works:** User selects "Gratitude" mode. The AI presents a structured 3-prompt sequence: (1) "What are you grateful for today?" (2) "Why does this matter to you?" (3) "How did it make you feel?" Each response is saved as a structured gratitude entry.
- **Model integration:** Modify the system prompt per mode to shape the AI's follow-up questions around gratitude themes. The model's role shifts from open-ended reflection to guided facilitation.
- **UI:** A mode selector in the input area (dropdown or tabs). When in Gratitude mode, the welcome card changes to show the gratitude prompt sequence. Progress indicator (1/3, 2/3, 3/3).
- **Why first:** Gratitude journaling is the most well-researched evidence-based journaling practice. It's structured enough to differentiate from free-form but simple enough to implement quickly. The existing `journalPrompts.ts` already has 8 gratitude prompts.

#### 2. Morning/Evening Check-in
- **How it works:** Time-aware mode that presents a short structured check-in. Morning: "How are you feeling? What do you want to focus on today? Any concerns?" Evening: "How was your day? What went well? What would you do differently?"
- **Model integration:** System prompt shifts to check-in facilitator. Responses are shorter and more structured.
- **UI:** Auto-suggested based on time of day. 3-step wizard-style input.

#### 3. CBT Thought Records
- **How it works:** Guided 7-column thought record: Situation -> Automatic Thought -> Emotions -> Evidence For -> Evidence Against -> Alternative Thought -> Re-rating. The AI helps identify cognitive distortions.
- **Model integration:** The model processes each step and offers gentle observations. Types already defined in `types.ts`.
- **UI:** Multi-step form with AI assistance at each step.

#### 4. Stream-of-Consciousness with AI Summary
- **How it works:** User writes freely for a set duration. When done, the AI summarizes themes, emotions, and patterns.
- **Model integration:** Single longer generation at the end rather than turn-by-turn.

**Recommendation:** Build Guided Gratitude first. It's the smallest scope, has clear structure, leverages existing prompt data, and provides a template for other modes.

## E. Model & Runtime Research

### BREAKING: Gemma 4 Released April 3, 2026

Gemma 4 launched just 2 days ago. This significantly changes the runtime landscape since the design doc was written on 2026-04-02.

**Gemma 4 details:**
- **Four sizes:** E2B, E4B, 26B MoE, 31B Dense — all Apache 2.0
- **New architecture features:** Per-Layer Embeddings (PLE), Variable Head Dimensions, KV Cache Sharing
- **Designed for edge/browser deployment** with E2B/E4B variants

### Gemma 4 E2B — Browser Runtime Availability

| Runtime | Gemma 4 E2B Status | Notes |
|---------|-------------------|-------|
| **Transformers.js v4** | **YES — Working** | ONNX Q4 checkpoint at `onnx-community/gemma-4-E2B-it-ONNX`. Live demo at `webml-community/Gemma-4-WebGPU`. ~13k downloads. Supports text, image, audio. **Most mature browser path today.** |
| **MediaPipe/LiteRT-LM** | **YES — Text-only** | Community bundle at `litert-community/gemma-4-E2B-it-litert-lm`. Web uses `gemma-4-E2B-it-web.task` via WebGPU. No multimodal on web yet. E4B variant has reported issues. |
| **@mlc-ai/web-llm** | **NOT YET** | PLE architecture support needed. Gemma 3 support was still being worked on (issues #675, #727). May take weeks. |
| **ONNX Runtime Web (native)** | **BLOCKED** | `onnxruntime-genai` v0.12.2 doesn't support PLE/variable heads/KV sharing. However, Transformers.js wraps ONNX Runtime Web internally and works. |
| **Chrome Built-in AI** | **NO** | Prompt API updates target Gemini Nano, not arbitrary Gemma models. |

### Gemma-3n E2B Status
- **Available** in `.litertlm` format at `google/gemma-3n-E2B-it-litert-lm` for web
- **Web version** uses WASM + WebGPU with platform-specific optimizations
- **~3GB** — lightweight enough for browser use
- **More battle-tested** on web than Gemma 4 (available longer)
- **Multimodal support** (images + audio) via MediaPipe LLM Inference

### Significant Runtime Updates Since Design Doc (2026-04-02)

| Update | Impact |
|--------|--------|
| **Transformers.js v4** (Feb 2026) | C++ rewrite with WebGPU backend. 3-10x speedup. 200+ architectures. Llama 3.2 3B at ~60 tok/s. Build 53% smaller. **This changes the recommendation.** |
| **WebGPU universally shipped** | Default-on in Chrome, Firefox, Edge, Safari. No more origin trials. |
| **LiteRT-LM** | Google's successor to TFLite for LLMs. Now the recommended path over MediaPipe LLM Inference for mobile. Web path still uses MediaPipe JS API. |
| **ONNX Runtime Web v2.6** | Direct WASM + WebGPU backends, under 150KB footprint. |

### Updated Recommendation

The design doc recommended **MediaPipe LLM Inference + Gemma-3n E2B**. With Gemma 4's release and Transformers.js v4's maturity, the recommendation should be updated:

**Primary path:** Build the runtime adapter (unchanged) but evaluate **Transformers.js v4 + Gemma 4 E2B ONNX Q4** as the new primary target alongside MediaPipe + Gemma-3n E2B.

**Rationale:**
- Transformers.js v4 has a working Gemma 4 E2B demo right now
- ONNX Q4 quantization keeps model size practical for browser
- Multimodal support (text + image + audio) out of the box
- The adapter architecture makes it low-cost to evaluate both and pick the winner

**Evaluation plan:** Once the adapter is built, implement both backends behind feature flags and compare:
- Startup time / model download size
- Inference speed (tokens/sec)
- Response quality for journaling prompts
- Memory usage
- Browser compatibility

### Changes Since Last Assessment (2026-04-02)
- **Gemma 4 released** (April 3) — E2B already available in Transformers.js and LiteRT-LM
- **Transformers.js v4** is now a top contender (was listed as "Medium" maturity before)
- TypeScript compiles cleanly (`tsc --noEmit`), build has 8 pre-existing errors
- Session deletion implemented (#24), closing UX issue #18
- All 295 tests still pass

## F. Top 3 Priorities for the Execution Task

### 1. Runtime Adapter Interface (Objective 3 — Phase 1)
Create the inference abstraction layer so QuietNote can swap between WebLLM, MediaPipe, and future runtimes without rewriting the app. This is the critical-path blocker for all model/runtime improvements.

### 2. Guided Gratitude Journaling Mode (Objective 1B)
Build the first structured journaling mode. This expands QuietNote's core value proposition and provides a template for future modes (check-ins, CBT thought records).

### 3. Fix Pre-existing TypeScript Build Errors (Code Quality)
Clean up the 8 pre-existing TS errors that cause `npm run build` to fail. These are all minor (unused variables, type mismatches) but they block production builds.

## G. Concrete Implementation Plan

### Priority 1: Runtime Adapter Interface

**New files to create:**
- `src/inference/types.ts` — Shared inference engine contract:
  ```typescript
  interface InferenceEngine {
    load(onProgress?: (progress: number, message: string) => void): Promise<void>;
    generate(messages: ChatMessage[], options: GenerateOptions): AsyncIterable<string>;
    resetContext(): Promise<void>;
    dispose(): Promise<void>;
    getStatus(): EngineStatus;
  }
  ```
- `src/inference/webllm-engine.ts` — WebLLM backend implementing `InferenceEngine`, extracted from current `useMLCEngine.ts`
- `src/inference/index.ts` — Factory/registry that returns the appropriate engine based on config

**Files to modify:**
- `src/hooks/useMLCEngine.ts` — Refactor to `src/hooks/useInferenceEngine.ts`, consuming the adapter interface instead of WebLLM directly
- `src/App.tsx` — Update imports from `useMLCEngine` to `useInferenceEngine`; all other logic unchanged
- `src/utils/webgpuCheck.ts` — Move capability detection into the engine adapter (each backend owns its own capability check)

**What to test:**
- All 295 existing tests still pass
- App loads and behaves identically (WebLLM is still the only backend)
- `npm run build` succeeds (after TS fixes)
- Error recovery path still works

**How to verify:**
- Run `npm run test` — all pass
- Run `npm run build` — no new errors
- Start dev server, verify welcome screen loads, error recovery works

### Priority 2: Guided Gratitude Journaling Mode

**New files to create:**
- `src/components/JournalingModeSelector.tsx` — Compact mode selector (dropdown or pill tabs) for the input area. Modes: "Free Write" (default), "Gratitude"
- `src/components/GratitudeGuide.tsx` — Step indicator and structured prompts for the gratitude flow (3 steps)

**Files to modify:**
- `src/App.tsx` — Add `journalingMode` state ("freewrite" | "gratitude"). Pass to ChatPanel. When in gratitude mode, modify the system instruction to focus on gratitude facilitation.
- `src/components/ChatPanel.tsx` — Render `JournalingModeSelector` in the input area. When mode is "gratitude", show `GratitudeGuide` instead of the generic welcome card. Track step progress (1/3, 2/3, 3/3).
- `src/data/journalPrompts.ts` — Add a `GRATITUDE_SEQUENCE` export with the 3 structured prompts for the guided flow.

**System prompt for gratitude mode:**
```
You are Quietnote in Gratitude Journaling mode. Guide the user through a 3-step gratitude reflection:
1. What they're grateful for
2. Why it matters to them
3. How it makes them feel

After each response, gently acknowledge what they shared and move to the next step.
Keep responses warm and brief (2-3 sentences). Do not give advice.
```

**What to test:**
- Mode selector appears and switches between modes
- Gratitude mode shows step indicator
- System prompt changes based on mode
- Existing free-form journaling still works when mode is "Free Write"

### Priority 3: Fix Pre-existing TypeScript Build Errors

**Files to modify:**
- `src/components/MoodTracker.tsx:141` — Remove unused `getIntensityColor` declaration
- `src/utils/__tests__/moodPatterns.test.ts:7` — Remove unused `detectDayOfWeekPatterns` import
- `src/utils/__tests__/systemPromptEval.test.ts:7` — Remove unused `EvalResult` import
- `src/utils/crisisDetection.ts:163` — Fix impossible `"critical"` comparison (type only allows `"none" | "high"`)
- `src/utils/evalScorer.ts:263-264` — Remove unused `primaryScore` and `primaryWeight` declarations
- `src/utils/webgpuCheck.ts:19,27` — Add proper WebGPU type declarations (`/// <reference types="@webgpu/types" />` or inline `navigator.gpu` typing)

**What to test:**
- `npm run build` succeeds with zero errors
- `npm run test` still passes all 295 tests

## H. Testing Plan

### Automated Tests
- Run `npm run test` — verify all 295 tests pass after changes
- Run `npm run build` — verify zero TypeScript errors after TS fixes

### Manual Tests (for execution task)
- **Runtime adapter:** Start dev server, verify app loads identically. Verify error recovery. Verify model load flow unchanged.
- **Gratitude mode:** Switch to Gratitude mode via selector. Verify step indicator shows. Send a message in gratitude mode. Switch back to Free Write, verify normal behavior.
- **Structural verification:** Use preview snapshots to verify welcome screen, mode selector, and gratitude guide render correctly on desktop and mobile.

### Regression Checks
- Free-form journaling still works as before
- Session creation, loading, and deletion still work
- Mood tracker, privacy dashboard, and crisis resources still function
- Auto-scroll and auto-grow textarea still work

## I. Deliverables

- [ ] `src/inference/types.ts` — Inference engine interface
- [ ] `src/inference/webllm-engine.ts` — WebLLM adapter
- [ ] `src/inference/index.ts` — Engine factory
- [ ] `src/hooks/useInferenceEngine.ts` — Refactored hook
- [ ] `src/components/JournalingModeSelector.tsx` — Mode selector component
- [ ] `src/components/GratitudeGuide.tsx` — Gratitude flow component
- [ ] TypeScript build errors fixed (8 errors -> 0)
- [ ] All 295 tests pass
- [ ] `npm run build` succeeds
- [ ] PR(s) opened with changes

## J. Roadmap Tracker

### Objective 1: Model Quality & Journaling Modes
| Item | Status |
|------|--------|
| Model output quality (Gemma 2 2B) | Rated 4/5 — adequate, no immediate changes needed |
| System prompt tuning | Stable — medical refusal, conciseness, warmth all working |
| Crisis detection & guardrails | Working correctly |
| **Guided Gratitude mode** | **Planned for today** |
| Morning/Evening Check-in mode | Designed, not yet planned |
| CBT Thought Records mode | Types defined, UI not started |

### Objective 2: Port to Gemma 4 E2B / Explore Models
| Item | Status |
|------|--------|
| Gemma 4 E2B availability assessment | **Updated** — Released April 3! Working in Transformers.js v4 + LiteRT-LM |
| Gemma-3n E2B as interim/fallback target | Confirmed — `.litertlm` bundles available, more battle-tested |
| Transformers.js v4 evaluation | **New** — Most mature Gemma 4 browser path, needs benchmarking |
| Model comparison plan | Ready — evaluate after adapter is built |
| **Migration path** | **Phase 1 (adapter) planned for today** |

### Objective 3: Explore Local Inference Paradigms
| Item | Status |
|------|--------|
| Design doc written | Done — `docs/design/2026-04-02-local-gemma-runtime-plan.md` |
| Runtime comparison table | Done (in this plan) |
| **Runtime adapter interface** | **Planned for today (Phase 1)** |
| MediaPipe backend (Phase 2) | Blocked on Phase 1 |
| Privacy truthfulness pass (Phase 3) | Not started |
| Verification & audit (Phase 4) | Not started |

### Completed Features
| Date | Feature | PR |
|------|---------|-----|
| 2026-03-28 | Humanize loading screen + welcome animations | #17 |
| 2026-03-29 | Header subtitle + mobile sessions + prompt link | — |
| 2026-03-30 | Humanize footer and placeholder text | #18 |
| 2026-03-31 | Phase 1 safety & correctness fixes | #19 |
| 2026-04-01 | Phase 2 accessibility improvements | #20 |
| 2026-04-02 | Aria-labels, storage display, new session button | #21 |
| 2026-04-03 | Mobile sessions, export feedback, mood grid, animation fix | #22 |
| 2026-04-04 | Auto-scroll, auto-grow textarea, layout overflow fix | #23 |
| 2026-04-04 | Session deletion with confirmation | #24 |

### Remaining UX Issues (from evaluations)
| # | Issue | Priority | Status |
|---|-------|----------|--------|
| 8 | Footer overlap on short viewports | Low | Open |
| 20 | PromptSelector mobile overflow | Medium | Open |
| 22 | No keyboard shortcut hints | Medium | Open |
| 27 | Session titles are raw text truncation | Low | Open |
| 28 | Context trimming notice not dismissible | Low | Open |

---

*Generated by automated scheduled task on 2026-04-05.*
